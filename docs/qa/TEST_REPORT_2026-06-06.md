# Test Report 2026-06-06 / 测试报告 2026-06-06

## Scope / 范围
- Project progress API freshness / 项目进度接口日期新鲜度。
- Settings progress panel fallback and backend snapshot display / 设置页进度卡本地 fallback 与后端快照显示。
- Computed readiness evidence for each progress category / 项目进度分项的后端计算依据。
- Real data-source vendor readiness checklist / 真实数据源供应商筛选与授权清单。
- Market-data vendor acceptance checklist / 行情供应商验收清单。
- News/filings vendor acceptance checklist / 新闻公告供应商验收清单。
- Macro-data vendor acceptance checklist / 宏观数据供应商验收清单。
- Public-statements vendor acceptance checklist / 公开言论供应商验收清单。
- Public-statement identity verification and manual-review gates in the news/filings adapter / 新闻公告适配器中的公开言论身份验证与人工复核门禁。
- Real data-source integration and provider-registry dry-run preflight gates / 真实数据源接入与 Provider 注册表 dry-run 预检门禁。
- Alpha Vantage real quote and real news sentiment connectors / Alpha Vantage 真实报价与真实新闻情绪连接器。
- SEC EDGAR company submissions connector / SEC EDGAR 公司公告连接器。
- AI output validation, citation evidence, human-review, audit, cost, and secret-management readiness gates / AI 输出校验、引用证据、人工复核、审计、成本和密钥管理进度门禁。
- Daily dev-log continuity / 每日开发日志连续性。

## Results / 结果
- Syntax checks passed:
  - `node --check backend/server.mjs`
  - `node --check app.js`
  - `node --check backend/tests/backend-api.test.mjs`
  - `node --check tests/frontend-regression.test.mjs`
- Backend regression passed: 156/156 tests.
- Frontend regression passed: 140/140 tests.
- Full automated regression passed: 302/302 tests.
- Live smoke passed:
  - Webpage `GET /?refresh=20260606-data-preflight#settings`: 200, `text/html`
  - Backend `GET /api/project/progress`: `updatedAt=2026-06-06`, local Demo `100`, public-launch readiness `83.9`
  - Served `app.js` contains `2026-06-06`, `100`, `83.9`, `302 条自动化回归`, `每日开发日志已延续`, `apiProjectProgress`, and `/api/project/progress`
  - Backend `GET /api/project/progress` contains readiness items for `data-sources`, `ai-analysis`, `production-database`, `auth-security`, `compliance-release`, and `deployment-ops`
  - Backend `GET /api/project/progress` now reports `source=mock-backend-computed-readiness`, `readinessEvidence.mode=computed-from-backend-status`, `readinessAverage=79`, data-source evidence `30/32`, AI-analysis evidence `17/19`, production-database evidence `22/24`, auth-security evidence `16/17`, compliance-release evidence `15/18`, and deployment-ops evidence `16/18` checks passed
  - Backend market-data adapter exposes `alphaVantageConnector` for `GLOBAL_QUOTE`, A/HK/US symbol mapping, explicit network opt-in, API-key redaction, and `alphaVantageSmokeTestPlan`; tests cover symbol mapping, response parsing, and provider-request behavior with a fake Alpha Vantage response.
  - Backend news/filings adapter exposes `alphaVantageNewsConnector` for `NEWS_SENTIMENT`, US ticker mapping, planned A/HK support, explicit network opt-in, API-key redaction, and `alphaVantageNewsSmokeTestPlan`; tests cover response parsing and provider-request behavior with a fake Alpha Vantage news response.
  - Backend news/filings adapter now exposes `secFilingsConnector`, `secFilingsSmokeTestPlan`, and `secFilingsAccessPreflight` for SEC EDGAR company submissions. Tests cover SEC response parsing and explicit-network provider-request behavior with a fake SEC response.
  - Backend and Settings now expose Alpha Vantage quote/news credential preflight states as `missing`, `demo-key-limited`, or `configured-redacted`; tests confirm forbidden audit fields include `apiKey` so real keys are not displayed or stored in status output.
  - Backend and Settings now expose `GET /api/data-sources/provider-setup-guide`, which lists real provider env vars, smoke order, manual actions, forbidden audit fields, and no-secret/no-live-runtime gates.
  - Backend provider registry now includes `alpha-vantage` as a market-data candidate, so the Settings data-source panel no longer labels the selected Alpha Vantage provider as unsupported when the demo connector is enabled.
  - Backend provider registry now includes `alpha-vantage-news` as a market-news candidate, so news sentiment can be enabled behind `FINANCE_AI_NEWS_PROVIDER=alpha-vantage-news`, `FINANCE_AI_NEWS_API_KEY`, and `FINANCE_AI_NEWS_ALLOW_NETWORK=true` without turning the default fixture runtime into live mode.
  - Local Alpha Vantage smoke passed with demo configuration: `GET /api/market-data/quote?market=us&code=IBM` returned `mode=real-provider`, `provider.id=alpha-vantage`, `source.label=Alpha Vantage GLOBAL_QUOTE`, `lastPrice=284.84`, and `asOf=2026-06-05T00:00:00.000Z`.
  - Local Alpha Vantage NEWS_SENTIMENT smoke reached the provider path with `FINANCE_AI_NEWS_PROVIDER=alpha-vantage-news`, but the `demo` key returned Alpha Vantage's free-key notice; `GET /api/news/intelligence?market=us&symbol=AAPL&minImportance=70` correctly returned `mode=fixture`, `sourceStatus=provider-error-fixture-fallback`, and a visible `providerError` instead of mislabeling fallback news as real provider data.
  - Backend data-source readiness counts `integrationDryRunPreflightDefined`, `providerRegistryPreflightDefined`, `alphaVantageProviderRegistered`, `vendorContractHandoffPackageDefined`, `providerSecretQuotaRunbookDefined`, `providerSetupGuideDefined`, `providerSetupSmokeOrderDefined`, `secCompanyFilingsConnectorDefined`, and `secCompanyFilingsAccessPreflightDefined` as passed while `integrationPlan` and `providerRegistry` remain blocked.
  - Backend `GET /api/data-sources/vendor-contract-handoff` reports `dry-run-no-contract-signing`, required contract/licensing artifacts, forbidden sensitive artifacts, no provider runtime enablement, and explicit no-signed-provider disclaimer
  - Backend `GET /api/data-sources/provider-secret-quota-runbook` reports `dry-run-no-secret-use`, vault field requirements, quota/cost controls, secret redaction, hash-chain audit controls, and explicit no-secret/no-network disclaimer
  - Backend `GET /api/data-sources/provider-setup-guide` reports `no-secret-provider-setup-guide`, four provider setup groups, `marketDataQuote -> newsSentiment -> macroContext -> publicStatements` smoke order, forbidden `apiKey` audit fields, and explicit no-live-runtime gates
  - Backend `GET /api/data-sources/vendor-readiness` reports contact order `marketData -> marketNews -> macroData -> publicStatements`, `3/8` checklist items passed, and explicit no-contract/no-live-data disclaimer
  - Backend `GET /api/data-sources/market-data-vendor-checklist` reports quote/history/calendar/delay-label acceptance areas, `3/9` checklist items passed, vendor questions, and explicit no-live-market-data disclaimer
  - Backend `GET /api/data-sources/news-filings-vendor-checklist` reports headline/excerpt/source-link/retention/paywall acceptance areas, `3/9` checklist items passed, vendor questions, and explicit no-real-news disclaimer
  - Backend `GET /api/data-sources/macro-data-vendor-checklist` reports rates/FX/inflation/policy-event/revision acceptance areas, `3/10` checklist items passed, vendor questions, and explicit no-real-macro-data disclaimer
  - Backend `GET /api/data-sources/public-statements-vendor-checklist` reports verified-identity/source/speaker/platform/excerpt/review acceptance areas, `3/10` checklist items passed, vendor questions, and explicit no-real-public-statements disclaimer
  - Backend `GET /api/data-sources/news-filings-adapter` exposes `publicStatementVerificationPolicy` and `publicStatementManualReviewPolicy` gates with blocked preflight checks
  - Backend `GET /api/ai-services/provider-adapter` exposes `responseValidationPolicy`, `citationEvidencePolicy`, `humanReviewPolicy`, `auditPolicy`, `budgetPolicy`, `secretManagementPolicy`, `supportedProviderIds`, `endpointContracts`, `modelCallPreflightPlan`, `modelEvaluationEvidencePackage`, and `modelReleaseRollbackEvidencePackage`; project progress counts those policy/preflight/evidence structures as passed readiness gates while live model runtime remains blocked
  - Backend `GET /api/auth/provider-adapter` exposes `credentialStoragePolicy`, `sessionSecurityPolicy`, and `csrfProtectionPolicy`; project progress counts those defined safety structures as passed readiness gates while production auth remains blocked
  - Backend `GET /api/auth/provider-adapter` exposes `mfaPolicy`, `emailVerificationPolicy`, and `oidcCallbackPolicy`; project progress counts those defined safety structures as passed readiness gates while production auth remains blocked
  - Backend `GET /api/auth/provider-adapter` exposes `roleAuthorizationPolicy`, `loginRiskPolicy`, and `accountRecoveryPolicy`; project progress counts those defined safety structures as passed readiness gates while production auth remains blocked
  - Backend `GET /api/auth/provider-adapter` exposes `auditLoggingPolicy` and `privacyConsentPolicy`; project progress counts those defined structures as passed readiness gates while production auth and legal/privacy review remain blocked
  - Backend `GET /api/compliance/status` exposes fixed disclaimer, suitability questionnaire, risk acknowledgement, suitability enforcement, jurisdiction enforcement, disclosure versioning, licensed-adviser review, legal-review preflight, and public-release evidence package policies; project progress counts those defined compliance structures while user acknowledgement, legal review, and public release remain blocked
  - Backend `GET /api/notification-services` and `GET /api/scheduler/status` expose notification receipt/webhook/suppression/bounce/preflight/observability policies and scheduler queue/run-safety/backpressure/worker-auth/runbook/preflight/incident-drill policies; project progress counts those defined operations structures while external delivery and background workers remain blocked
  - Backend `GET /api/project/progress` counts production PostgreSQL repository adapter, read-only smoke, SQL contract, execution, parameter validation, pool, SQL executor, result audit, read rehearsal, parity, dual-write, shadow-write, backup/restore evidence structure, monitoring, rollback, audit-trail, and manual cutover structures while keeping real connection and runtime cutover blocked
  - In-app browser DOM-visible render check target: Settings page should show `100%`, `83.9%`, `真实数据源 100%`, `真实 AI 分析 75%`, `部署运维 64%`, `计算依据 30/32 项通过`, `计算依据 17/19 项通过`, `SEC 公告连接器`, `SEC 公告访问预检`, `配置向导`, `Smoke 顺序`, and `302 条自动化回归`.
  - In-app browser screenshot capture timed out twice; visible DOM verification and live API smoke passed.
  - Served `app.js` and `styles.css` contain `readiness-grid`, `readiness-item`, `readiness-evidence`, `readiness-source`, and `compact-track`

## Cases / 用例
- **TC-440 Project Progress Freshness / 项目进度新鲜度：** `GET /api/project/progress` should return `updatedAt: 2026-06-06`, local Demo `100%`, public-launch readiness `83.9%`, and a completed item showing the daily log has continued to `2026-06-06`.
- **TC-441 Project Readiness Breakdown / 项目分项准备度：** `GET /api/project/progress` should return six readiness categories, and the Settings progress card should render category progress for real data sources, AI analysis, production database, auth security, compliance release, and deployment operations.
- **TC-442 Computed Readiness Evidence / 分项计算依据：** `GET /api/project/progress` should derive readiness from backend subsystem status, expose evidence counts and source endpoints, and the Settings card should render those evidence summaries.
- **TC-443 Vendor Readiness Checklist / 供应商授权清单：** `GET /api/data-sources/vendor-readiness` should expose market/news/macro/public-statement provider groups, licensing checks, cost drivers, contact order, next actions, and a disclaimer that no provider is signed or connected.
- **TC-463 Data-Source Contract And Secret Handoff / 数据源合同与密钥交接：** `GET /api/data-sources/vendor-contract-handoff`, `GET /api/data-sources/provider-secret-quota-runbook`, and the Settings provider panel should expose contract/licensing handoff, secret vault, quota, fallback, redaction, and hash-chain controls while proving no provider is signed, no secrets are read, no provider network is called, and no real runtime is enabled.
- **TC-468 Real Provider Setup Guide / 真实 Provider 配置向导：** `GET /api/data-sources/provider-setup-guide`, `GET /api/data-sources`, `GET /api/project/progress`, and the Settings provider panel should expose real provider env vars, smoke order, forbidden audit fields, no-secret/no-env-write/no-live-runtime gates, and readiness evidence without exposing API keys.
- **TC-464 AI Model Evidence Gates / AI 模型证据门禁：** `GET /api/ai-services/provider-adapter` and `GET /api/project/progress` should expose model evaluation evidence and model release rollback evidence, count them as readiness evidence, and keep live model calls, model certification, user-visible advice publication, and live runtime blocked.
- **TC-444 Market-Data Vendor Checklist / 行情供应商验收清单：** `GET /api/data-sources/market-data-vendor-checklist` should expose quote/history/calendar/delay-label acceptance fields, provider questions, licensing/cache/rate-limit/audit checks, next actions, and a disclaimer that no live/delayed provider is connected.
- **TC-445 News-Filings Vendor Checklist / 新闻公告供应商验收清单：** `GET /api/data-sources/news-filings-vendor-checklist` should expose headline-summary, short-excerpt, source-link, retention, attribution, paywall-boundary fields, provider questions, next actions, and a disclaimer that no real news/filings provider is connected.
- **TC-469 SEC Filings Connector / SEC 公告连接器：** `GET /api/data-sources/news-filings-adapter`, `GET /api/news/filings`, and `GET /api/project/progress` should expose SEC EDGAR company-submissions connector metadata, no-API-key but User-Agent-required preflight, AAPL smoke expectations, source URL attribution, fixture fallback on provider failure, and readiness evidence without exposing raw provider responses.
- **TC-446 Macro-Data Vendor Checklist / 宏观数据供应商验收清单：** `GET /api/data-sources/macro-data-vendor-checklist` should expose rate, FX, inflation, policy-event, revision-policy, asOf, timezone, audit fields, provider questions, next actions, and a disclaimer that no real macro provider is connected.
- **TC-447 Public-Statements Vendor Checklist / 公开言论供应商验收清单：** `GET /api/data-sources/public-statements-vendor-checklist` should expose verified identity, source URL, speaker role, platform terms, short-excerpt, manual-review, audit fields, provider questions, next actions, and a disclaimer that no real public-statements provider is connected.
- **TC-448 Public-Statement Policy Gates / 公开言论策略门禁：** `GET /api/data-sources/news-filings-adapter` and the Settings provider panel should expose public-statement identity verification and manual-review policy gates before any real provider runtime is enabled.
- **TC-449 AI Readiness Policy Gates / AI 进度策略门禁：** `GET /api/project/progress` should count defined AI response-validation, citation-evidence, and human-review policy gates as passed readiness evidence while keeping live model execution blocked until provider runtime is safe.
- **TC-450 AI Operational Structure Gates / AI 运行结构门禁：** `GET /api/project/progress` should count defined AI audit, cost-control, and secret-management structures as passed readiness evidence while keeping provider runtime and live model execution blocked.
- **TC-451 AI Provider Preflight Structure Gates / AI Provider 预检结构门禁：** `GET /api/project/progress` should count provider registry, model request envelope, and manual dry-run approval structures as passed readiness evidence while keeping live model execution blocked.
- **TC-452 Auth Safety Structure Gates / 认证安全结构门禁：** `GET /api/project/progress` should count credential storage, session security, and CSRF protection structures as passed auth-security readiness evidence while keeping production auth provider execution blocked.
- **TC-453 Auth MFA Email OIDC Structure Gates / 认证 MFA 邮箱 OIDC 结构门禁：** `GET /api/project/progress` should count MFA, email verification, and OIDC callback structures as passed auth-security readiness evidence while keeping production auth provider execution blocked.
- **TC-454 Auth Role Risk Recovery Structure Gates / 认证角色风控恢复结构门禁：** `GET /api/project/progress` should count role authorization, login risk, and account recovery structures as passed auth-security readiness evidence while keeping production auth provider execution blocked.
- **TC-455 Auth Audit Privacy Structure Gates / 认证审计隐私结构门禁：** `GET /api/project/progress` should count auth audit logging and privacy consent structures as passed auth-security readiness evidence while keeping production auth provider execution and privacy/legal release blocked.
- **TC-456 Compliance Structure Gates / 合规结构门禁：** `GET /api/project/progress` should count fixed disclaimer, suitability questionnaire, risk acknowledgement, suitability enforcement, jurisdiction enforcement, disclosure versioning, licensed-adviser review, legal-review preflight, and public-release evidence package structures as passed compliance-release readiness evidence while keeping public release blocked.
- **TC-459 Local Demo Completion Gate / 本地 Demo 完成门禁：** `GET /api/project/progress` and the Settings card should show local Demo `100%` while keeping formal launch readiness separate and blocked by production gates.
- **TC-457 Deployment Ops Structure Gates / 部署运维结构门禁：** `GET /api/project/progress` should count notification receipt/webhook/suppression/bounce/preflight and scheduler queue/run-safety/backpressure/worker-auth/runbook/preflight structures as passed deployment-ops readiness evidence while keeping real external delivery and background workers blocked.
- **TC-465 Deployment Observability And Incident Drills / 部署可观测与事故演练：** `GET /api/notification-services/provider-adapter`, `GET /api/scheduler/provider-adapter`, and `GET /api/project/progress` should expose notification observability evidence and scheduler incident response drill evidence, count them as readiness evidence, and keep external delivery and background workers blocked.
- **TC-458 Production Database Cutover Structure Gates / 生产数据库切换结构门禁：** `GET /api/project/progress` should count production repository adapter, smoke, SQL, execution, validation, parity, dual-write, shadow-write, backup/restore evidence structure, monitoring, rollback, audit-trail, and manual cutover structures as passed database readiness evidence while keeping real PostgreSQL connection and runtime cutover blocked.
- **TC-460 Production Preflight Evidence Gates / 生产预检证据门禁：** `GET /api/project/progress` should count backup/restore evidence structure, external-delivery dry-run preflight, and background-worker dry-run preflight while proving real database connection, real external delivery, and real background workers remain blocked.
- **TC-461 Data-Source Preflight Evidence Gates / 数据源预检证据门禁：** `GET /api/project/progress` should count real-data-source integration dry-run preflight and provider-registry rollout preflight while proving real provider integration and registry readiness remain blocked.
- **TC-462 Compliance Pre-Release Evidence Gates / 合规预发布证据门禁：** `GET /api/project/progress` should count legal-review dry-run preflight and public-release evidence package structures while proving real risk acknowledgement, legal review, and public release remain blocked.
- **TC-466 Alpha Vantage Real Quote Connector / Alpha Vantage 真实报价连接器：** `GET /api/data-sources`, `GET /api/data-sources/market-data-adapter`, `GET /api/market-data/quote`, and `GET /api/project/progress` should expose the Alpha Vantage `GLOBAL_QUOTE` connector, symbol mapping, demo smoke plan, opt-in network gate, key redaction, and data-source readiness evidence while keeping default runtime on safe fixture fallback unless explicit provider env vars are configured.
- **TC-467 Alpha Vantage Real News Sentiment Connector / Alpha Vantage 真实新闻情绪连接器：** `GET /api/data-sources`, `GET /api/data-sources/news-filings-adapter`, `GET /api/news/intelligence`, and `GET /api/project/progress` should expose the Alpha Vantage `NEWS_SENTIMENT` connector, US ticker mapping, planned A/HK support, demo smoke plan, opt-in network gate, key redaction, normalized source attribution, and data-source readiness evidence while keeping default runtime on safe fixture fallback unless explicit provider env vars are configured.
