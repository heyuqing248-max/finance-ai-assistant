# Test Report 2026-06-05 / 测试报告 2026-06-05

## Scope / 范围
- Reminder-rule account-session stale-response protection.
- Local webpage availability after the user reported the URL could not be opened.
- Portfolio account-session stale-response protection.
- Account role-management stale-response protection.
- Scheduler dead-letter stale-response protection.
- Scheduler worker-health stale-response protection.
- Scheduler queue stale-response protection.
- Worker nonce cleanup stale-response protection.
- Audit retention purge stale-response protection.
- Audit export-chain stale-response protection.
- Email login and saved-session stale-response protection.
- Demo auth token stale-response protection.
- Scheduler/audit maintenance stale demo-token follow-up protection.
- Notification Center auto demo-token continuation protection.
- Portfolio stale demo-token follow-up protection.
- Analysis history stale demo-token follow-up protection.
- Preference stale demo-token follow-up protection.
- Watchlist stale demo-token follow-up protection.
- Reminder-rule stale demo-token follow-up protection.
- Compliance-record stale demo-token follow-up protection.
- AI provider production gate coverage for model cost controls and source grounding.
- AI model audit policy and dry-run preflight plan coverage.
- AI six-factor input quality and weight-version gate coverage.
- PWA offline cache ready/version feedback coverage.
- Audit retention automation dry-run gate coverage.
- Auth provider production login-risk, account-recovery, and dry-run preflight coverage.
- Notification provider receipt, suppression, bounce-handling, and dry-run preflight coverage.
- Scheduler provider backpressure, worker-auth, runbook, and dry-run preflight coverage.
- Startup backend service-cache refresh when local development is already connected.
- Right-side live preview recovery after the local webpage server stopped.
- LAN webpage availability after the user reported the URL could not be opened.
- Frontend regression, full backend/frontend automated regression, HTTP smoke checks, and in-app browser render check.

## Test Cases / 测试用例
- **TC-345 Reminder Add Stale Response / 新增提醒旧响应：** If users sign out while a backend reminder-add request is pending, the late old-account response is ignored and cannot mark the local rule as backend-synced or show stale sync success.
- **TC-346 Reminder Remove Stale Response / 删除提醒旧响应：** If users sign out while a backend reminder-delete request is pending, the late old-account response is ignored and cannot show stale backend-removal success.
- **TC-347 Local Webpage Availability / 本地网页可访问性：** The local stack is running and serves the webpage, app script, and backend health endpoint.
- **TC-348 Browser Render Check / 浏览器渲染检查：** The in-app browser opens `http://127.0.0.1:4173/` and reads rendered Chinese UI text.
- **TC-349 Watchlist Loading State / 自选股加载态：** Backend watchlist synchronization renders the intended loading state instead of silently showing the previous empty/ready state.
- **TC-350 Watchlist Load Stale Response / 自选股读取旧响应：** If users sign out while a backend watchlist load is pending, the late old-account response is ignored and cannot write old-account symbols into local storage or the current page.
- **TC-351 Watchlist Add Stale Response / 自选股加入旧响应：** If users sign out while a backend watchlist-add request is pending, the late response is ignored and cannot show stale backend-sync success.
- **TC-352 Watchlist Remove Stale Response / 自选股移除旧响应：** If users sign out while a backend watchlist-delete request is pending, the late response is ignored and cannot show stale backend-removal success.
- **TC-353 Preference Load Stale Response / 偏好读取旧响应：** If users sign out while backend preference loading is pending, the late old-account response is ignored and cannot change the current risk profile, notification channels, or signed-out status.
- **TC-354 Risk Profile Save Stale Response / 分析风格保存旧响应：** If users sign out while a backend risk-profile save is pending, the late response is ignored and cannot replace the user's local selection or show stale backend-sync success.
- **TC-355 Notification Preference Save Stale Response / 提醒偏好保存旧响应：** If users sign out while a backend notification-preference save is pending, the late response is ignored and cannot overwrite local channel choices or show stale backend-sync success.
- **TC-356 Sign-In/Sign-Out Race / 登录退出竞态：** If sign-in is still chaining account sync steps when sign-out happens, the stale sign-in flow stops and cannot show a later sign-in success message.
- **TC-357 Compliance Acknowledgement Save Stale Response / 风险确认保存旧响应：** If users sign out while risk acknowledgement saving is pending, the late response is ignored and cannot write a local compliance acknowledgement or show stale success.
- **TC-358 Suitability Questionnaire Save Stale Response / 适当性问卷保存旧响应：** If users sign out while suitability questionnaire saving is pending, the late response is ignored and cannot write a local questionnaire record or show stale success.
- **TC-359 Analysis Load Stale Account Response / AI 分析旧账号响应：** If users sign out while an authenticated analysis request is pending, the late old-account response is ignored and cannot render old analysis reasons/actions into the signed-out page.
- **TC-360 Analysis History Save Stale Response / 分析历史保存旧响应：** If users sign out while analysis-history saving is pending, the late response is ignored and cannot show stale history-sync feedback.
- **TC-361 Same-Market News Stale Response / 同市场新闻旧响应：** If users switch from Apple to Microsoft within the US market while Apple news intelligence is pending, the late Apple response is ignored and cannot render into the Microsoft news list.
- **TC-362 Portfolio Load Stale Account Response / 持仓读取旧账号响应：** If users sign out while backend portfolio loading is pending, the late old-account response is ignored and cannot fill current portfolio fields or mark the signed-out page as synced.
- **TC-363 Portfolio Save Stale Account Response / 持仓保存旧账号响应：** If users sign out while backend portfolio saving is pending, the late old-account response is ignored and cannot show stale backend-sync success.
- **TC-364 Auth Role Update Stale Account Response / 角色切换旧账号响应：** If users sign out while a mock role update is pending, the late response is ignored and cannot switch the signed-out page to the old role.
- **TC-365 Admin Role Assign Stale Account Response / 管理员授权旧账号响应：** If users sign out while admin role assignment is pending, the late response is ignored and cannot show stale authorization success.
- **TC-366 Admin Role Revoke Stale Account Response / 管理员撤销旧账号响应：** If users sign out while admin role revocation is pending, the late response is ignored and cannot show stale revocation success.
- **TC-367 Admin Role History Stale Account Response / 角色历史旧账号响应：** If users sign out while role-history refresh is pending, the late response is ignored and cannot render old admin audit rows.
- **TC-368 Scheduler Dead-Letter Refresh Stale Account Response / 死信队列刷新旧账号响应：** If users sign out while dead-letter refresh is pending, the late response is ignored and cannot write old dead-letter jobs into local cache or the scheduler panel.
- **TC-369 Scheduler Dead-Letter Replay Stale Account Response / 死信任务重放旧账号响应：** If users sign out while dead-letter replay is pending, the late response is ignored and cannot mark the job as replayed or show stale replay success.
- **TC-370 Scheduler Worker-Health Refresh Stale Account Response / Worker 健康刷新旧账号响应：** If users sign out while worker-health refresh is pending, the late response is ignored and cannot write old worker telemetry into local cache or the scheduler panel.
- **TC-371 Scheduler Worker Heartbeat Stale Account Response / Worker 心跳旧账号响应：** If users sign out while sample worker heartbeat recording is pending, the late response is ignored and cannot show stale heartbeat success.
- **TC-372 Scheduler Queue Refresh Stale Account Response / 调度队列刷新旧账号响应：** If users sign out while scheduler queue refresh is pending, the late response is ignored and cannot write old queue state into `schedulerQueueState`, local cache, or the scheduler panel.
- **TC-373 Scheduler Queue Enqueue Stale Account Response / 调度队列入队旧账号响应：** If users sign out while sample queue enqueue is pending, the late response is ignored and cannot write a queued job or show stale enqueue success.
- **TC-374 Scheduler Queue Process Stale Account Response / 调度队列处理旧账号响应：** If users sign out while queue processing is pending, the late response is ignored and cannot mark queued jobs completed or show stale process success.
- **TC-375 LAN Webpage Availability / 局域网网页可访问性：** A LAN-bound preview port serves the webpage from the Mac IP so phones, iPads, and other computers on the same network can open the current web prototype.
- **TC-376 Worker Nonce Cleanup Stale Success Response / Worker nonce 清理旧成功响应：** If users sign out while worker nonce cleanup is pending, the late success response is ignored and cannot render cleanup counts or show stale success.
- **TC-377 Worker Nonce Cleanup Stale Error Response / Worker nonce 清理旧失败响应：** If users sign out while worker nonce cleanup is pending and the late response fails, the stale error is ignored and cannot replace the signed-out status.
- **TC-378 Audit Retention Purge Stale Success Response / 审计保留清理旧成功响应：** If users sign out while audit retention purge is pending, the late success response is ignored and cannot render purge counts, integrity hash changes, or stale success.
- **TC-379 Audit Retention Purge Stale Error Response / 审计保留清理旧失败响应：** If users sign out while audit retention purge is pending and the late response fails, the stale error is ignored and cannot replace the signed-out status.
- **TC-380 Audit Export Chain Stale Success Responses / 审计导出链路旧成功响应：** If users sign out while audit export, verify, archive, receipt refresh, download, or replay-preview is pending, the late success response is ignored and cannot render stale audit results, trigger stale downloads, or replace the signed-out status.
- **TC-381 Audit Export Chain Stale Error Responses / 审计导出链路旧失败响应：** If users sign out while audit export, verify, archive, receipt refresh, download, or replay-preview is pending and the late response fails, the stale error is ignored and cannot render stale failure messages or replace the signed-out status.
- **TC-382 Email Login Stale Error Response / 邮箱登录旧失败响应：** If users sign out while email login is pending and the late response fails, the stale error is ignored and cannot replace the signed-out status.
- **TC-383 Saved Session Validation Stale Response / 保存会话校验旧响应：** If users sign out while saved-session validation is pending, late success and failure responses are ignored and cannot restore old account state or replace the signed-out status.
- **TC-384 Demo Auth Token Stale Response / Demo 登录令牌旧响应：** If users sign out while `/api/auth/demo-login` is pending, the late token is ignored and cannot write `apiAuthToken`, render logged-in state, or show stale demo sign-in success.
- **TC-385 Maintenance Follow-Up After Stale Demo Token / 维护操作旧 Demo token 后续请求：** If users sign out while a scheduler maintenance refresh is auto-fetching a demo token, the late token is ignored and the dead-letter API is not requested.
- **TC-386 Notification Mark-Read Auto Demo Token / 通知标记已读自动 Demo token：** If users are in sample-login state without an auth token, marking a notification read auto-fetches a demo token and still calls the backend read endpoint.
- **TC-387 Portfolio Load After Stale Demo Token / 持仓读取旧 Demo token 后续请求：** If users sign out while portfolio loading is auto-fetching a demo token, the late token is ignored and `/api/portfolio` is not requested.
- **TC-388 Analysis History After Stale Demo Token / 分析历史旧 Demo token 后续请求：** If users sign out while analysis-history saving is auto-fetching a demo token, the late token is ignored and `/api/analysis/history` is not requested.
- **TC-389 Preference Save After Stale Demo Token / 偏好保存旧 Demo token 后续请求：** If users sign out while risk-profile preference saving is auto-fetching a demo token, the late token is ignored and `/api/preferences` is not requested.
- **TC-390 Watchlist Add After Stale Demo Token / 自选股加入旧 Demo token 后续请求：** If users sign out while watchlist add is auto-fetching a demo token, the late token is ignored and backend `POST /api/watchlist` is not requested; local watchlist stays saved.
- **TC-391 Reminder Add After Stale Demo Token / 提醒规则加入旧 Demo token 后续请求：** If users sign out while reminder-rule add is auto-fetching a demo token, the late token is ignored and backend `POST /api/reminders` is not requested; the local reminder rule stays saved.
- **TC-392 Compliance Acknowledgement After Stale Demo Token / 风险确认旧 Demo token 后续请求：** If users sign out while compliance acknowledgement saving is auto-fetching a demo token, the late token is ignored and backend `POST /api/compliance/acknowledgements` is not requested.
- **TC-393 AI Provider Cost and Source Gates / AI 成本与来源门禁：** The AI provider adapter reports blocked cost-control and source-grounding gates by default, and only becomes gate-ready when budget, token limit, source coverage, audit, and compliance flags are configured; runtime remains inactive.
- **TC-394 Startup Connected Backend Refresh / 已连接后端启动刷新：** In local development, a page reload with cached `apiHealthStatus=connected` refreshes backend service status and replaces stale AI provider cache with the latest cost/source gate fields.
- **TC-395 AI Model Audit and Preflight Plan / AI 模型审计与调用预检：** The AI provider adapter reports blocked audit policy without an audit sink; preflight remains `dry-run-no-model-call`, forbids raw prompt/API key fields in the audit envelope, and still does not execute live model calls when readiness config is present.
- **TC-396 Auth Login Risk and Preflight Plan / 认证登录风控与预检：** The auth provider adapter reports blocked login-risk and account-recovery policies by default; production-auth preflight remains `dry-run-no-provider-call`, forbids password/token/MFA secret logging, and still does not execute production auth when readiness config is present.
- **TC-397 Notification Receipt and Preflight Plan / 通知回执与投递预检：** The notification provider adapter reports blocked delivery receipts, suppression list, and bounce-handling policies by default; external-delivery preflight remains `dry-run-no-external-send`, forbids recipient identifiers/message bodies in audit envelopes, and still does not send external notifications when readiness config is present.
- **TC-398 Scheduler Backpressure and Worker Preflight / 调度背压与 Worker 预检：** The scheduler provider adapter reports blocked queue backpressure, worker callback auth, and runbook policies by default; worker preflight remains `dry-run-no-worker-start`, forbids worker secrets/signatures/raw payloads in audit envelopes, and still does not start background workers when readiness config is present.
- **TC-399 Market Data Entitlement and Provider Preflight / 行情授权与 Provider 预检：** The market-data provider adapter reports blocked entitlement tiers, delay-label rules, and provider preflight by default; provider preflight remains `dry-run-no-provider-request`, forbids raw ticks/order books/API keys/trading instructions in audit envelopes, and still does not request real providers when readiness config is present.
- **TC-400 News Source Verification and Ingestion Preflight / 新闻来源验证与采集预检：** The news/filings/public-statements adapter reports blocked source-verification, redistribution, and ingestion-precheck policies by default; preflight remains `dry-run-no-provider-fetch`, forbids raw article/social/private-message fields in audit envelopes, and still does not fetch real provider content when readiness config is present.
- **TC-401 Macro Freshness and Provider Preflight / 宏观新鲜度与 Provider 预检：** The macro-data adapter reports blocked indicator-freshness, policy-calendar verification, and provider-precheck policies by default; preflight remains `dry-run-no-provider-fetch`, forbids raw policy/API-key/calendar-session/trading fields in audit envelopes, and still does not fetch real macro provider data when readiness config is present.
- **TC-402 AI Factor Input and Weight Gates / AI 因子输入与权重门禁：** The AI provider adapter reports blocked six-factor input coverage and factor-weight policies by default; preflight requires `factorCoverage` and `factorWeightVersion`, remains `dry-run-no-model-call`, and still does not execute live model calls when readiness config is present.
- **TC-403 PWA Offline Cache Ready Feedback / PWA 离线缓存就绪反馈：** When the service worker reaches ready state, the web app records `offlineCacheReadyVersion=finance-ai-assistant-v3` and shows a quiet one-time offline-cache-ready message; opening the same cache version again does not repeat the message.
- **TC-404 Audit Retention Automation Gate / 审计自动保留清理门禁：** The audit service reports a blocked `audit-retention-automation-plan` by default, keeps `canStartAutomatedPurge=false`, requires scheduler, single-flight lock, archive-before-delete, approval, and rollback-drill checks, and renders those blockers in Settings without starting a real cron or worker.
- **TC-405 Notification Webhook Receipt Verification Gate / 通知 Webhook 回执验签门禁：** The notification provider adapter reports a blocked `notification-webhook-receipt-verification-plan` by default, keeps `canAcceptProviderWebhook=false`, requires webhook secret, endpoint registration, signature timestamp window, replay protection, provider event id idempotency, and redacted receipt audit checks, and renders those blockers in Settings without accepting real provider webhooks.
- **TC-406 Production Database Encryption Gate / 生产数据库加密门禁：** The production database adapter reports a blocked `production-database-encryption-plan` by default, keeps `canUseProductionKeys=false`, requires KMS provider/key id, encryption-at-rest evidence, field-level envelope encryption, key rotation, and encrypted backup evidence, and renders those blockers in Settings without reading real keys or switching production storage.
- **TC-407 Production Database Access Control Gate / 生产数据库权限门禁：** The production database adapter reports a blocked `production-database-access-control-plan` by default, keeps `canApplyDatabaseRoles=false`, requires least-privilege roles, RLS/user isolation, service-role audit, and admin approval, and renders those blockers in Settings without mutating database permissions or creating production roles.
- **TC-408 Production Database Privacy Retention Gate / 生产数据库隐私保留门禁：** The production database adapter reports a blocked `production-database-privacy-retention-plan` by default, keeps `canEraseProductionData=false`, requires retention policy, erasure workflow, subject export, privacy audit, legal-hold review, and privacy approval, and renders those blockers in Settings without exporting or deleting production user data.
- **TC-409 Production Database Residency Transfer Gate / 生产数据库数据驻留门禁：** The production database adapter reports a blocked `production-database-residency-transfer-plan` by default, keeps `canTransferAcrossRegions=false`, requires data residency, cross-border transfer review, regional backup evidence, subprocessor review, and residency approval, and renders those blockers in Settings without copying data or changing regional replication.
- **TC-410 AI Model Evaluation Gate / AI 模型评测门禁：** The AI provider adapter reports a blocked `ai-model-evaluation-policy` by default, requires offline evaluation suites, red-team cases, regression thresholds, hallucination monitoring, and post-launch sampling, and renders those blockers in Settings without calling a live model.
- **TC-411 AI Human Review Gate / AI 人工复核门禁：** The AI provider adapter reports a blocked `ai-human-review-policy` by default, keeps `canEscalateToHumanReview=false`, requires a review queue, escalation runbook, low-confidence policy, user-visible fallback, and audit trail, and renders those blockers in Settings without auto-releasing low-confidence advice.
- **TC-412 AI Model Release Gate / AI 模型发布门禁：** The AI provider adapter reports a blocked `ai-model-release-policy` by default, keeps `canPromoteModelVersion=false`, requires canary rollout metrics, model/prompt/factor/schema version locks, rollback switch readiness, and release approval, and renders those blockers in Settings without silently promoting model or prompt versions.
- **TC-413 AI Runtime Monitoring Gate / AI 上线运行监控门禁：** The AI provider adapter reports a blocked `ai-runtime-monitoring-policy` by default, keeps `canOperateLiveMonitoring=false`, requires hallucination/schema/latency/source/user-feedback/drift signals, alert thresholds, on-call runbook, and user feedback loop, and renders those blockers in Settings without operating live monitoring or silently continuing bad model output.
- **TC-414 AI Response Validation Gate / AI 输出校验门禁：** The AI provider adapter reports a blocked `ai-response-validation-policy` by default, keeps `canPublishValidatedOutput=false`, requires JSON schema, probability bounds, scenario probability sum, forbidden-claim filtering, disclaimer presence, source reference presence, validation audit, and user-visible fallback before any model output can be shown as analysis.
- **TC-415 Suitability Enforcement Gate / 适当性执行门禁：** The compliance service reports a blocked `suitability-enforcement-policy` by default, keeps `canRestrictAnalysisByProfile=false`, requires profile-based restriction rules, user-visible reason, audit trail, and education-only fallback before personalized analysis can be restricted or released by risk profile.
- **TC-416 Jurisdiction Enforcement Gate / 地区执行门禁：** The compliance service reports a blocked `jurisdiction-enforcement-policy` by default, keeps `canRestrictByJurisdiction=false`, requires user jurisdiction detection, restricted-region fallback, localized disclosures, per-jurisdiction legal review, and education-only fallback for unknown or unreviewed regions.
- **TC-417 Disclosure Versioning Gate / 披露版本门禁：** The compliance service reports a blocked `disclosure-versioning-policy` by default, keeps `canReleaseDisclosureVersion=false`, requires immutable disclosure versions, changelog, user re-acknowledgement on material changes, legal approval, rollback version, and audit trail before disclosure text can be publicly released.
- **TC-418 Licensed Adviser Review Gate / 持牌复核门禁：** The compliance service reports a blocked `licensed-adviser-review-policy` by default, keeps `canApprovePersonalizedAdvice=false`, requires strong-signal/high-risk triggers, licensed adviser and compliance-officer review roles, 24-hour SLA, conflict disclosure, audit trail, and user-visible pending state before advice-like or high-risk output can be released.
- **TC-419 Auth Session Security Gate / 认证会话安全门禁：** The auth provider adapter reports a blocked `auth-session-security-policy` by default, keeps `canIssueProductionSessions=false`, requires refresh-token rotation, reuse detection, device binding, revocation, idle timeout, audit trail, device list, expiry notice, and redacted session audit fields before production sessions can be issued.
- **TC-420 Auth Credential Storage Gate / 认证凭证存储门禁：** The auth provider adapter reports a blocked `auth-credential-storage-policy` by default, keeps `canStoreProductionCredentials=false`, requires memory-hard hashing, pepper secret management, breached-password screening, password history, reset-token hashing, audit redaction, forbidden stored-field boundaries, credential rotation triggers, and migration planning before production credentials can be stored.
- **TC-421 Auth MFA Gate / 认证 MFA 门禁：** The auth provider adapter reports a blocked `auth-mfa-policy` by default, keeps `canChallengeProductionUsers=false`, requires MFA enrollment, step-up challenge, backup-code hashing, MFA recovery review, trusted-device expiry, audit trail, supported factors, forbidden stored-field boundaries, and user-visible fallback before production MFA challenges can run.
- **TC-422 Auth Email Verification Gate / 认证邮箱验证门禁：** The auth provider adapter reports a blocked `auth-email-verification-policy` by default, keeps `canVerifyProductionEmail=false`, requires one-time hashed verification tokens, 30-minute token TTL, resend cooldown, hourly resend limits, email-change reverification, audit trail, bounce-aware suppression, forbidden audit fields, visible expiry, and email-change review before production email verification can run.
- **TC-423 AI Citation Evidence Gate / AI 引用证据门禁：** The AI provider adapter reports a blocked `ai-citation-evidence-policy` by default, keeps `canPublishUncitedAnalysis=false`, requires at least one citation per key claim, claim-citation mapping, source freshness warnings, required citation metadata, allowed source types, and forbidden evidence-field boundaries before live model output can be published.
- **TC-424 AI Prompt Injection Defense Gate / AI 提示词注入防护门禁：** The AI provider adapter reports a blocked `ai-prompt-injection-defense-policy` by default, keeps `canUseUnsanitizedSourceText=false`, requires source-text sanitization, instruction stripping, source-role isolation, quoted-evidence-only handling, unsafe-source quarantine, audit flagging, forbidden prompt-field boundaries, and user-visible source-risk warnings before source text can feed live model analysis.
- **TC-425 AI Personal Data Minimization Gate / AI 个人数据最小化门禁：** The AI provider adapter reports a blocked `ai-data-minimization-policy` by default, keeps `canSendPersonalDataToModel=false`, requires field allowlists, pseudonymous user ids, portfolio bucketing, credential exclusion, pre-request redaction, privacy audit flags, consent context, and redacted portfolio context before personalized inputs can feed live model analysis.
- **TC-426 AI Secret Management Gate / AI 密钥管理门禁：** The AI provider adapter reports a blocked `ai-provider-secret-management-policy` by default, keeps `canUseProductionSecrets=false`, requires managed secret storage, server-side-only access, key rotation, least-privilege runtime roles, secret-access audit, frontend secret exclusion, break-glass procedure, and revocation drill before real model credentials can be used.
- **TC-427 Auth Role Authorization Gate / 认证角色授权门禁：** The auth provider adapter reports a blocked `auth-role-authorization-policy` by default, keeps `canUseProductionAdminRoles=false`, requires verified IdP role claims, server-side role mapping, admin approval workflow, role expiry, least-privilege roles, role-change audit trail, dual admin approval, and role-review runbook before production privileged roles can be used.
- **TC-428 Auth OIDC Callback Gate / 认证 OIDC 回调门禁：** The auth provider adapter reports a blocked `auth-oidc-callback-policy` by default, keeps `canHandleProductionCallback=false`, requires redirect URI allowlist, state/nonce validation, PKCE verification, callback domain allowlist, SameSite cookie, callback audit trail, replay protection, and provider issuer validation before production OIDC/OAuth callbacks can be handled.
- **TC-429 Auth CSRF Protection Gate / 认证 CSRF 防护门禁：** The auth provider adapter reports a blocked `auth-csrf-protection-policy` by default, keeps `canAcceptCrossSiteMutations=false`, requires SameSite cookie controls, CSRF token binding, Origin/Referer validation, state-changing method guards, synchronizer or double-submit token strategy, CORS allowlist, replay protection, and audit trail before production mutating auth requests can be accepted.
- **TC-430 Auth Audit Logging Gate / 认证审计日志门禁：** The auth provider adapter reports a blocked `auth-audit-logging-policy` by default, keeps `canReleaseProductionAuthEvents=false`, requires redacted auth metadata, tamper-evident hash chain, retention classes, request correlation ids, privileged-action review, audit export handoff, and forbidden audit-field boundaries before production auth events can be released.
- **TC-431 Auth Audit Event Standardization / 认证审计事件标准化：** Mock auth register, login, logout, self role update, admin role assignment, and admin role revocation now write standardized audit events (`auth.signUp`, `auth.signIn`, `auth.signOut`, `auth.roleChange`) with `auth-audit-v1`, request correlation ids, `auth-security-365d`, hash-chain markers, privileged-review markers, and legacy event names retained only inside redacted metadata for compatibility.
- **TC-432 Mock Session Refresh Rotation / Mock 会话刷新轮换：** `POST /api/auth/session/refresh` rotates mock email auth sessions by revoking the old session, issuing a new token, invalidating the old token, preserving access through the new token, and writing a standardized `auth.sessionRefresh` audit event with `rotated=true`, `auth-audit-v1`, request correlation id, and `auth-security-365d`.
- **TC-433 Redacted Auth Session List / 脱敏认证会话列表：** `GET /api/auth/sessions` returns active mock email sessions as redacted summaries with `id`, `createdAt`, `expiresAt`, `current`, and `sessionMode`, never returns `tokenHash` or raw tokens, and writes a standardized `auth.sessionList` audit event with `sensitiveSessionFieldsReturned=false`.
- **TC-434 Auth Session Revoke / 认证会话撤销：** `DELETE /api/auth/sessions/:id` revokes another active mock email session owned by the current user, makes the revoked token fail `/api/me`, keeps the current token valid, blocks current-session revocation with `CURRENT_SESSION_REVOKE_UNSUPPORTED`, and writes a standardized `auth.sessionRevoke` audit event with redacted session metadata.
- **TC-435 Account Session Panel / 账户会话面板：** The authenticated account card fetches `GET /api/auth/sessions`, renders redacted current/other session summaries, states that token/tokenHash are not displayed, and can revoke another session through `DELETE /api/auth/sessions/:id` without removing the current session row.
- **TC-436 Account Session Rotation / 账户会话轮换：** The authenticated account card can call `POST /api/auth/session/refresh` from the session panel, update `state.authToken` and `localStorage.apiAuthToken` with the rotated token, reload `GET /api/auth/sessions` with the new token, and show the user that the old token has expired.
- **TC-437 API Health Alias / API 健康检查别名：** The mock backend accepts both `GET /health` and `GET /api/health` with the same backend status payload, so deployment monitors and API-prefixed smoke checks do not receive a false 404.
- **TC-438 Project Progress Panel / 项目进度面板：** The Settings page renders a project progress card with honest local-demo and public-launch percentages, the update date, completed capability groups, and the next real blockers so progress is visible in the webpage, not only in chat.
- **TC-439 Project Progress API / 项目进度接口：** `GET /api/project/progress` returns the backend progress snapshot for the Settings panel, including local-demo percentage, public-launch readiness percentage, completed groups, real blockers, source label, and a project-management disclaimer.

## Results / 结果
- Syntax checks passed:
  - `node --check app.js`
  - `node --check tests/frontend-regression.test.mjs`
  - `node --check backend/server.mjs`
  - `node --check backend/tests/backend-api.test.mjs`
- Frontend regression passed: 137/137 tests.
- Backend regression passed: 122/122 tests.
- Full automated regression passed: 265/265 tests.
- Local webpage stack smoke passed:
  - Webpage `GET /`: 200, `text/html`
  - Webpage shell contains `项目进度`, `真实完成度与上线差距`, and `projectProgressState`
  - Web script `GET /app.js`: 200, `text/javascript`
  - Served `app.js` includes `projectProgress`, `86.4`, `38.4`, `真实进度更新时间`, and `下一批真实阻断`
  - Backend `GET /api/project/progress` returns `mock-backend-progress-snapshot`, `86.6`, `38.6`, completed groups, blockers, and disclaimer
  - Served `app.js` includes `loadProjectProgressStatus`, `/api/project/progress`, and `apiProjectProgress`
  - Served `styles.css` includes `project-progress-panel`, `progress-heading`, and `progress-track`
  - Served `app.js` includes `轮换当前会话`, `data-auth-session-rotate`, `rotateCurrentAuthSession`, and `POST /api/auth/session/refresh`
  - Actual auth API smoke confirmed session rotation returns a new token, the old token is rejected by `GET /api/auth/sessions`, and the new token can read the redacted session list
  - Service worker `GET /service-worker.js`: 200 and serves `finance-ai-assistant-v3`
  - Backend `GET /health`: `{"status":"ok","service":"finance-ai-assistant-backend","version":"0.1.0"}`
  - Backend `GET /api/health`: `{"status":"ok","service":"finance-ai-assistant-backend","version":"0.1.0"}`
- LAN webpage smoke passed:
  - `GET http://127.0.0.1:4174/`: 200
  - `GET http://172.16.113.15:4174/`: 200
  - `GET http://172.16.113.15:4180/health`: `{"status":"ok","service":"finance-ai-assistant-backend","version":"0.1.0"}`
- In-app browser render check passed: the right-side live preview stayed open at `http://127.0.0.1:4173/`, with title, `自选股`, `提醒`, risk-profile/analysis-style text, `设置`, and `风险提示` rendered.
- Right-side render check confirmed AI settings display `成本限流 blocked`, `来源覆盖 blocked`, `costControls:blocked`, and `sourceGrounding:blocked`.
- Right-side render check confirmed AI settings display `审计策略 blocked`, `调用预检 blocked`, and `dry-run-no-model-call`.
- Right-side render check confirmed AI settings display `因子输入 blocked`, `因子权重 blocked`, `factorInputs:blocked`, and `factorWeights:blocked`.
- Right-side render check confirmed audit settings display `自动保留清理 blocked`, `dry-run-no-scheduler-start`, and `wormArchive:blocked`.
- Right-side render check confirmed the account card displays `登录风控 blocked`, `账号恢复 blocked`, `认证预检 blocked`, and `dry-run-no-provider-call`.
- Right-side render check confirmed the notification delivery card displays `回执策略 blocked`, `退订抑制 blocked`, `反弹处理 blocked`, `投递预检 blocked`, and `dry-run-no-external-send`.
- Right-side render check confirmed the notification delivery card displays `Webhook 回执预检 blocked`, `dry-run-no-webhook-accept`, `providerEventIdempotency:blocked`, `时间窗 300s`, `重放窗口 24h`, and `event id 幂等`.
- Right-side render check confirmed the database card displays `加密门禁 blocked`, `dry-run-no-key-usage`, `fieldLevelEncryption:blocked`, protected data classes, and key-safety labels.
- Right-side render check confirmed the database card displays `权限门禁 blocked`, `dry-run-no-permission-change`, `rowLevelSecurity:blocked`, least-privilege role labels, protected scopes, and permission-safety labels.
- Right-side render check confirmed the database card displays `隐私保留 blocked`, `dry-run-no-data-erasure`, `erasureWorkflow:blocked`, governed data scopes, retention classes, and privacy-safety labels.
- Right-side render check confirmed the database card displays `数据驻留 blocked`, `dry-run-no-cross-border-transfer`, `crossBorderTransfer:blocked`, governed regions, regulated data scopes, and residency-safety labels.
- Right-side render check confirmed the AI service card displays `模型评测 blocked`, `通过率 95%`, `幻觉率 ≤1%`, and `modelEvaluation:blocked`.
- Right-side render check confirmed the AI service card displays `人工复核 blocked`, `dry-run-no-user-visible-escalation`, `阈值 55%`, and `humanReview:blocked`.
- Right-side render check confirmed the AI service card displays `模型发布 blocked`, `dry-run-no-model-release`, `modelRelease:blocked`, and version locks including `promptVersion`.
- Right-side render check confirmed the AI service card displays `运行监控 blocked`, `dry-run-no-live-monitoring`, `来源覆盖 ≥95%`, and `runtimeMonitoring:blocked`.
- Right-side render check confirmed the AI service card displays `输出校验 blocked`, `dry-run-no-user-visible-invalid-output`, `概率 0-100%`, and `responseValidation:blocked`.
- Right-side render check confirmed the compliance service card displays `适当性执行 blocked`, `dry-run-no-personalized-restriction`, `lowRiskBlocksAggressiveSignals`, and `suitabilityEnforcement:blocked`.
- Right-side render check confirmed the compliance service card displays `地区执行 blocked`, `dry-run-no-region-restriction`, `默认 AU-QLD`, and `jurisdictionEnforcement:blocked`.
- Right-side render check confirmed the compliance service card displays `披露版本 blocked`, `dry-run-no-disclosure-version-release`, `disclaimer:disclaimer-v0`, and `disclosureVersioning:blocked`.
- Right-side render check confirmed the compliance service card displays `持牌复核 blocked`, `dry-run-no-adviser-approval`, `strongBuySellLanguage`, and `licensedAdviserReview:blocked`.
- Right-side render check confirmed the account card displays `会话安全 blocked`, `dry-run-no-session-hardening`, `refreshTokenRotation`, and `sessionSecurity:blocked`.
- Right-side render check confirmed the account card displays `凭证存储 blocked`, `dry-run-no-production-credential-storage`, `breachedPasswordScreening`, and `credentialStorage:blocked`.
- Right-side render check confirmed the account card displays `MFA 门禁 blocked`, `dry-run-no-production-mfa`, `backupCodeHashing`, and `mfaPolicy:blocked`.
- Right-side render check confirmed the account card displays `邮箱验证 blocked`, `dry-run-no-production-email-verification`, `oneTimeVerificationToken`, and `emailVerificationPolicy:blocked`.
- Local service smoke confirmed the AI provider API and served `app.js` expose `引用证据 blocked`, `dry-run-no-uncited-model-output`, `linkedFactor`, and `citationEvidence:blocked`; the page shell at `http://127.0.0.1:4173/?refresh=20260605-citation-evidence#settings` returns the Settings anchor.
- Local service smoke confirmed the AI provider API and served `app.js` expose `注入防护 blocked`, `dry-run-no-unsanitized-source-text`, `sourceRoleIsolation`, and `promptInjectionDefense:blocked`; the page shell at `http://127.0.0.1:4173/?refresh=20260605-prompt-injection#settings` returns the Settings anchor.
- Local service smoke confirmed the AI provider API and served `app.js` expose `数据最小化 blocked`, `dry-run-no-personal-data-to-model`, `brokerCredentials`, and `dataMinimization:blocked`; the page shell at `http://127.0.0.1:4173/?refresh=20260605-data-minimization#settings` returns the Settings anchor.
- Local service smoke confirmed the AI provider API and served `app.js` expose `密钥管理 blocked`, `dry-run-no-model-secret-use`, `managedSecretStore`, and `secretManagement:blocked`; the page shell at `http://127.0.0.1:4173/?refresh=20260605-secret-management#settings` returns the Settings anchor.
- Local service smoke confirmed the auth provider API and served `app.js` expose `角色授权 blocked`, `dry-run-no-production-role-escalation`, `verifiedIdpRoleClaims`, and `roleAuthorization:blocked`; the page shell at `http://127.0.0.1:4173/?refresh=20260605-role-authorization#settings` returns the Settings anchor.
- Local service smoke confirmed the auth provider API and served `app.js` expose `OIDC 回调 blocked`, `dry-run-no-oidc-callback`, `redirectUriAllowlist`, and `oidcCallback:blocked`; the page shell at `http://127.0.0.1:4173/?refresh=20260605-oidc-callback#settings` returns the Settings anchor.
- Local service smoke confirmed the auth provider API and served `app.js` expose `CSRF 防护 blocked`, `dry-run-no-cross-site-mutation`, `csrfTokenBinding`, and `csrfProtection:blocked`; the page shell at `http://127.0.0.1:4173/?refresh=20260605-csrf-protection#settings` returns the Settings anchor.
- Local service smoke confirmed the auth provider API and served `app.js` expose `认证审计 blocked`, `dry-run-no-auth-audit-release`, `tamperEvidentHashChain`, and `auditLogging:blocked`; the page shell at `http://127.0.0.1:4173/?refresh=20260605-auth-audit-logging#settings` returns the Settings anchor.
- Local service smoke confirmed actual auth API writes standardized audit events: `POST /api/auth/register`, `POST /api/auth/login`, and `GET /api/audit-log` returned `auth.signUp`, `auth.signIn`, `auth-audit-v1`, `requestCorrelationId`, and `auth-security-365d`; webpage shell at `http://127.0.0.1:4173/?refresh=20260605-auth-audit-events#settings` returns 200.
- Local service smoke confirmed actual session refresh rotation: `POST /api/auth/session/refresh` returned a new token, the old token was rejected by `/api/me`, the new token was accepted, and `GET /api/audit-log` returned `auth.sessionRefresh`, `rotated=true`, and `auth-security-365d`; webpage shell at `http://127.0.0.1:4173/?refresh=20260605-session-refresh#settings` returns 200.
- Local service smoke confirmed actual redacted session listing: after register/login, `GET /api/auth/sessions` returned 2 redacted items, exactly 1 `current` session, no token hash fields on session items, and `sessionPolicy.tokenHashReturned=false`; `GET /api/audit-log` returned `auth.sessionList`, `auth.sessions.list`, and `sensitiveSessionFieldsReturned=false`; webpage shell at `http://127.0.0.1:4173/?refresh=20260605-session-list#settings` returns 200.
- Local service smoke confirmed actual session revocation: after register/login, `DELETE /api/auth/sessions/:id` revoked the old session, old token `/api/me` returned 401, current token `/api/me` returned 200, current-session deletion returned `CURRENT_SESSION_REVOKE_UNSUPPORTED`, and `GET /api/audit-log` returned `auth.sessionRevoke`, `auth.sessions.revoke`, and `sensitiveSessionFieldsReturned=false`; webpage shell at `http://127.0.0.1:4173/?refresh=20260605-session-revoke#settings` returns 200.
- Local service smoke confirmed the served frontend includes the account session panel: `GET /` returns 200, served `app.js` contains `登录会话`, `data-auth-session-revoke`, `loadAuthSessions`, and `revokeAuthSession`, served `styles.css` contains `auth-session-panel` and `session-list`, and live backend register/login/session-list/revoke calls succeed for the same panel flow; webpage shell at `http://127.0.0.1:4173/?refresh=20260605-session-ui#settings` returns 200.
- Local service smoke confirmed the served frontend includes account session rotation: `GET /` returns 200, served `app.js` contains `轮换当前会话`, `data-auth-session-rotate`, `rotateCurrentAuthSession`, and `POST /api/auth/session/refresh`; live backend refresh returns a new token, the old token is rejected by `GET /api/auth/sessions`, and the new token reads the redacted session list; refresh the webpage shell at `http://127.0.0.1:4173/?refresh=20260605-session-rotate#settings`.
- Right-side render check confirmed the scheduler card displays `队列背压 blocked`, `Worker 回调认证 blocked`, `Runbook blocked`, `Worker 预检 blocked`, and `dry-run-no-worker-start`.
- Right-side render check confirmed the data-source card displays `授权分层 blocked`, `延迟标签 blocked`, `Provider 预检 blocked`, and `dry-run-no-provider-request`.
- Right-side render check confirmed the data-source card displays `来源验证 blocked`, `再分发 blocked`, `新闻预检 blocked`, and `dry-run-no-provider-fetch`.
- Right-side render check confirmed the data-source card displays `新鲜度 blocked`, `政策日历 blocked`, `宏观预检 blocked`, and `dry-run-no-provider-fetch`.
- Right-side preview recovery passed: after `4173` was found stopped, `node scripts/full-dev-server.mjs` was restarted and the preview rendered the current web app again.

## Notes / 备注
- The same-Mac URL is `http://127.0.0.1:4173/` while `node scripts/full-dev-server.mjs` is running.
- For phone/iPad testing, use `node scripts/full-dev-server.mjs --lan` and open the printed `LAN:` URL.
- Current temporary LAN preview URL is `http://172.16.113.15:4174/` while the LAN preview process remains running.
