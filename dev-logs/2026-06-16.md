# Development Log / 开发日志 - 2026-06-16

## 今日完成 / Completed Today

- v152 免费 AI 限流优化：默认输出 token 上限降到 `800`，compact/ultra-compact 降到 `560/320`，提示词和输入证据进一步缩短，只要求 JSON。
- 后端新增同股票 AI 本地缓存：完整 AI 成功结果 10 分钟复用，失败原因 60 秒复用，避免短时间重复消耗免费 provider 额度。
- 前端“重新检测 AI”新增防重复点击：同一股票有最近失败缓存时直接展示缓存失败原因，不再立刻重复请求；切换股票后可重新检测。
- 首页 AI 失败文案收敛为“完整 AI 暂不可用，当前显示规则参考。”；provider 原始错误、冷却、模型、可重试状态和建议等待时间进入折叠技术诊断。
- 自选股卡片改为每只股票单独保存分析状态，不再复用当前主卡片可见概率；卡片显示自己的规则/AI 参考概率、更新时间和当前是否正在查看。
- `?resetLocalState=true` 扩展清理 AI 冷却锁、上次 provider relay、失败缓存、自选股分析展示缓存和 prototype 临时状态；保留真实自选列表、持仓、提醒和风险偏好。
- 情景目标价和后端规则情景继续保证无真实价格时显示 `目标价暂无`，不渲染 `0.00`。
- 预测型新闻标题新增标签：`媒体观点`、`非公司公告`、`不代表模型预测`、`仅作新闻参考`。
- 前端版本升级到 `finance-ai-assistant-v152`，`index.html` 加载 `app.js?v=152`，项目进度显示 `494 条自动化回归目标`。

## Verification / 验证

- Syntax checks passed for `app.js`, `backend/services/ai-provider-adapter.mjs`, and `backend/services/mock-ai-service.mjs`.
- `tests/ai-provider-relay.test.mjs`: 10/10 passed.
- Focused frontend regression for AI quota, reset, watchlist, news, scenario, and predictive labels: 40/40 passed.

## 问题风险 / Risks

- 免费 AI provider 仍可能因为真实额度、速率或冷却窗口降级到规则参考；v152 降低触发概率，但不能保证每次完整 AI 都成功。
- 公开 Render 还需要推送后重新验证，确认 `liveAppVersion=152` 和固定网址可访问。

## 明日待办 / Next Todo

- 跑完整 frontend/PWA/Render live-status 回归。
- 推送到 GitHub 后等待 Render 自动部署，再复测固定公开网址。
- 若完整 AI 仍频繁限流，继续评估更稳定的免费或低成本 JSON-capable fallback model。

## 决策记录 / Decisions

- 保留真实自选列表作为长期用户数据；`resetLocalState=true` 只清测试污染和临时分析状态。
- 完整 AI 不可用时，页面优先显示规则参考，不展示样例建议，不把 provider 原始错误放在普通用户视图。
