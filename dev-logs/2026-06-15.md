# 2026-06-15 开发日志

## 今日完成
- 成功将本地 v109 提交 `fa557cf Add fixed Render live status check` 推送到 GitHub `main`。
- 使用固定 Render 网址运行线上状态检查：`https://finance-ai-assistant-web.onrender.com`。
- 线上检查结果显示：固定网址可访问，首页、`/health`、`/api/health`、`/api/project/progress`、`/api/ai-services/provider-adapter` 和 MSFT 分析接口均返回 `200`。
- 线上仍加载 `app.js?v=108`，而本地最新已推进到 v109/v110；说明 Render 还没有部署最新 GitHub 提交。
- 线上 AI provider 状态仍为 `runtimeMode=inactive`，主模型缺少 `FINANCE_AI_MODEL_API_KEY`，三个备用模型也未配置 key。
- 将本地版本推进到 `finance-ai-assistant-v110` / `app.js?v=110`，项目进度显示为 `451 条自动化回归目标`。
- 后端 AI provider adapter、设置页摘要和固定网址检查脚本新增备用模型缺口诊断：每个 fallback 槽位会显示具体缺失的 Render Dashboard 变量和 `setupStatus`。
- 将 v110 提交 `fae70a1 Expose AI fallback key gaps` 推送到 GitHub，Render 已自动部署到 `app.js?v=110`。
- 固定 Render 网址 180 秒连续健康门禁通过：`externalUseReady=true`、`continuousHealthPassed=true`、`iterationCount=11`、`lastFailure=null`。
- 连续门禁覆盖 `/`、`/health`、`/api/health`、`/api/analysis?symbol=MSFT&riskProfile=balanced`、`/api/stocks/search?q=Microsoft`，均保持 `200`。

## 问题风险
- 固定网址稳定性已通过当前 180 秒门禁，但正式上线仍需更长时间监控和告警。
- 完整真实 AI 仍未在固定 Render 环境跑通；线上需要填写 OpenAI/Gemini/OpenRouter/Groq 对应模型密钥。
- 当前固定网址稳定性已明显优于 `lhr.life` 临时隧道，但正式上线仍需要继续验证自动部署、监控、密钥和完整 AI 输出。

## 明日/下一步待办
- 在 Render Dashboard 添加 `FINANCE_AI_MODEL_API_KEY`、`FINANCE_AI_MODEL_FALLBACK_API_KEY`、`FINANCE_AI_MODEL_FALLBACK2_API_KEY`、`FINANCE_AI_MODEL_FALLBACK3_API_KEY` 后，复测 `providerAdapter.canCallLiveModel` 和完整 AI 输出。
- 继续收敛首页普通用户视图，把详细技术诊断放入折叠区或管理员模式。

## English Summary
- Successfully pushed local v109 commit `fa557cf Add fixed Render live status check` to GitHub `main`.
- Ran the fixed Render live-status check against `https://finance-ai-assistant-web.onrender.com`.
- Live check showed the fixed URL is reachable and homepage, `/health`, `/api/health`, `/api/project/progress`, `/api/ai-services/provider-adapter`, and MSFT analysis all return `200`.
- Live Render still loads `app.js?v=108`, while local is now v109/v110; Render has not deployed the latest GitHub commit yet.
- Live AI provider status remains `runtimeMode=inactive`; the primary model is missing `FINANCE_AI_MODEL_API_KEY`, and the three fallback model keys are also not configured.
- Bumped local version to `finance-ai-assistant-v110` / `app.js?v=110`, with progress at `451 automated regression targets`.
- Backend AI provider adapter, settings summaries, and the fixed-URL checker now expose per-fallback missing Render Dashboard variables and `setupStatus`.
- Pushed v110 commit `fae70a1 Expose AI fallback key gaps` to GitHub, and Render auto-deployed `app.js?v=110`.
- The fixed Render URL passed the 180-second continuous health gate: `externalUseReady=true`, `continuousHealthPassed=true`, `iterationCount=11`, and `lastFailure=null`.
- The continuous gate covered `/`, `/health`, `/api/health`, `/api/analysis?symbol=MSFT&riskProfile=balanced`, and `/api/stocks/search?q=Microsoft`, all staying at `200`.

## v111 新闻第一屏相关性收紧

## 今日完成
- 将 PWA 缓存和脚本版本更新为 `finance-ai-assistant-v111` / `app.js?v=111`，项目进度显示为 `452 条自动化回归目标`。
- 新闻第一屏现在只展示公司直接新闻、公司公告、管理层/公开言论。
- 行业新闻、供应链/监管新闻和市场泛新闻默认折叠；如果没有公司直接新闻，第一屏显示明确摘要，而不是把泛市场新闻当作个股重点新闻展示。
- 针对 MSFT 场景，Ryanair、Cadence、Meta、Nvidia 这类非 Microsoft 直接标题不会出现在第一屏。

## 问题风险
- 新闻链路仍依赖公开 RSS/API 标题质量；完整新闻授权和更高精度实体识别仍是后续生产任务。
- 如果真实 provider 只返回市场泛新闻，页面会显示摘要加折叠详情，首屏新闻数量可能少于 3 条，这是为了避免误导。

## 下一步
- 继续优化新闻实体识别：产品、供应链、监管、管理层言论要有更细的权重。
- 继续收敛首页普通用户视图，把开发诊断留在折叠区或设置页。

## English Summary
- v111 updates the cache/script version to `finance-ai-assistant-v111` / `app.js?v=111`, with progress at `452 automated regression targets`.
- The news first screen now only shows company-direct news, filings, and management/public statements.
- Industry, supply-chain/regulatory, and broad market news are folded by default. If no company-direct news exists, the first screen shows a clear summary instead of presenting background market news as stock-specific key news.
- In the MSFT case, non-Microsoft direct headlines such as Ryanair, Cadence, Meta, and Nvidia stay out of the first screen.

## 固定 Render 网址复核

## 今日完成
- 用户确认固定公开网址：`https://finance-ai-assistant-web.onrender.com`。
- 使用固定网址线上状态检查脚本复核，线上版本为 `app.js?v=111`，与当前项目版本一致。
- 首页、`/health`、`/api/health`、`/api/project/progress`、`/api/ai-services/provider-adapter`、`/api/analysis?symbol=MSFT&riskProfile=balanced` 均返回 `200`。
- MSFT 分析接口可返回真实数据规则参考，当前 `analysisMode=real-data-rule-reference`。

## 问题风险
- 完整真实 AI 仍未启用，线上 `runtimeMode=inactive`，`canCallLiveModel=false`。
- Render Dashboard 仍缺少 `FINANCE_AI_MODEL_API_KEY`、`FINANCE_AI_MODEL_FALLBACK_API_KEY`、`FINANCE_AI_MODEL_FALLBACK2_API_KEY`、`FINANCE_AI_MODEL_FALLBACK3_API_KEY`。
- 当前分析仍是“真实数据规则参考”，不是完整 AI 深度分析。

## 下一步
- 在 Render Dashboard 手动配置模型密钥后重新部署，再复测完整 AI 输出。
- 继续把普通用户首页的内部诊断信息收敛到设置页或折叠诊断区。

## English Summary
- The user confirmed the fixed public URL: `https://finance-ai-assistant-web.onrender.com`.
- Ran the fixed Render live-status checker again. The live app is now `app.js?v=111`, matching the current project version.
- Homepage, `/health`, `/api/health`, `/api/project/progress`, `/api/ai-services/provider-adapter`, and `/api/analysis?symbol=MSFT&riskProfile=balanced` all return `200`.
- The MSFT analysis endpoint can return real-data rule-reference output, with `analysisMode=real-data-rule-reference`.
- Full real AI is still not enabled: live `runtimeMode=inactive` and `canCallLiveModel=false`.
- Render Dashboard is still missing `FINANCE_AI_MODEL_API_KEY`, `FINANCE_AI_MODEL_FALLBACK_API_KEY`, `FINANCE_AI_MODEL_FALLBACK2_API_KEY`, and `FINANCE_AI_MODEL_FALLBACK3_API_KEY`.
- Next step: manually configure the model keys in Render Dashboard, redeploy, and retest full AI output.

## v112 设置页开发诊断收敛

## 今日完成
- 将 PWA 缓存和脚本版本更新为 `finance-ai-assistant-v112` / `app.js?v=112`，项目进度显示为 `453 条自动化回归目标`。
- 设置页新增默认折叠的 `展开开发者诊断详情` 区块，把数据存储、生产数据库、审计、任务运行、调度服务收纳进去。
- 普通用户打开设置页时先看到项目摘要、数据源、AI、合规、账户、提醒和持仓，不再先被数据库、审计和任务队列细节打断。
- 复核自选股卡片回归：有规则参考分析时显示 `规则参考 xx%` 和 `完整 AI 待模型`，不会只显示“待 AI 模型”。
- 本地浏览器验证 `http://127.0.0.1:4174/?refresh=public-v112-settings-collapse#settings`：页面加载 `app.js?v=112`，设置页可见，开发者诊断默认未展开，没有横向溢出。

## 问题风险
- 完整真实 AI 仍依赖 Render Dashboard 模型密钥和 provider 额度，当前线上仍只能确认规则参考输出。
- 设置页的数据源和 AI 服务状态仍有较多诊断细节，后续可继续做普通用户/开发者模式切换。

## 下一步
- 配置 Render Dashboard 模型密钥后，继续复测完整 AI 输出。
- 继续把数据源和 AI 服务状态里的详细技术码收敛到开发者诊断区。

## English Summary
- v112 updates the cache/script version to `finance-ai-assistant-v112` / `app.js?v=112`, with progress at `453 automated regression targets`.
- Settings now has a collapsed `展开开发者诊断详情` section for repository, production database, audit, job runner, and scheduler details.
- Normal users now see project summary, data source, AI, compliance, account, reminders, and portfolio first, instead of being interrupted by database/audit/job internals.
- Watchlist card regression was rechecked: when rule-reference analysis exists, cards show `规则参考 xx%` plus `完整 AI 待模型`.
- Local browser verification against `http://127.0.0.1:4174/?refresh=public-v112-settings-collapse#settings` confirmed `app.js?v=112`, Settings visible, developer diagnostics collapsed, and no horizontal overflow.
- Render auto-deployed v112 to `https://finance-ai-assistant-web.onrender.com`. Live status confirmed homepage, `/health`, `/api/health`, `/api/project/progress`, `/api/ai-services/provider-adapter`, and MSFT analysis all return `200`, with live `app.js?v=112`.
- Live browser verification confirmed the fixed URL's Settings page keeps developer diagnostics collapsed by default and has no horizontal overflow.
- Full real AI still depends on Render Dashboard model keys and provider quota; the live site can currently only be confirmed for rule-reference output.

## v113 项目进度普通用户摘要

## 今日完成
- 将 PWA 缓存和脚本版本更新为 `finance-ai-assistant-v113` / `app.js?v=113`，项目进度显示为 `454 条自动化回归目标`。
- 设置页 `项目进度` 面板现在先显示普通用户能理解的测试版状态摘要：本地 Demo 可用、固定公开网址已建立、真实数据规则参考可用、完整 AI 待模型密钥。
- 上线差距、生产门禁、来源端点、自动化回归证据和分项准备度被收纳到默认折叠的 `展开开发者进度详情` 与 `展开开发诊断`。
- 本地浏览器验证 `http://127.0.0.1:4174/?refresh=public-v113-progress-summary#settings`：页面加载 `app.js?v=113`，项目进度详情、开发诊断和开发者设置均默认折叠，没有横向溢出。

## 问题风险
- 完整真实 AI 仍依赖 Render Dashboard 模型密钥和 provider 额度。
- 数据源状态和 AI 服务状态仍有可继续收敛的技术细节。

## 下一步
- 继续把数据源和 AI 服务状态里的技术码收敛到开发者诊断区。
- 配置 Render Dashboard 模型密钥后，复测完整 AI 输出。

## English Summary
- v113 updates the cache/script version to `finance-ai-assistant-v113` / `app.js?v=113`, with progress at `454 automated regression targets`.
- The Settings `项目进度` panel now shows a user-readable test-version summary first: local demo available, fixed public URL established, real-data rule reference available, and full AI awaiting model keys.
- Launch-gap percentages, production gates, endpoint evidence, regression evidence, and readiness details are now inside collapsed `展开开发者进度详情` and `展开开发诊断` sections.
- Local browser verification against `http://127.0.0.1:4174/?refresh=public-v113-progress-summary#settings` confirmed `app.js?v=113`, collapsed project progress details, collapsed developer diagnostics, collapsed developer settings, and no horizontal overflow.
- Render auto-deployed v113 to `https://finance-ai-assistant-web.onrender.com`. Live status confirmed homepage, `/health`, `/api/health`, `/api/project/progress`, `/api/ai-services/provider-adapter`, and MSFT analysis all return `200`, with live `app.js?v=113`.
- Live browser verification confirmed the fixed URL's Settings page has the user project summary, keeps project progress details / developer diagnostics / developer settings collapsed by default, and has no horizontal overflow.
- Full real AI still depends on Render Dashboard model keys and provider quota.

## v114 AI 服务普通用户摘要

## 今日完成
- 将 PWA 缓存和脚本版本更新为 `finance-ai-assistant-v114` / `app.js?v=114`，项目进度显示为 `455 条自动化回归目标`。
- 设置页 `AI 服务状态` 现在默认只显示普通用户需要看的状态：完整 AI 暂不可用、需要模型 key、规则参考保持可用，以及本机模型 key 配置入口。
- AI provider adapter、备用槽位、模型路线、审计、成本限流、密钥管理、输出校验、来源引用、发布和监控等技术细节被收纳到默认折叠的 `展开 AI 技术诊断`。
- 本地浏览器验证 `http://127.0.0.1:4174/?refresh=public-v114-ai-copy-final#settings`：页面加载 `app.js?v=114`，AI 技术诊断默认折叠，普通用户摘要可见，默认文案不再出现 `provider 合同`，没有横向溢出。

## 问题风险
- 完整真实 AI 仍依赖 Render Dashboard 模型密钥和 provider 额度。
- 数据源状态仍有部分技术细节，后续可继续按同样方式收敛。

## 下一步
- 继续把数据源状态里的技术码收敛到折叠诊断区。
- 配置 Render Dashboard 模型密钥后，复测完整 AI 输出。

## English Summary
- v114 updates the cache/script version to `finance-ai-assistant-v114` / `app.js?v=114`, with progress at `455 automated regression targets`.
- The Settings `AI 服务状态` panel now shows only the user-facing state by default: full AI unavailable, model key required, rule reference still available, and local model-key setup.
- Provider adapter, fallback slots, model strategy, audit, cost/rate limits, secret management, output validation, source grounding, release, and monitoring details are now inside collapsed `展开 AI 技术诊断`.
- Local browser verification against `http://127.0.0.1:4174/?refresh=public-v114-ai-copy-final#settings` confirmed `app.js?v=114`, collapsed AI diagnostics, visible user summary, no `provider 合同` wording in the default view, and no horizontal overflow.
- Render auto-deployed v114 to `https://finance-ai-assistant-web.onrender.com`. Live status confirmed homepage, `/health`, `/api/health`, `/api/project/progress`, `/api/ai-services/provider-adapter`, and MSFT analysis all return `200`, with live `app.js?v=114`.
- Live browser verification confirmed the fixed URL's Settings AI service panel keeps AI technical diagnostics collapsed by default, shows the user summary, and has no horizontal overflow.
- Full real AI still depends on Render Dashboard model keys and provider quota.

## Render 固定网址与模型变量补充

## 今日完成
- 已在 Render Environment Variables 的左列补充 4 个真实 AI 模型变量名：`FINANCE_AI_MODEL_API_KEY`、`FINANCE_AI_MODEL_FALLBACK_API_KEY`、`FINANCE_AI_MODEL_FALLBACK2_API_KEY`、`FINANCE_AI_MODEL_FALLBACK3_API_KEY`；真实 key 值只保存在 Render Dashboard，不写入仓库、日志或项目文档。
- `render.yaml` 新增非密钥变量 `FINANCE_AI_STABLE_PUBLIC_URL=https://finance-ai-assistant-web.onrender.com`，让托管环境明确知道当前固定公开测试网址。
- `scripts/stable-access-gate.mjs` 与 `scripts/stable-hosting-preflight.mjs` 默认使用固定 Render 网址作为稳定验收目标；以后运行稳定门禁时，不再默认退回到临时 `lhr.life` 入口。
- 更新 `tests/stable-access-gate.test.mjs`，现在断言固定网址已配置，但仍必须通过 2-3 分钟连续健康门禁后才算稳定外部可用。
- 公开接口复测：`/health` 返回 `200`，`/api/ai-services/provider-adapter` 返回 `200` 且显示主模型和 3 个备用模型均已配置、可接力。
- MSFT 分析接口返回 `200`，但当前仍是 `real-data-rule-reference`：主模型 `gpt-5.5` 额度不足，Gemini 与 Qwen 限流，Groq Llama 调用成功但结构化输出缺少必需指标，metric repair 仍失败；系统正确保留规则参考概率而没有假装完整 AI 成功。
- 新增真实模型第二级结构修复：普通 metric repair 仍缺指标或结构时，会追加一次 `forced metric-shape repair`，强制模型返回固定 JSON 形状、6 个因子和 bull/base/bear 3 个情景；这仍然必须由真实模型返回完整 JSON，不用本地默认值冒充完整 AI。
- 已提交并推送到 GitHub `main`，提交 `916ae8d Add stable Render gate and forced AI metric repair`；Render 固定网址复测 `/health`、`/api/ai-services/provider-adapter`、MSFT 分析接口均返回 `200`。
- 推送后公开 MSFT 复测仍降级为 `real-data-rule-reference`，但失败主因变为 provider 限流：`gpt-5.5` 额度不足，Gemini/Qwen/Groq Llama 均返回额度或速率受限。因此本轮代码修复路径已就绪，但当前没有一个备用模型可成功进入完整结构化输出阶段。

## 问题风险
- 这次只保存了固定网址和模型变量名配置；完整真实 AI 是否成功，还要等 Render 重新部署后用公开接口复测真实模型输出。
- 没有把任何 API key 写入项目文件；如果 Render 里右侧 Value 填错或额度不足，页面仍会显示规则参考或模型受限状态。
- 当前实测 key 已生效，但完整 AI 仍受 provider 额度、限流和小模型结构化输出能力影响；下一轮应优先解决 Groq/OpenRouter 备用模型输出完整度或切换更强的免费/低成本模型。
- 公开环境当前所有可用 AI provider 都处于额度不足或限流状态；如果不补主模型额度或更换可用 fallback，即使结构修复代码存在，也无法生成完整 AI。

## 下一步
- 等 Render 完成新部署后，运行固定网址 live status 和 AI 分析接口复测。
- 如果完整 AI 仍失败，继续看每个 provider 的失败类型、冷却时间和输出校验原因。
- 下一轮优先：更换或新增一个当前可用、免费/低成本、支持稳定 JSON 输出的备用模型，或给主模型补额度；之后再复测完整 AI 是否能从 `real-provider` 输出。

## English Summary
- Completed the Render Environment Variables left-column names for the four real AI model keys: `FINANCE_AI_MODEL_API_KEY`, `FINANCE_AI_MODEL_FALLBACK_API_KEY`, `FINANCE_AI_MODEL_FALLBACK2_API_KEY`, and `FINANCE_AI_MODEL_FALLBACK3_API_KEY`; real key values remain only in Render Dashboard and are not stored in the repo, logs, or docs.
- Added non-secret `FINANCE_AI_STABLE_PUBLIC_URL=https://finance-ai-assistant-web.onrender.com` to `render.yaml`, so the hosted runtime knows the current fixed public test URL.
- `scripts/stable-access-gate.mjs` and `scripts/stable-hosting-preflight.mjs` now default to the fixed Render URL for stable validation instead of falling back to temporary `lhr.life` links.
- Updated `tests/stable-access-gate.test.mjs`: the fixed URL is considered configured, but stable external use still requires the 2-3 minute continuous health gate.
- Verification passed: stable access / stable hosting / Render env-template tests `8/8`, plus syntax checks for both stable-access scripts.
- Public runtime check returned `200` for `/health` and `/api/ai-services/provider-adapter`; all model keys are now visible to the backend as configured and relay-ready.
- MSFT analysis still returned `real-data-rule-reference`, not full AI: `gpt-5.5` had insufficient quota, Gemini/Qwen were rate-limited, and Groq Llama returned incomplete structured metrics after repair.
- Added a second-level real-model repair path: if normal metric repair is still incomplete, the backend now asks the model for a forced exact JSON shape with 6 factor rows and 3 scenarios. This still requires a real model response; it does not fabricate full AI locally.
- Pushed commit `916ae8d` to GitHub `main`. The fixed Render URL returned `200` for `/health`, `/api/ai-services/provider-adapter`, and MSFT analysis after the push.
- Public MSFT retest still returned `real-data-rule-reference`; the current blocker is now provider quota/rate-limit across all attempted models, including Groq Llama.

## AI 同 Key 备用模型轮换

## 今日完成
- 后端 AI provider adapter 新增 `*_ALT_IDS` 支持：同一个 fallback provider / API key 可以拆成多个模型依次尝试，不再一个 key 只绑死一个模型。
- `render.yaml` 为 Groq 备用 3 新增 `FINANCE_AI_MODEL_FALLBACK3_ALT_IDS=openai/gpt-oss-120b`。该模型 ID 来自 Groq 官方模型文档，不需要新增 key。
- `scripts/stable-hosting-preflight.mjs` 同步检查 Groq 备用模型轮换变量，避免 Render 蓝图漏配。
- 新增回归测试：主模型限流、Groq `llama-3.1-8b-instant` 限流后，会继续尝试同一个 Groq key 下的 `openai/gpt-oss-120b`，并在成功时显示完整 AI。

## 问题风险
- 如果 Groq 账户级额度整体耗尽，轮换到 `openai/gpt-oss-120b` 仍可能继续 429；但如果只是单模型或短窗口限流，这能提高完整 AI 命中率。
- Render 需要重新部署后才会读取 `FINANCE_AI_MODEL_FALLBACK3_ALT_IDS`。

## 下一步
- 推送到 GitHub，等待 Render 自动部署后复测 `/api/ai-services/provider-adapter` 是否显示 Groq 轮换模型。
- 继续复测 MSFT 分析是否能通过 `openai/gpt-oss-120b` 生成完整 AI。

## English Summary
- Added `*_ALT_IDS` support to the AI provider adapter, so one fallback provider/API key can rotate through multiple model IDs instead of being locked to a single model.
- Added `FINANCE_AI_MODEL_FALLBACK3_ALT_IDS=openai/gpt-oss-120b` to `render.yaml` for the Groq fallback. This model ID is from Groq's official model docs and does not require a new key.
- Stable-hosting preflight now checks this Groq alternate-model variable.
- Added regression coverage proving the relay tries `openai/gpt-oss-120b` after the primary and `llama-3.1-8b-instant` are rate-limited.
- Verification passed: AI provider relay / stable-hosting / Render env-template tests `13/13`, plus syntax checks.
- GitHub push `7a53596` triggered Render auto-deploy. Public provider status now lists `fallback3-alt1` / `openai/gpt-oss-120b` as configured and relay-ready.
- Public MSFT analysis now succeeds as full real AI: `serviceMode=real-provider`, `serviceModel=openai/gpt-oss-120b`, `providerRelay.used=openai/gpt-oss-120b`, `modelIssue=null`, 6 factor rows, and 3 scenario cases. The relay still records primary quota and earlier fallback limits, but the final alternate model passes validation and can show `完整 AI 分析已生成`.

## 固定 Render 3 分钟稳定门禁复核

## 今日完成
- 使用固定网址 `https://finance-ai-assistant-web.onrender.com` 运行 180 秒连续稳定门禁，检查首页、`/health`、`/api/health`、`/api/analysis?symbol=MSFT&riskProfile=balanced` 和 `/api/stocks/search?q=Microsoft`。
- 权威网络复测结果：`ok=true`、`smokeReady=true`、`externalUseReady=true`、`continuousHealthPassed=true`、`iterationCount=5`、`lastFailure=null`、`blockers=[]`。
- 门禁后再次复测 MSFT 分析接口：返回 `200`，仍为完整真实 AI，`serviceMode=real-provider`、`serviceModel=openai/gpt-oss-120b`、`modelIssue=null`、最终尝试 `完整 AI 分析已生成 / 校验通过`，并保留 6 个因子和 3 个情景。

## 问题风险
- 非提权沙盒网络曾出现 DNS 解析失败，但提权网络门禁和普通 curl 复测均证明固定 Render 网址可达；后续稳定性判断应以外部网络/Render 监控为准。
- 固定测试环境已通过当前 3 分钟门禁，但正式上线仍需要更长周期监控、告警和生产运维策略。

## 下一步
- 继续处理普通用户界面剩余项：新闻加载空状态、来源可信度说明、首页技术诊断收敛和数据更新时间展示。

## English Summary
- Ran a 180-second stable-access gate against `https://finance-ai-assistant-web.onrender.com`, covering homepage, `/health`, `/api/health`, MSFT analysis, and stock search.
- Authoritative network result: `ok=true`, `smokeReady=true`, `externalUseReady=true`, `continuousHealthPassed=true`, `iterationCount=5`, `lastFailure=null`, and no blockers.
- Post-gate MSFT analysis still returned full real AI: `serviceMode=real-provider`, `serviceModel=openai/gpt-oss-120b`, `modelIssue=null`, final attempt `完整 AI 分析已生成 / 校验通过`, with 6 factors and 3 scenarios.

## 新闻加载态与来源可信度 UX / News Loading and Source-Credibility UX

## 今日完成
- 前端版本升级到 `finance-ai-assistant-v115`，`index.html` 加载 `app.js?v=115`，项目进度递增到 `456 条自动化回归目标`。
- 新闻加载中状态改为明确显示：已请求几个来源、当前已返回几条、正在按公司名/ticker/产品词/公告/公开言论做相关性过滤。
- 新闻空结果改为完成态文案：`筛选完成：暂未发现高重要性新闻`，避免刚打开页面时把“加载中”误认为“没有新闻”。
- 低可信来源提示增加解释：低于 70 分阈值，仅作辅助参考，不直接推动结论，需要更多公告或权威来源确认。
- 补充前端回归测试 `connected backend news loading state explains requested sources and filtering progress`，并加强低可信来源断言。

## 问题风险
- 本轮主要解决普通用户可见的新闻状态说明；真实新闻 provider 的可用性、授权和更长周期监控仍按现有数据源路线继续处理。
- 本地静态预览没有接入后端 API，因此浏览器验证只确认前端 v115 和空结果/覆盖文案；真实线上数据仍需 Render 部署后复测。

## 下一步
- 推送后等待 Render 自动部署，再检查固定网址是否加载 `app.js?v=115`。
- 继续处理剩余普通用户体验项：更多数据更新时间可见性、首页/设置页诊断信息进一步收敛、规则参考与完整 AI 的视觉区分复核。

## English Summary
- Upgraded the frontend to `finance-ai-assistant-v115`; `index.html` now loads `app.js?v=115`, and project progress is `456 regression targets`.
- News loading now explicitly shows how many sources were requested, how many items have returned so far, and that relevance filtering is in progress.
- Empty news results now say `filtering completed: no high-importance news found`, preventing loading from being mistaken as no news.
- Low-credibility source warnings now explain the below-70 threshold, mark the item as auxiliary evidence, and say it should not directly drive conclusions without stronger confirmation.
- Verification passed: syntax checks for `app.js` and `service-worker.js`; frontend regression `178/178`; PWA assets `5/5`; Render live-status `3/3`; local browser loaded `app.js?v=115` and showed the completed empty-state wording.

## Render v115 线上状态一致性 / Render v115 Live-Status Consistency

## 今日完成
- 固定 Render 网址已确认加载 `app.js?v=115`。
- 修正后端 `/api/project/progress` 的完成项文案，从 `455 条自动化回归目标` 对齐到 `456 条自动化回归目标`。
- `scripts/render-live-status.mjs` 新增分析接口独立超时：普通健康接口仍默认 15 秒，`/api/analysis` 默认 30 秒，并支持 `--analysis-timeout-ms` / `FINANCE_AI_RENDER_LIVE_STATUS_ANALYSIS_TIMEOUT_MS`。
- 新增回归测试，证明即使普通接口超时窗口很短，分析 smoke 也可以使用更长窗口，避免真实 AI 偶发慢响应造成误报。
- 复测固定网址：首页、`/health`、`/api/health`、`/api/project/progress`、`/api/ai-services/provider-adapter` 和 MSFT 分析接口均返回 `200`；MSFT 分析约 2.36 秒。
- 复核“规则参考”和“完整 AI”视觉区分：聚焦前端测试通过，规则参考不会被标成完整 AI，完整 AI 成功态也不会显示规则参考标签。

## 问题风险
- 当前线上 MSFT 分析虽然返回 `200`，但处于 `real-data-rule-reference`：所有模型尝试均为 `REAL_AI_MODEL_PROVIDER_COOLDOWN_ACTIVE`，说明完整 AI 不是持续可用状态。
- 后端全量测试中提醒/通知/调度相关 5 个用例失败，本轮改动不涉及这些模块；已用聚焦项目进度测试确认本轮后端进度改动通过。

## 下一步
- 等 provider 冷却结束后，继续复测完整 AI 是否重新回到 `real-provider`。
- 继续处理剩余普通用户体验项：设置页内部诊断进一步收敛、线上完整 AI 冷却状态更清晰、正式上线级监控。

## English Summary
- Confirmed the fixed Render URL loads `app.js?v=115`.
- Aligned backend `/api/project/progress` from `455 regression targets` to `456 regression targets`.
- Added a dedicated analysis timeout to `scripts/render-live-status.mjs`: normal health endpoints remain at 15s, while `/api/analysis` defaults to 30s via `--analysis-timeout-ms` / `FINANCE_AI_RENDER_LIVE_STATUS_ANALYSIS_TIMEOUT_MS`.
- Added regression coverage proving the analysis smoke can use the longer timeout.
- Public retest returned `200` for homepage, `/health`, `/api/health`, `/api/project/progress`, `/api/ai-services/provider-adapter`, and MSFT analysis; MSFT analysis took about 2.36s.
- Current MSFT output is still `real-data-rule-reference` because all provider attempts are in `REAL_AI_MODEL_PROVIDER_COOLDOWN_ACTIVE`; full AI should not be described as continuously available during cooldown.

## AI 冷却状态摘要 / AI Cooldown Status Summary

## 今日完成
- `scripts/render-live-status.mjs` 的 `analysis` 段新增 `relayAttempts`，逐个展示模型角色、模型名、错误码、最终主因、调用状态、输出状态、校验状态、上次失败时间、建议重试时间和下一步动作。
- 新增 `analysis.cooldown` 摘要：是否处于冷却、冷却模型数量、最早重试时间、最长等待秒数、是否还有未冷却备用模型可立即尝试。
- `nextSteps` 现在会明确提示最早建议重试时间，以及当前是否还有未冷却备用模型。
- 线上固定网址复测：全部关键接口返回 `200`，`projectProgress` 已显示 `456 条自动化回归目标`；MSFT 分析返回规则参考，原因是 5 个模型尝试都处在额度/限流/HTTP 错误后的冷却记录中，最早重试时间为 `2026-06-14T16:42:18.037Z`，当前没有未冷却备用模型可立即继续检查。

## 问题风险
- 冷却摘要让失败原因更透明，但没有消除 provider 额度和速率限制本身；完整 AI 仍需等冷却结束、补额度或切换更稳定 provider 后复测。

## 下一步
- 冷却结束后再次运行固定网址 live-status，确认是否重新生成 `real-provider` 完整 AI。
- 若仍然降级规则参考，继续优化备用模型选择或模型请求格式。

## English Summary
- Added per-model `analysis.relayAttempts` to `scripts/render-live-status.mjs`, including role, model, code, final reason, call/output/validation status, last failure time, suggested retry time, and next action.
- Added `analysis.cooldown` with active status, cooldown attempt count, earliest retry time, longest wait, and whether any uncooldown fallback remains immediately available.
- `nextSteps` now includes the earliest retry time and fallback availability.
- Live fixed-URL retest returned `200` for all key endpoints; MSFT analysis is currently rule-reference because all five model attempts are cooling down after quota/rate-limit/HTTP-error results.

## AI 冷却恢复提示前端优化 / AI Cooldown Recovery Hint Frontend UX

## 今日完成
- 前端版本升级到 `finance-ai-assistant-v116`，`index.html` 加载 `app.js?v=116`，项目进度递增到 `457 条自动化回归目标`。
- 完整 AI 简要状态新增用户可读的“恢复提示”，区分：等待冷却、还有明确配置的未冷却备用模型可继续检查、当前没有冷却锁。
- 收紧备用模型提示逻辑：只有 `/api/ai-services/provider-adapter` 明确返回可调用、未冷却、未尝试过的备用模型时，前端才提示“可继续检查未冷却备用模型”；否则提示“当前没有未冷却备用模型可立即继续检查”。
- 技术错误码仍保留在诊断明细里，首页简要原因继续使用普通用户能理解的额度不足、限流、冷却等表达。
- 补充前端回归断言，覆盖规则参考降级时必须显示等待冷却提示，并避免把规则参考误标成完整 AI。
- 验证通过：`app.js` 和 `service-worker.js` 语法检查；前端 AI 聚焦回归 `2/2`；PWA 资源回归 `5/5`；Render live-status 回归 `6/6`。

## 问题风险
- 本轮提升的是冷却解释和恢复路径可读性，不会消除 OpenAI/Gemini/OpenRouter/Groq 自身额度或速率限制。
- 完整 AI 是否可持续输出仍取决于线上 provider 当前额度、冷却状态和模型响应质量。

## 下一步
- 推送后等待 Render 自动部署，再确认固定网址加载 `app.js?v=116`。
- 冷却结束后继续复测 `/api/analysis` 是否能重新返回 `real-provider` 完整 AI。

## English Summary
- Upgraded the frontend to `finance-ai-assistant-v116`; `index.html` now loads `app.js?v=116`, and project progress is `457 regression targets`.
- Added a user-facing recovery hint to the compact full-AI status, distinguishing wait-for-cooldown, confirmed uncooldown fallback available, and no current cooldown lock.
- Tightened fallback wording: the UI only says another model can be checked when the backend reports a callable, configured fallback that has not already been attempted and is not cooling down.
- Technical codes remain in diagnostics, while the compact homepage explanation stays in user-friendly language.
- Added frontend regression assertions for the cooldown wait message and the rule-reference/full-AI distinction.
- Verification passed: `app.js` and `service-worker.js` syntax checks; focused AI frontend regression `2/2`; PWA asset regression `5/5`; Render live-status regression `6/6`.

## Render live-status 完整 AI 成功态修正 / Render Live-Status Full-AI Success Detection

## 今日完成
- 前端缓存版本升级到 `finance-ai-assistant-v117`，`index.html` 加载 `app.js?v=117`，项目进度递增到 `458 条自动化回归目标`。
- 线上固定 Render 检查确认：当前已部署 v116，首页、`/health`、`/api/health`、`/api/project/progress`、`/api/ai-services/provider-adapter`、MSFT 分析接口均返回 `200`。
- 60 秒连续稳定门禁通过：检查首页、健康接口、MSFT 分析、股票搜索共 4 轮，`lastFailure=null`，没有复现 503 或 `no tunnel here`。
- 修正 `scripts/render-live-status.mjs`：完整 AI 成功不再只看 `analysisMode`，也会识别 `analysisService.mode=real-provider`，以及 providerRelay 最终 attempt 的 `完整 AI 分析已生成 / 调用成功 / 校验通过`。
- 修正 `successfulRelayModel`：只有完整 AI 已确认成功时才显示成功模型；冷却降级为规则参考时保持为空，避免误导。
- 新增回归测试，覆盖线上这次出现的形态：`analysisMode` 为空，但最后备用模型 `openai/gpt-oss-120b` 已生成完整 AI。
- 修正后再次复测固定网址：线上仍是 v116，因此部署新版本前 live-status 会提示 stale bundle；同时所有模型进入冷却，最新 MSFT 请求降级为 `real-data-rule-reference`，这说明完整 AI 曾跑通但当前不可连续保证。

## 问题风险
- 本轮证明固定 Render 入口当前稳定，且完整 AI 在本次 MSFT 请求中通过最终备用模型输出；但 provider 额度和冷却仍是外部状态，后续仍需持续监控。
- 当前完整 AI 的可用性会被额度/冷却状态影响；不能把一次成功输出描述成“持续可用”。
- 临时 lhr.life 链接依然不应作为正式演示入口；固定 Render 网址仍是当前优先入口。

## 下一步
- v117 已部署并复测通过；后续继续用固定 Render 网址作为当前主外部测试入口。
- 继续处理剩余 P1/P2：新闻相关性默认展示、首页诊断信息收敛、自选股规则参考状态复核。

## English Summary
- Bumped the frontend cache to `finance-ai-assistant-v117`; `index.html` now loads `app.js?v=117`, and project progress is `458 regression targets`.
- Live fixed-Render check confirmed v116 is deployed and homepage, health, API health, project progress, AI provider adapter, and MSFT analysis all returned `200`.
- The 60-second stability gate passed across homepage, health, MSFT analysis, and stock search for 4 iterations, with `lastFailure=null` and no 503/no-tunnel failure.
- Fixed `scripts/render-live-status.mjs` so full AI success can be detected from `analysisService.mode=real-provider` and from a successful final providerRelay attempt, not only from `analysisMode`.
- Fixed `successfulRelayModel` so it only appears when full AI is actually confirmed; cooldown rule-reference downgrades keep it blank.
- Added regression coverage for the live shape where `analysisMode` is omitted but `openai/gpt-oss-120b` successfully generated full AI.
- A follow-up fixed-URL retest still saw live v116 before deployment, and the latest MSFT request downgraded to `real-data-rule-reference` because all five providers were cooling down. This means full AI has been proven once but is not continuously available yet.
- Post-deploy verification passed: fixed Render is now v117, all key endpoints returned `200`, MSFT analysis returned `real-provider` with `successfulRelayModel=openai/gpt-oss-120b`, and the 60-second stability gate passed 4 iterations with `lastFailure=null`.

## 新闻去重与默认展示降噪 / News Dedupe and Default Display Noise Reduction

## 今日完成
- 前端缓存版本升级到 `finance-ai-assistant-v118`，`index.html` 加载 `app.js?v=118`，项目进度递增到 `459 条自动化回归目标`。
- 新闻展示层新增去重：先按个股相关性和重要性排序，再按原文 URL 或规范化标题去重，保留最该展示的一条。
- 新闻摘要新增 `已去重 N`，让用户知道折叠新闻里有多少重复标题被合并。
- 保持第一屏规则：如果有公司直接新闻、公告或公开言论，第一屏最多展示 5 条；行业、供应链、监管和市场泛新闻继续默认折叠。
- 新增回归测试，覆盖 Cadence/Ryanair 这类弱相关新闻重复出现时只保留一条，并且不会进入第一屏个股重点新闻。

## 问题风险
- 本轮只做展示层去重，不改变真实新闻 provider 返回内容；如果 provider 返回标题语义相同但措辞差异很大，后续还需要更强的语义去重。
- 新闻相关性仍需继续在后端 provider 层加强，特别是供应链/监管关系的可解释证据。

## 下一步
- v118 已部署并复测通过；固定 Render 网址继续作为当前主外部测试入口。
- 继续处理自选股卡片“规则参考/完整 AI”双状态，以及首页内部诊断信息收敛。

## English Summary
- Bumped the frontend cache to `finance-ai-assistant-v118`; `index.html` now loads `app.js?v=118`, and project progress is `459 regression targets`.
- Added display-layer news dedupe: items are sorted by stock relevance and importance first, then deduped by source URL or normalized title, keeping the best item.
- News summary now shows `deduped N` so users can see repeated folded headlines were merged.
- First-screen behavior remains strict: direct company news, filings, and public statements show first, up to 5 items; industry, supply-chain/regulatory, and market-background items stay folded by default.
- Added regression coverage for duplicate weakly related Cadence/Ryanair-style headlines appearing once and staying outside the first screen.
- Post-deploy verification passed: fixed Render is now v118, all key endpoints returned `200`, MSFT analysis returned `real-provider` with `successfulRelayModel=openai/gpt-oss-120b`, and the 60-second stability gate passed with `lastFailure=null`.

## 普通用户状态摘要收敛 / User-Facing Status Summary Cleanup

## 今日完成
- 前端缓存版本升级到 `finance-ai-assistant-v119`，`index.html` 加载 `app.js?v=119`，项目进度递增到 `460 条自动化回归目标`。
- 设置页项目状态卡片的可见标题从“项目进度 / 真实完成度与上线差距”改为“测试版状态 / 当前可用能力”。
- 普通用户摘要改为直接展示可用能力：固定公开网址可用、真实数据规则参考可用、完整 AI 已验证、新闻已去重折叠。
- 上线差距、生产门禁、数据库、审计、任务队列、来源端点和百分比证据继续保留在默认折叠的开发者详情里。
- 复核自选股卡片：现有测试已覆盖“规则参考 54% / 完整 AI 待模型”，该 P1 项当前已验证。

## 问题风险
- 本轮只收敛状态卡片的可见文案；设置页的开发者详情仍保留完整内部信息，后续如果要做正式普通用户模式，可以继续加入管理员开关。

## 下一步
- 部署后复查 v119 固定网址，确认普通用户状态摘要显示新文案。
- 继续处理首页/设置页剩余内部诊断入口，或进入自选股线上交互复测。

## English Summary
- Bumped the frontend cache to `finance-ai-assistant-v119`; `index.html` now loads `app.js?v=119`, and project progress is `460 regression targets`.
- Changed the visible status card from project-management wording to product wording: `beta status / current available capabilities`.
- The normal summary now shows fixed public URL available, real-data rule reference available, full AI verified, and news dedupe/collapse enabled.
- Launch gap, production gates, database, audit, task queue, source endpoints, and percentage evidence remain in collapsed developer details.
- Rechecked the watchlist card: existing tests already cover `规则参考 54% / 完整 AI 待模型`, so that P1 item is currently verified.

## v119 固定网址部署复测 / v119 Fixed-URL Deployment Verification

## 今日完成
- 固定 Render 网址 `https://finance-ai-assistant-web.onrender.com` 已确认部署 `app.js?v=119`，`deployedLatest=true`。
- 线上接口复测通过：首页、`/health`、`/api/health`、`/api/project/progress`、`/api/ai-services/provider-adapter`、MSFT 分析接口全部返回 `200`。
- 项目进度线上显示为 `460 条自动化回归目标`。
- AI provider adapter 线上显示主模型和 4 个备用模型槽位均已配置、可接力。
- 60 秒固定网址稳定门禁通过：检查首页、健康接口、MSFT 分析和股票搜索共 4 轮，`lastFailure=null`，`externalUseReady=true`。

## 问题风险
- 本轮 MSFT 分析返回 `real-data-rule-reference`，原因是 5 个模型尝试均处于 provider 冷却状态，当前没有未冷却备用模型可立即继续检查。
- 因此当前应表述为“完整 AI 已验证过，但 provider 冷却期间会降级为规则参考”，不能表述为“完整 AI 持续保证可用”。

## 下一步
- 等最早冷却窗口结束后继续复测完整 AI 是否重新返回 `real-provider`。
- 继续使用固定 Render 网址作为当前主外部测试链接，不再依赖临时 `lhr.life` 链接。

## English Summary
- Confirmed the fixed Render URL `https://finance-ai-assistant-web.onrender.com` is serving `app.js?v=119` with `deployedLatest=true`.
- Live endpoint verification passed: homepage, `/health`, `/api/health`, `/api/project/progress`, `/api/ai-services/provider-adapter`, and MSFT analysis all returned `200`.
- Live project progress shows `460 regression targets`.
- The live AI provider adapter shows the primary model and 4 fallback slots configured and relay-ready.
- The 60-second fixed-URL stability gate passed across homepage, health, MSFT analysis, and stock search for 4 iterations with `lastFailure=null` and `externalUseReady=true`.
- Current caveat: this MSFT analysis returned `real-data-rule-reference` because all 5 model attempts were in provider cooldown, so full AI is verified but not continuously guaranteed during cooldown windows.

## 数据更新时间分项展示 / Per-Source Data Freshness Display

## 今日完成
- 前端缓存版本升级到 `finance-ai-assistant-v120`，`index.html` 加载 `app.js?v=120`，项目进度递增到 `461 条自动化回归目标`。
- 股票标题区的数据覆盖提示新增稳定的分项更新时间：行情更新、新闻更新、公告更新、宏观更新。
- 如果某项没有真实数据，明确显示 `待真实数据`，不再因为缺时间而整行消失。
- 修复一个严格真实数据细节：metadata-only 股票没有真实行情时，不再拿本机样例历史行情的 `updatedAt` 当成行情更新时间。
- 更新时间提示增加轻量胶囊样式，普通用户能扫到数据新鲜度，但不会变成开发诊断页面。

## 问题风险
- 这轮只改善用户可见的新鲜度展示，不改变 provider 本身的返回速度或额度限制。
- 如果真实 provider 不返回时间戳，页面会如实显示“更新时间待确认”或“待真实数据”。

## 验证
- 语法检查通过：`app.js`、`service-worker.js`、`backend/server.mjs`。
- 前端聚焦回归通过：metadata-only 搜索、真实行情接入、免费 API 行情接力、新闻接入、AI 规则参考标签共 `7/7`。
- PWA 回归通过：`5/5`。
- Render live-status 回归通过：`7/7`。

## English Summary
- Bumped the frontend cache to `finance-ai-assistant-v120`; `index.html` now loads `app.js?v=120`, and project progress is `461 regression targets`.
- Added stable per-source freshness/status chips in the stock header: quote, news, filings, and macro.
- If a source has no real data, the UI now explicitly says `pending real data` instead of silently hiding that freshness row.
- Fixed a strict-real-data issue: metadata-only stocks no longer borrow local sample history `updatedAt` as quote freshness when no real quote is connected.
- Verification passed: syntax checks for `app.js`, `service-worker.js`, and `backend/server.mjs`; focused frontend regression `7/7`; PWA `5/5`; Render live-status `7/7`.

## v120 固定网址部署复测 / v120 Fixed-URL Deployment Verification

## 今日完成
- 固定 Render 网址已确认部署 `app.js?v=120`，`deployedLatest=true`。
- 线上接口复测通过：首页、`/health`、`/api/health`、`/api/project/progress`、`/api/ai-services/provider-adapter`、MSFT 分析接口全部返回 `200`。
- 项目进度线上显示为 `461 条自动化回归目标`。
- 60 秒固定网址稳定门禁通过：检查首页、健康接口、MSFT 分析和股票搜索共 4 轮，`lastFailure=null`，`externalUseReady=true`。

## 问题风险
- 本轮 MSFT 分析仍返回 `real-data-rule-reference`，因为主模型额度不足，Gemini/Qwen/Llama 限流，`openai/gpt-oss-120b` 返回 HTTP 400；所有失败都已记录冷却窗口。
- 固定网址和接口稳定，但完整 AI 仍受外部 provider 额度、速率和模型响应影响。

## English Summary
- Confirmed the fixed Render URL is serving `app.js?v=120` with `deployedLatest=true`.
- Live endpoint verification passed: homepage, `/health`, `/api/health`, `/api/project/progress`, `/api/ai-services/provider-adapter`, and MSFT analysis all returned `200`.
- Live project progress shows `461 regression targets`.
- The 60-second fixed-URL stability gate passed across homepage, health, MSFT analysis, and stock search for 4 iterations with `lastFailure=null` and `externalUseReady=true`.
- Current caveat: this MSFT analysis still returned `real-data-rule-reference` because providers hit quota/rate-limit/HTTP-error cooldown windows.

## AI 兼容接口 400 自恢复 / AI Compatible-API 400 Recovery

## 今日完成
- 前端缓存版本升级到 `finance-ai-assistant-v121`，`index.html` 加载 `app.js?v=121`，项目进度递增到 `462 条自动化回归目标`。
- 修复完整 AI 接力中的一个可控失败点：非 Gemini 的 OpenAI-compatible chat 接口如果带 `response_format: json_object` 返回 400，系统会自动用 compact JSON-only prompt 重试一次，并去掉 `response_format`。
- 重试成功后仍然走严格 JSON 提取、核心指标校验、安全表达校验和来源约束，不会把不可校验输出当成完整 AI。
- provider HTTP 错误现在会保留脱敏后的 provider message，技术诊断不再只有笼统的 `HTTP 400`。

## 问题风险
- 这轮提升的是参数兼容和自恢复能力；如果 provider 仍然额度不足、限流、模型不存在或服务端拒绝请求，仍会如实降级为规则参考。
- 完整 AI 是否持续可用仍取决于 provider 额度、冷却窗口和模型返回质量。

## 验证
- 语法检查通过：`app.js`、`service-worker.js`、`backend/server.mjs`、`backend/services/ai-provider-adapter.mjs`。
- AI provider relay 回归通过：`8/8`。
- PWA 回归通过：`5/5`。
- Render live-status 回归通过：`7/7`。
- 前端聚焦回归通过：`9/9`。
- 前端完整回归通过：`179/179`。

## English Summary
- Bumped the frontend cache to `finance-ai-assistant-v121`; `index.html` now loads `app.js?v=121`, and project progress is `462 regression targets`.
- Fixed a controllable full-AI relay failure: if a non-Gemini OpenAI-compatible chat endpoint returns 400 when `response_format: json_object` is sent, the system retries once with a compact JSON-only prompt and omits `response_format`.
- Successful retries still go through strict JSON extraction, required metric validation, safety validation, and source constraints.
- Provider HTTP errors now keep a sanitized provider message in diagnostics, so `HTTP 400` is easier to debug.
- Verification passed: syntax checks; AI provider relay `8/8`; PWA `5/5`; Render live-status `7/7`; focused frontend `9/9`; full frontend regression `179/179`.

## AI 非 JSON 输出压缩重试 / AI Invalid-JSON Compact Retry

## 今日完成
- 前端缓存版本升级到 `finance-ai-assistant-v122`，`index.html` 加载 `app.js?v=122`，项目进度递增到 `463 条自动化回归目标`。
- 针对线上新暴露的问题继续修复：`openai/gpt-oss-120b` 可以调用成功，但返回内容不是可解析 JSON 时，系统会自动用 compact 和 ultra-compact JSON-only prompt 再试。
- 如果上一层已经因为 400 去掉了 `response_format`，后续 JSON 修复重试会继续保留这个兼容状态。
- 校验规则不放松：仍必须通过 JSON 提取、核心量化指标、六因子结构、情景分析结构和安全表达校验，失败才降级规则参考。

## 问题风险
- 该修复提高“模型已调用但输出格式不稳”时的成功率；如果 provider 限流、主模型额度不足或模型持续输出不合规，仍会降级为规则参考。
- 线上完整 AI 是否连续可用仍需要部署后复测，不能只凭本地单测判断。

## 验证
- 语法检查通过：`app.js`、`service-worker.js`、`backend/server.mjs`、`backend/services/ai-provider-adapter.mjs`。
- AI provider relay 回归通过：`9/9`。
- PWA 回归通过：`5/5`。
- Render live-status 回归通过：`7/7`。
- 前端聚焦回归通过：`9/9`。
- 前端完整回归通过：`179/179`。

## English Summary
- Bumped the frontend cache to `finance-ai-assistant-v122`; `index.html` now loads `app.js?v=122`, and project progress is `463 regression targets`.
- Continued fixing the newly exposed live issue: when `openai/gpt-oss-120b` is callable but returns non-parseable JSON, the system retries with compact and ultra-compact JSON-only prompts.
- If the prior 400 recovery removed `response_format`, the invalid-JSON retries preserve that compatibility state.
- Validation remains strict: JSON extraction, core metrics, factor breakdown, scenario structure, and safety checks must still pass before showing full AI.
- Verification passed: syntax checks; AI provider relay `9/9`; PWA `5/5`; Render live-status `7/7`; focused frontend `9/9`; full frontend regression `179/179`.

## AI 最短修复提示结构加固 / AI Ultra-Compact Shape Hardening

## 今日完成
- 前端缓存版本升级到 `finance-ai-assistant-v123`，`index.html` 加载 `app.js?v=123`，项目进度递增到 `465 条自动化回归目标`。
- ultra-compact JSON 修复提示现在也包含完整结构：6 个 `factorBreakdown` 行和 3 个 `scenarioAnalysis.cases`。
- 目的：避免模型在最短修复路径里只补顶层概率，却漏掉六因子和情景分析，导致完整 AI 仍无法展示。

## 问题风险
- 这仍然依赖 provider 遵循结构化提示；如果模型持续输出缺字段，系统仍会如实降级规则参考。
- 后续可考虑更强的结构化二次修复或更稳定的模型供应商。

## 验证
- 语法检查通过：`app.js`、`service-worker.js`、`backend/server.mjs`、`backend/services/ai-provider-adapter.mjs`。
- AI provider relay 回归通过：`9/9`。
- PWA 回归通过：`5/5`。
- Render live-status 回归通过：`7/7`。
- 前端聚焦回归通过：`9/9`。

## English Summary
- Bumped the frontend cache to `finance-ai-assistant-v123`; `index.html` now loads `app.js?v=123`, and project progress is `465 regression targets`.
- The ultra-compact JSON repair prompt now includes the full required structure: 6 `factorBreakdown` rows and 3 `scenarioAnalysis.cases`.
- This prevents the shortest repair path from only asking for top-level probabilities while losing the factor and scenario sections.
- Verification passed: syntax checks; AI provider relay `9/9`; PWA `5/5`; Render live-status `7/7`; focused frontend `9/9`.

## v123 固定网址部署复测 / v123 Fixed-URL Deployment Verification

## 今日完成
- 固定 Render 网址已确认部署 `app.js?v=123`，`deployedLatest=true`。
- 线上接口复测通过：首页、`/health`、`/api/health`、`/api/project/progress`、`/api/ai-services/provider-adapter`、MSFT 分析接口全部返回 `200`。
- 项目进度线上显示为 `465 条自动化回归目标`。
- MSFT 分析返回完整真实 AI：`analysisMode=real-provider`，成功模型为 `openai/gpt-oss-120b`。
- 最终 AI relay 状态为：`调用成功 / 完整 AI 输出可用 / 校验通过`。
- 60 秒固定网址稳定门禁通过，检查首页、健康接口、MSFT 分析和股票搜索，`lastFailure=null`，`externalUseReady=true`。

## 问题风险
- 主模型 `gpt-5.5` 仍然额度不足，Gemini/Qwen/Llama 仍可能限流；完整 AI 当前依赖 Groq 备用轮换模型成功输出。
- 因 provider 额度和冷却状态会变化，仍需保留规则参考降级和冷却说明。

## English Summary
- Confirmed the fixed Render URL is serving `app.js?v=123` with `deployedLatest=true`.
- Live endpoint verification passed: homepage, `/health`, `/api/health`, `/api/project/progress`, `/api/ai-services/provider-adapter`, and MSFT analysis all returned `200`.
- Live project progress shows `465 regression targets`.
- MSFT analysis returned full real AI: `analysisMode=real-provider`, with `openai/gpt-oss-120b` as the successful model.
- The final AI relay status was `call succeeded / full AI output available / validation passed`.
- The 60-second fixed-URL stability gate passed with `lastFailure=null` and `externalUseReady=true`.

## P0 180 秒固定网址稳定门禁 / P0 180-Second Fixed-URL Stability Gate

## 今日完成
- 按目标要求补跑 2-3 分钟连续稳定检查，而不是只看一次或只跑 60 秒。
- 固定 Render 网址 `https://finance-ai-assistant-web.onrender.com` 通过 180 秒连续门禁。
- 检查端点包括：首页 `/`、`/health`、`/api/health`、MSFT 分析 `/api/analysis?symbol=MSFT&riskProfile=balanced`、股票搜索 `/api/stocks/search?q=Microsoft`。
- 结果：`ok=true`、`externalUseReady=true`、`continuousHealthPassed=true`、`requiredDurationSeconds=180`、`iterationCount=5`、`lastFailure=null`。

## 问题风险
- 这证明当前固定 Render 网址满足本轮 2-3 分钟连续可用门禁。
- 这不改变 `lhr.life` 临时隧道的性质；临时链接仍可能 503，因此正式测试入口继续使用固定 Render 网址。

## English Summary
- Ran the objective-level 2-3 minute continuous stability check instead of relying on a one-shot or 60-second smoke.
- The fixed Render URL `https://finance-ai-assistant-web.onrender.com` passed the 180-second gate.
- Checked endpoints: `/`, `/health`, `/api/health`, `/api/analysis?symbol=MSFT&riskProfile=balanced`, and `/api/stocks/search?q=Microsoft`.
- Result: `ok=true`, `externalUseReady=true`, `continuousHealthPassed=true`, `requiredDurationSeconds=180`, `iterationCount=5`, and `lastFailure=null`.
- This proves the current fixed Render URL passes the P0 2-3 minute gate; temporary `lhr.life` links should still be treated as temporary only.

## 固定托管默认门禁锁定 / Stable-Hosting Default Gate Lock

## 今日完成
- 把固定托管预检默认门禁参数显式化：默认时长 `180000ms`、间隔 `15000ms`、超时 `15000ms`。
- `runStableHostingPreflight` 现在即使通过程序化调用、没有手动传 `durationMs`，也会明确向 health check 传入 180 秒门禁。
- 新增回归测试 `stable hosting preflight defaults to the 180-second public stability gate`，防止未来默认值被误改回短 smoke。

## 验证
- 稳定托管/稳定访问/交接页测试通过：`11/11`。
- Render live-status 回归通过：`7/7`。
- PWA 回归通过：`5/5`。
- 语法检查通过：`scripts/stable-hosting-preflight.mjs`、`scripts/stable-access-gate.mjs`。

## English Summary
- Made the fixed-hosting preflight defaults explicit: `180000ms` duration, `15000ms` interval, and `15000ms` timeout.
- `runStableHostingPreflight` now passes the 180-second gate explicitly to the health checker even in programmatic calls without `durationMs`.
- Added regression `stable hosting preflight defaults to the 180-second public stability gate`, preventing future accidental reduction to a short smoke.
- Verification passed: stable hosting/access/handoff `11/11`, Render live-status `7/7`, PWA `5/5`, and syntax checks.

## Render AI Key 左列补充 / Render AI Key Left-Column Completion

## 今日完成
- 用户已按顺序重新创建并复制 OpenAI、Gemini、OpenRouter、Groq 的模型 key。
- 已把 Render Environment Variables 左列应填变量名保存到 `docs/product/REMEDIATION_STATUS.md` 和 `docs/technical/DEPLOYMENT.md`：
  - `FINANCE_AI_MODEL_API_KEY`
  - `FINANCE_AI_MODEL_FALLBACK_API_KEY`
  - `FINANCE_AI_MODEL_FALLBACK2_API_KEY`
  - `FINANCE_AI_MODEL_FALLBACK3_API_KEY`
- 已补充项目目标修复证据表，方便以后核对 P0/P1/P2/P3 的完成状态。

## 问题风险
- Chrome 当前没有打开 Render 环境变量页面，因此本轮没有代替用户点击 Render 的 Save。
- 项目文档只保存变量名，不保存任何真实 key；真实 key 仍应只留在 Render Dashboard 的 Value 字段。

## English Summary
- The user recreated and copied model keys in the order OpenAI, Gemini, OpenRouter, and Groq.
- Saved the Render Environment Variables left-column names into `docs/product/REMEDIATION_STATUS.md` and `docs/technical/DEPLOYMENT.md`.
- Added a remediation evidence matrix for future P0/P1/P2/P3 completion checks.
- Chrome did not have the Render environment page open, so this turn did not click Render's Save button.
- The project docs store variable names only, never real key values.

## 完整 AI 稳定性门禁 / Full-AI Stability Gate

## 今日完成
- 新增 `scripts/render-full-ai-stability-gate.mjs` 和 `npm run gate:full-ai`。
- 该门禁连续调用固定 Render 线上状态检查，默认 3 轮；只有每一轮都返回完整真实 AI，才算通过。
- 如果其中一轮降级为规则参考、provider 冷却、接口失败或版本未更新，门禁会失败，并输出失败轮次、最早重试时间和下一步。

## 问题风险
- 该门禁不会让 provider 额度变多；它只是更诚实地区分“偶尔跑通”和“稳定可用”。
- 当前如果 Render key 没保存、provider 限流或冷却，门禁会失败，这是预期行为。

## English Summary
- Added `scripts/render-full-ai-stability-gate.mjs` and `npm run gate:full-ai`.
- The gate calls the fixed Render live-status check repeatedly, defaulting to 3 attempts, and passes only when every attempt returns full real AI.
- Any rule-reference fallback, provider cooldown, endpoint failure, or stale deployed version fails the gate and reports the failing attempt, earliest retry time, and next step.
- This does not increase provider quota; it makes the remaining reliability gap measurable.

## Responses JSON 修复增强 / Responses JSON Repair Hardening

## 今日完成
- 真实线上完整 AI 快速门禁显示：固定 Render 网址和所有关键接口均为 `200`，部署版本为 `v123`，但两轮完整 AI 都因 `REAL_AI_MODEL_INVALID_JSON` 和 provider 冷却降级为规则参考。
- 针对该失败主因，已增强 Responses API 路径：如果第一次未返回可解析 JSON，会改用 compact JSON prompt，再失败则改用 ultra-compact JSON prompt。
- 该路径现在与 chat-completions 的 JSON 修复策略一致。

## 验证
- 语法检查通过：`backend/services/ai-provider-adapter.mjs`、`scripts/render-full-ai-stability-gate.mjs`。
- AI relay、完整 AI 稳定门禁和 Render live-status 相关测试通过：`20/20`。

## English Summary
- The live quick full-AI gate showed the fixed Render URL and all key endpoints returning `200` with deployed version `v123`, but both full-AI attempts fell back to rule reference due to `REAL_AI_MODEL_INVALID_JSON` plus provider cooldown.
- To target that failure, the Responses API path now retries invalid JSON with compact and then ultra-compact JSON prompts.
- This makes the Responses path match the existing chat-completions JSON repair strategy.
- Verification passed: syntax checks and the AI relay/full-AI gate/Render live-status tests `20/20`.

## Render Key 保存后复测 / Post-Key Render Retest

## 今日完成
- 在 Render Environment 页面核对到 4 个 AI key 左列变量名均存在：OpenAI 主模型、Gemini、OpenRouter、Groq。
- 未读取、复制或记录右侧真实 key 值。
- 触发 Render `Manual Deploy -> Deploy latest commit`，页面显示服务已 `Live`。
- 线上 `render-live-status` 通过：固定网址、首页、健康接口、项目进度、AI provider、MSFT 分析接口全部 `200`；线上版本 `v123`；AI 主模型已配置，4 个备用模型均显示可接力，缺失 env vars 为 0。
- 线上完整 AI 三轮稳定门禁未通过：第 1 轮完整 AI 成功，模型 `openai/gpt-oss-120b`；第 2、3 轮因 `REAL_AI_MODEL_RATE_LIMIT_OR_QUOTA` / `REAL_AI_MODEL_PROVIDER_COOLDOWN_ACTIVE` 降级规则参考。

## 问题风险
- key 已保存并生效，但免费/低额度 provider 仍会触发 TPM/rate-limit 和冷却，因此完整 AI 尚不能视为稳定可用。
- 当前最早重试时间约为 `2026-06-14T20:12:28Z`；应等冷却结束后再跑 `npm run gate:full-ai -- --attempts 3 --interval-ms 10000 --analysis-timeout-ms 60000`。

## English Summary
- Confirmed the four AI key variable names exist in Render Environment for OpenAI, Gemini, OpenRouter, and Groq.
- Did not read, copy, or record any real key values.
- Triggered `Manual Deploy -> Deploy latest commit`; Render reached `Live`.
- `render-live-status` passed: fixed URL, homepage, health endpoints, progress, AI provider, and MSFT analysis all returned `200`; live version is `v123`; the primary model is configured; all four fallback providers are configured/callable; missing env vars are `0`.
- The three-attempt full-AI stability gate still failed: attempt 1 produced full AI via `openai/gpt-oss-120b`, while attempts 2 and 3 fell back to rule reference due to provider rate-limit/cooldown.

## Render 冷启动预热 / Render Cold-Start Warmup

## 今日完成
- 新增 GitHub Actions 工作流 `.github/workflows/render-health-warmup.yml`。
- 工作流每 10 分钟请求固定 Render 网址 `/`、`/health`、`/api/health`，用于免费预热，减少演示时看到 Render loading 页的概率。
- 最近一次检查会写入独立分支 `render-health-status`，包括 `render-health.json` 和 `index.html`。
- 状态不写回 `main`，避免每次健康检查都触发 Render 自动 redeploy。
- 新增说明文档 `docs/technical/RENDER_WARMUP.md`。

## 验证
- 新增回归测试 `tests/render-health-warmup-workflow.test.mjs`，检查 cron、固定网址、独立状态分支、健康端点和不推送 main。

## English Summary
- Added GitHub Actions workflow `.github/workflows/render-health-warmup.yml`.
- It requests the fixed Render URL `/`, `/health`, and `/api/health` every 10 minutes to reduce the chance of users seeing Render's loading page during demos.
- The latest check is published to the separate `render-health-status` branch as `render-health.json` and `index.html`.
- Status updates do not go back to `main`, avoiding unnecessary Render redeploys.
- Added documentation at `docs/technical/RENDER_WARMUP.md` and regression coverage in `tests/render-health-warmup-workflow.test.mjs`.

## Render Key 保存后 600519 复测 / Post-Key 600519 Retest

## 今日完成
- 用户在 Render 保存 AI key 并 redeploy 后，重新请求固定公开接口：
  `https://finance-ai-assistant-web.onrender.com/api/analysis?symbol=600519&riskProfile=balanced`
- 结果为 `HTTP 200`，完整真实 AI 已在该请求中成功输出。
- 本次接力路径为：OpenAI 主模型额度不足，Gemini/Qwen/Groq 限流或冷却，OpenRouter `openai/gpt-oss-120b` 成功输出。
- 返回结果包含上涨参考概率、下跌风险概率、分析置信度、六个因子拆解、三个情景分析和真实来源引用。

## 问题风险
- 免费/低额度 provider 仍会出现速率限制或冷却，完整 AI 目前是“可接力成功”，但还不能保证每次连续调用都成功。
- 接口返回里仍有 `historySource` 的 Mock 行情样例字段，后续应单独处理历史走势图/行情源，避免真实 AI 成功时混入样例走势说明。

## English Summary
- After the user saved Render AI keys and redeployed, retested:
  `https://finance-ai-assistant-web.onrender.com/api/analysis?symbol=600519&riskProfile=balanced`
- The endpoint returned `HTTP 200` with full real AI output for this request.
- Relay path: primary OpenAI quota failed, Gemini/Qwen/Groq were rate-limited or cooling down, and OpenRouter `openai/gpt-oss-120b` succeeded.
- Remaining risk: free/low-quota providers can still rate-limit, so full AI is relay-capable but not guaranteed on every repeated call.

## 首页主卡片同步后端分析 / Main-Card Backend Analysis Sync

## 今日完成
- 前端版本升级到 `finance-ai-assistant-v124`，`index.html` 改为加载 `app.js?v=124`。
- 首页主分析卡新增 `分析置信度` 指标和解释按钮。
- `/api/analysis` 返回后，前端现在会把 `upsideProbability`、`downsideProbability`、`sentimentScore`、`valuationScore`、`technicalScore`、`confidenceScore`、`actionReference` 写入主卡片状态。
- 修复目标问题：后端已返回 `upsideProbability: 54` / `confidenceScore: 65` 时，主卡不应继续显示 `待AI模型` / `暂无真实 AI 分析`。

## 验证
- 定向前端回归通过：`4/4`。
- PWA 资源测试通过：`5/5`。
- 语法检查通过：`app.js`、`service-worker.js`。
- `git diff --check` 通过。

## English Summary
- Bumped the frontend to `finance-ai-assistant-v124` and `app.js?v=124`.
- Added a visible `confidenceScore` metric and explanation button to the main analysis card.
- The main card now syncs backend-returned probabilities, factor scores, confidence score, and action reference after `/api/analysis` succeeds.
- Verification passed: focused frontend regressions `4/4`, PWA tests `5/5`, syntax checks, and `git diff --check`.

## 首屏样例行情闪现修复 / First-Screen Sample Flicker Fix

## 今日完成
- 前端缓存版本升级到 `finance-ai-assistant-v125`，`index.html` 加载 `app.js?v=125`。
- 项目进度递增到 `467 条自动化回归目标`。
- 后端分析加载中时，首屏统一显示 `正在请求真实数据`，并清空：
  - 交易计划；
  - 走势图 SVG；
  - 未来情景价格；
  - 因子拆解、分析依据、原因列表和风险框。
- 严格真实数据模式下，前端不再用本地股票目录自带历史价格生成走势图。
- 如果后端分析 payload 误带 `Mock 行情样例` 或 `local-sample` 情景，前端也会保持走势图和情景分析为空，不把样例价格画到首屏。

## 验证
- 首屏加载/样例防闪现定向回归通过：`9/9`。
- Render live-status 回归通过：`7/7`。
- PWA 资源测试通过：`5/5`。
- 语法检查通过：`app.js`、`backend/server.mjs`。
- `git diff --check` 通过。

## 问题风险
- 代码里仍保留本地演示数据结构，用于非严格模式和旧测试语义；当前严格真实数据模式不会在首屏加载阶段渲染这些内容。
- 线上部署后仍需检查固定网址是否已切到 `app.js?v=125`。

## English Summary
- Bumped the frontend to `finance-ai-assistant-v125` and `app.js?v=125`.
- Project progress is now `467 regression targets`.
- During backend analysis loading, the first screen now shows `正在请求真实数据` and clears trade plan, trend SVG, scenario prices, factor breakdown, analysis basis, reasons, and risk boxes.
- Strict real-data mode no longer renders trend charts from local stock-history defaults, and it ignores backend analysis payloads that still carry sample history or `local-sample` scenarios.
- Verification passed: focused anti-flicker regressions `9/9`, Render live-status `7/7`, PWA assets `5/5`, syntax checks, and `git diff --check`.

## 规则参考与完整 AI 状态拆分 / Rule Reference and Full AI Status Split

## 今日完成
- 前端缓存版本升级到 `finance-ai-assistant-v135`，`index.html` 加载 `app.js?v=135`。
- 项目进度递增到 `477 条自动化回归目标`。
- 主卡片覆盖状态从单一 `AI` 拆成：
  - `规则参考：已生成`
  - `完整 AI：未生成/已生成/可检测/待配置`
- 规则参考已经生成时，上涨/下跌/情绪/估值/技术/置信度继续显示后端返回的真实规则概率，不会因为完整 AI 未生成而回到 `待AI模型`。
- 行情状态新增缺口表达：有真实报价但没有真实历史走势时显示 `行情：已连接 / 缺历史走势`。
- 分析接口返回真实报价来源时，即使单独行情接口短暂失败，主卡片也会显示 `行情：已连接 / 缺历史走势`。
- 前端真实分析请求等待时间从 15 秒提高到 45 秒，避免规则参考约 20 多秒返回时页面提前显示 `待AI模型`。
- 自选股卡片现在直接复用当前股票规则参考结果：例如贵州茅台显示 `规则参考 54%` 和 `完整 AI 待模型`，不再显示 `上涨参考概率 待AI模型`。
- 自选股卡片复用当前分析结果时改为按股票代码匹配，避免分析结果缺少/改变 market 字段时仍停留在待模型。
- 分析渲染完成末尾会再次刷新自选股卡片，避免后续异步重绘把 `规则参考 xx%` 覆盖回待模型。
- 自选股卡片新增主卡片可见百分比兜底：当前股票主卡片已经显示 `54%` 和规则参考状态时，自选股直接显示 `规则参考 54%`。
- 可见百分比兜底改用主卡片标题确认当前股票代码，避免异步状态对象不同步时仍不能命中。
- 如果只有一只自选股，即使当前标题因本地状态滞后显示了其它股票，也允许复用主卡片可见规则百分比。
- 新闻、公告、宏观继续独立显示 `已连接` 或 `待真实数据`。

## 验证
- 规则参考主卡片定向回归通过：确认 `规则参考 已生成`、`完整 AI 未生成`、`行情 已连接 / 缺历史走势`、`新闻/公告/宏观 已连接`，且规则概率保持 `54%`、`46%`、`65/100`。
- 新增回归覆盖：分析接口带真实报价来源但单独行情接口为空时，行情状态仍显示已连接且标注缺历史走势。
- 新增回归覆盖：后端分析请求默认前端超时为 `45000ms`，慢速规则参考不会被 15 秒提前截断。
- 新增回归覆盖：自选股卡片在规则参考生成后显示 `规则参考 54%`，并明确禁止旧的 `上涨参考概率 待AI模型` / `AI 待真实模型`。
- 新增回归覆盖：当前分析结果缺少可靠 market 字段时，自选股仍按股票代码显示 `AI 参考 55% / AI 已生成`。
- 新增回归覆盖：没有内部分析状态但主卡片已经显示规则参考百分比时，加入自选后显示 `规则参考 54% / 完整 AI 待模型`。
- 新增回归覆盖：标题滞后显示 MSFT、唯一自选股是 600519 时，仍显示 `规则参考 54% / 完整 AI 待模型`。
- 定向前端回归通过：`12/12`。
- PWA + Render live-status 回归通过：`12/12`。
- 语法检查和 `git diff --check` 通过。

## English Summary
- Bumped the frontend to `finance-ai-assistant-v135` and `app.js?v=135`.
- Project progress is now `477 regression targets`.
- The main-card coverage row now separates `Rule reference` from `Full AI`.
- Rule-reference probabilities remain visible after backend return, even when full AI is not generated.
- Quote status now distinguishes connected quote data from missing historical trend data.
- Analysis-response quote sources now keep quote coverage connected even when the standalone quote endpoint is temporarily empty.
- The frontend analysis timeout is now 45 seconds, so slower rule-reference responses are less likely to be cut off into a pending-AI state.
- Watchlist cards now reuse current rule-reference analysis and show `规则参考 xx%` plus `完整 AI 待模型`.
- Watchlist cards now match current analysis by stock code, so missing or differing market metadata no longer leaves the card pending.
- Watchlist cards refresh once more after analysis rendering to avoid async redraws overwriting the metric state.
- Watchlist cards can now fall back to visible main-card metrics for the current stock.
- Verification passed: focused frontend regressions `12/12`, PWA + Render live-status `12/12`, syntax checks, and `git diff --check`.
