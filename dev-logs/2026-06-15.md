# 2026-06-15 开发日志

## 今日完成
- 成功将本地 v109 提交 `fa557cf Add fixed Render live status check` 推送到 GitHub `main`。
- 使用固定 Render 网址运行线上状态检查：`https://finance-ai-assistant-web.onrender.com`。
- 线上检查结果显示：固定网址可访问，首页、`/health`、`/api/health`、`/api/project/progress`、`/api/ai-services/provider-adapter` 和 MSFT 分析接口均返回 `200`。
- 线上仍加载 `app.js?v=108`，而本地最新已推进到 v109/v110；说明 Render 还没有部署最新 GitHub 提交。
- 线上 AI provider 状态仍为 `runtimeMode=inactive`，主模型缺少 `FINANCE_AI_MODEL_API_KEY`，三个备用模型也未配置 key。
- 将本地版本推进到 `finance-ai-assistant-v110` / `app.js?v=110`，项目进度显示为 `451 条自动化回归目标`。
- 后端 AI provider adapter、设置页摘要和固定网址检查脚本新增备用模型缺口诊断：每个 fallback 槽位会显示具体缺失的 Render Dashboard 变量和 `setupStatus`。
- 将 v110 提交 `fae70a1 Expose AI fallback key gaps` 推送到 GitHub，Render 已自动部署到 `app.js?v=110`。
- 固定 Render 网址 180 秒连续健康门禁通过：`externalUseReady=true`、`continuousHealthPassed=true`、`iterationCount=11`、`lastFailure=null`。
- 连续门禁覆盖 `/`、`/health`、`/api/health`、`/api/analysis?symbol=MSFT&riskProfile=balanced`、`/api/stocks/search?q=Microsoft`，均保持 `200`。

## 问题风险
- 固定网址稳定性已通过当前 180 秒门禁，但正式上线仍需更长时间监控和告警。
- 完整真实 AI 仍未在固定 Render 环境跑通；线上需要填写 OpenAI/Gemini/OpenRouter/Groq 对应模型密钥。
- 当前固定网址稳定性已明显优于 `lhr.life` 临时隧道，但正式上线仍需要继续验证自动部署、监控、密钥和完整 AI 输出。

## 明日/下一步待办
- 在 Render Dashboard 添加 `FINANCE_AI_MODEL_API_KEY`、`FINANCE_AI_MODEL_FALLBACK_API_KEY`、`FINANCE_AI_MODEL_FALLBACK2_API_KEY`、`FINANCE_AI_MODEL_FALLBACK3_API_KEY` 后，复测 `providerAdapter.canCallLiveModel` 和完整 AI 输出。
- 继续收敛首页普通用户视图，把详细技术诊断放入折叠区或管理员模式。

## English Summary
- Successfully pushed local v109 commit `fa557cf Add fixed Render live status check` to GitHub `main`.
- Ran the fixed Render live-status check against `https://finance-ai-assistant-web.onrender.com`.
- Live check showed the fixed URL is reachable and homepage, `/health`, `/api/health`, `/api/project/progress`, `/api/ai-services/provider-adapter`, and MSFT analysis all return `200`.
- Live Render still loads `app.js?v=108`, while local is now v109/v110; Render has not deployed the latest GitHub commit yet.
- Live AI provider status remains `runtimeMode=inactive`; the primary model is missing `FINANCE_AI_MODEL_API_KEY`, and the three fallback model keys are also not configured.
- Bumped local version to `finance-ai-assistant-v110` / `app.js?v=110`, with progress at `451 automated regression targets`.
- Backend AI provider adapter, settings summaries, and the fixed-URL checker now expose per-fallback missing Render Dashboard variables and `setupStatus`.
- Pushed v110 commit `fae70a1 Expose AI fallback key gaps` to GitHub, and Render auto-deployed `app.js?v=110`.
- The fixed Render URL passed the 180-second continuous health gate: `externalUseReady=true`, `continuousHealthPassed=true`, `iterationCount=11`, and `lastFailure=null`.
- The continuous gate covered `/`, `/health`, `/api/health`, `/api/analysis?symbol=MSFT&riskProfile=balanced`, and `/api/stocks/search?q=Microsoft`, all staying at `200`.

## v111 新闻第一屏相关性收紧

## 今日完成
- 将 PWA 缓存和脚本版本更新为 `finance-ai-assistant-v111` / `app.js?v=111`，项目进度显示为 `452 条自动化回归目标`。
- 新闻第一屏现在只展示公司直接新闻、公司公告、管理层/公开言论。
- 行业新闻、供应链/监管新闻和市场泛新闻默认折叠；如果没有公司直接新闻，第一屏显示明确摘要，而不是把泛市场新闻当作个股重点新闻展示。
- 针对 MSFT 场景，Ryanair、Cadence、Meta、Nvidia 这类非 Microsoft 直接标题不会出现在第一屏。

## 问题风险
- 新闻链路仍依赖公开 RSS/API 标题质量；完整新闻授权和更高精度实体识别仍是后续生产任务。
- 如果真实 provider 只返回市场泛新闻，页面会显示摘要加折叠详情，首屏新闻数量可能少于 3 条，这是为了避免误导。

## 下一步
- 继续优化新闻实体识别：产品、供应链、监管、管理层言论要有更细的权重。
- 继续收敛首页普通用户视图，把开发诊断留在折叠区或设置页。

## English Summary
- v111 updates the cache/script version to `finance-ai-assistant-v111` / `app.js?v=111`, with progress at `452 automated regression targets`.
- The news first screen now only shows company-direct news, filings, and management/public statements.
- Industry, supply-chain/regulatory, and broad market news are folded by default. If no company-direct news exists, the first screen shows a clear summary instead of presenting background market news as stock-specific key news.
- In the MSFT case, non-Microsoft direct headlines such as Ryanair, Cadence, Meta, and Nvidia stay out of the first screen.

## 固定 Render 网址复核

## 今日完成
- 用户确认固定公开网址：`https://finance-ai-assistant-web.onrender.com`。
- 使用固定网址线上状态检查脚本复核，线上版本为 `app.js?v=111`，与当前项目版本一致。
- 首页、`/health`、`/api/health`、`/api/project/progress`、`/api/ai-services/provider-adapter`、`/api/analysis?symbol=MSFT&riskProfile=balanced` 均返回 `200`。
- MSFT 分析接口可返回真实数据规则参考，当前 `analysisMode=real-data-rule-reference`。

## 问题风险
- 完整真实 AI 仍未启用，线上 `runtimeMode=inactive`，`canCallLiveModel=false`。
- Render Dashboard 仍缺少 `FINANCE_AI_MODEL_API_KEY`、`FINANCE_AI_MODEL_FALLBACK_API_KEY`、`FINANCE_AI_MODEL_FALLBACK2_API_KEY`、`FINANCE_AI_MODEL_FALLBACK3_API_KEY`。
- 当前分析仍是“真实数据规则参考”，不是完整 AI 深度分析。

## 下一步
- 在 Render Dashboard 手动配置模型密钥后重新部署，再复测完整 AI 输出。
- 继续把普通用户首页的内部诊断信息收敛到设置页或折叠诊断区。

## English Summary
- The user confirmed the fixed public URL: `https://finance-ai-assistant-web.onrender.com`.
- Ran the fixed Render live-status checker again. The live app is now `app.js?v=111`, matching the current project version.
- Homepage, `/health`, `/api/health`, `/api/project/progress`, `/api/ai-services/provider-adapter`, and `/api/analysis?symbol=MSFT&riskProfile=balanced` all return `200`.
- The MSFT analysis endpoint can return real-data rule-reference output, with `analysisMode=real-data-rule-reference`.
- Full real AI is still not enabled: live `runtimeMode=inactive` and `canCallLiveModel=false`.
- Render Dashboard is still missing `FINANCE_AI_MODEL_API_KEY`, `FINANCE_AI_MODEL_FALLBACK_API_KEY`, `FINANCE_AI_MODEL_FALLBACK2_API_KEY`, and `FINANCE_AI_MODEL_FALLBACK3_API_KEY`.
- Next step: manually configure the model keys in Render Dashboard, redeploy, and retest full AI output.

## v112 设置页开发诊断收敛

## 今日完成
- 将 PWA 缓存和脚本版本更新为 `finance-ai-assistant-v112` / `app.js?v=112`，项目进度显示为 `453 条自动化回归目标`。
- 设置页新增默认折叠的 `展开开发者诊断详情` 区块，把数据存储、生产数据库、审计、任务运行、调度服务收纳进去。
- 普通用户打开设置页时先看到项目摘要、数据源、AI、合规、账户、提醒和持仓，不再先被数据库、审计和任务队列细节打断。
- 复核自选股卡片回归：有规则参考分析时显示 `规则参考 xx%` 和 `完整 AI 待模型`，不会只显示“待 AI 模型”。
- 本地浏览器验证 `http://127.0.0.1:4174/?refresh=public-v112-settings-collapse#settings`：页面加载 `app.js?v=112`，设置页可见，开发者诊断默认未展开，没有横向溢出。

## 问题风险
- 完整真实 AI 仍依赖 Render Dashboard 模型密钥和 provider 额度，当前线上仍只能确认规则参考输出。
- 设置页的数据源和 AI 服务状态仍有较多诊断细节，后续可继续做普通用户/开发者模式切换。

## 下一步
- 配置 Render Dashboard 模型密钥后，继续复测完整 AI 输出。
- 继续把数据源和 AI 服务状态里的详细技术码收敛到开发者诊断区。

## English Summary
- v112 updates the cache/script version to `finance-ai-assistant-v112` / `app.js?v=112`, with progress at `453 automated regression targets`.
- Settings now has a collapsed `展开开发者诊断详情` section for repository, production database, audit, job runner, and scheduler details.
- Normal users now see project summary, data source, AI, compliance, account, reminders, and portfolio first, instead of being interrupted by database/audit/job internals.
- Watchlist card regression was rechecked: when rule-reference analysis exists, cards show `规则参考 xx%` plus `完整 AI 待模型`.
- Local browser verification against `http://127.0.0.1:4174/?refresh=public-v112-settings-collapse#settings` confirmed `app.js?v=112`, Settings visible, developer diagnostics collapsed, and no horizontal overflow.
- Render auto-deployed v112 to `https://finance-ai-assistant-web.onrender.com`. Live status confirmed homepage, `/health`, `/api/health`, `/api/project/progress`, `/api/ai-services/provider-adapter`, and MSFT analysis all return `200`, with live `app.js?v=112`.
- Live browser verification confirmed the fixed URL's Settings page keeps developer diagnostics collapsed by default and has no horizontal overflow.
- Full real AI still depends on Render Dashboard model keys and provider quota; the live site can currently only be confirmed for rule-reference output.

## v113 项目进度普通用户摘要

## 今日完成
- 将 PWA 缓存和脚本版本更新为 `finance-ai-assistant-v113` / `app.js?v=113`，项目进度显示为 `454 条自动化回归目标`。
- 设置页 `项目进度` 面板现在先显示普通用户能理解的测试版状态摘要：本地 Demo 可用、固定公开网址已建立、真实数据规则参考可用、完整 AI 待模型密钥。
- 上线差距、生产门禁、来源端点、自动化回归证据和分项准备度被收纳到默认折叠的 `展开开发者进度详情` 与 `展开开发诊断`。
- 本地浏览器验证 `http://127.0.0.1:4174/?refresh=public-v113-progress-summary#settings`：页面加载 `app.js?v=113`，项目进度详情、开发诊断和开发者设置均默认折叠，没有横向溢出。

## 问题风险
- 完整真实 AI 仍依赖 Render Dashboard 模型密钥和 provider 额度。
- 数据源状态和 AI 服务状态仍有可继续收敛的技术细节。

## 下一步
- 继续把数据源和 AI 服务状态里的技术码收敛到开发者诊断区。
- 配置 Render Dashboard 模型密钥后，复测完整 AI 输出。

## English Summary
- v113 updates the cache/script version to `finance-ai-assistant-v113` / `app.js?v=113`, with progress at `454 automated regression targets`.
- The Settings `项目进度` panel now shows a user-readable test-version summary first: local demo available, fixed public URL established, real-data rule reference available, and full AI awaiting model keys.
- Launch-gap percentages, production gates, endpoint evidence, regression evidence, and readiness details are now inside collapsed `展开开发者进度详情` and `展开开发诊断` sections.
- Local browser verification against `http://127.0.0.1:4174/?refresh=public-v113-progress-summary#settings` confirmed `app.js?v=113`, collapsed project progress details, collapsed developer diagnostics, collapsed developer settings, and no horizontal overflow.
- Render auto-deployed v113 to `https://finance-ai-assistant-web.onrender.com`. Live status confirmed homepage, `/health`, `/api/health`, `/api/project/progress`, `/api/ai-services/provider-adapter`, and MSFT analysis all return `200`, with live `app.js?v=113`.
- Live browser verification confirmed the fixed URL's Settings page has the user project summary, keeps project progress details / developer diagnostics / developer settings collapsed by default, and has no horizontal overflow.
- Full real AI still depends on Render Dashboard model keys and provider quota.

## v114 AI 服务普通用户摘要

## 今日完成
- 将 PWA 缓存和脚本版本更新为 `finance-ai-assistant-v114` / `app.js?v=114`，项目进度显示为 `455 条自动化回归目标`。
- 设置页 `AI 服务状态` 现在默认只显示普通用户需要看的状态：完整 AI 暂不可用、需要模型 key、规则参考保持可用，以及本机模型 key 配置入口。
- AI provider adapter、备用槽位、模型路线、审计、成本限流、密钥管理、输出校验、来源引用、发布和监控等技术细节被收纳到默认折叠的 `展开 AI 技术诊断`。
- 本地浏览器验证 `http://127.0.0.1:4174/?refresh=public-v114-ai-copy-final#settings`：页面加载 `app.js?v=114`，AI 技术诊断默认折叠，普通用户摘要可见，默认文案不再出现 `provider 合同`，没有横向溢出。

## 问题风险
- 完整真实 AI 仍依赖 Render Dashboard 模型密钥和 provider 额度。
- 数据源状态仍有部分技术细节，后续可继续按同样方式收敛。

## 下一步
- 继续把数据源状态里的技术码收敛到折叠诊断区。
- 配置 Render Dashboard 模型密钥后，复测完整 AI 输出。

## English Summary
- v114 updates the cache/script version to `finance-ai-assistant-v114` / `app.js?v=114`, with progress at `455 automated regression targets`.
- The Settings `AI 服务状态` panel now shows only the user-facing state by default: full AI unavailable, model key required, rule reference still available, and local model-key setup.
- Provider adapter, fallback slots, model strategy, audit, cost/rate limits, secret management, output validation, source grounding, release, and monitoring details are now inside collapsed `展开 AI 技术诊断`.
- Local browser verification against `http://127.0.0.1:4174/?refresh=public-v114-ai-copy-final#settings` confirmed `app.js?v=114`, collapsed AI diagnostics, visible user summary, no `provider 合同` wording in the default view, and no horizontal overflow.
- Render auto-deployed v114 to `https://finance-ai-assistant-web.onrender.com`. Live status confirmed homepage, `/health`, `/api/health`, `/api/project/progress`, `/api/ai-services/provider-adapter`, and MSFT analysis all return `200`, with live `app.js?v=114`.
- Live browser verification confirmed the fixed URL's Settings AI service panel keeps AI technical diagnostics collapsed by default, shows the user summary, and has no horizontal overflow.
- Full real AI still depends on Render Dashboard model keys and provider quota.

## Render 固定网址与模型变量补充

## 今日完成
- 已在 Render Environment Variables 的左列补充 4 个真实 AI 模型变量名：`FINANCE_AI_MODEL_API_KEY`、`FINANCE_AI_MODEL_FALLBACK_API_KEY`、`FINANCE_AI_MODEL_FALLBACK2_API_KEY`、`FINANCE_AI_MODEL_FALLBACK3_API_KEY`；真实 key 值只保存在 Render Dashboard，不写入仓库、日志或项目文档。
- `render.yaml` 新增非密钥变量 `FINANCE_AI_STABLE_PUBLIC_URL=https://finance-ai-assistant-web.onrender.com`，让托管环境明确知道当前固定公开测试网址。
- `scripts/stable-access-gate.mjs` 与 `scripts/stable-hosting-preflight.mjs` 默认使用固定 Render 网址作为稳定验收目标；以后运行稳定门禁时，不再默认退回到临时 `lhr.life` 入口。
- 更新 `tests/stable-access-gate.test.mjs`，现在断言固定网址已配置，但仍必须通过 2-3 分钟连续健康门禁后才算稳定外部可用。
- 公开接口复测：`/health` 返回 `200`，`/api/ai-services/provider-adapter` 返回 `200` 且显示主模型和 3 个备用模型均已配置、可接力。
- MSFT 分析接口返回 `200`，但当前仍是 `real-data-rule-reference`：主模型 `gpt-5.5` 额度不足，Gemini 与 Qwen 限流，Groq Llama 调用成功但结构化输出缺少必需指标，metric repair 仍失败；系统正确保留规则参考概率而没有假装完整 AI 成功。
- 新增真实模型第二级结构修复：普通 metric repair 仍缺指标或结构时，会追加一次 `forced metric-shape repair`，强制模型返回固定 JSON 形状、6 个因子和 bull/base/bear 3 个情景；这仍然必须由真实模型返回完整 JSON，不用本地默认值冒充完整 AI。
- 已提交并推送到 GitHub `main`，提交 `916ae8d Add stable Render gate and forced AI metric repair`；Render 固定网址复测 `/health`、`/api/ai-services/provider-adapter`、MSFT 分析接口均返回 `200`。
- 推送后公开 MSFT 复测仍降级为 `real-data-rule-reference`，但失败主因变为 provider 限流：`gpt-5.5` 额度不足，Gemini/Qwen/Groq Llama 均返回额度或速率受限。因此本轮代码修复路径已就绪，但当前没有一个备用模型可成功进入完整结构化输出阶段。

## 问题风险
- 这次只保存了固定网址和模型变量名配置；完整真实 AI 是否成功，还要等 Render 重新部署后用公开接口复测真实模型输出。
- 没有把任何 API key 写入项目文件；如果 Render 里右侧 Value 填错或额度不足，页面仍会显示规则参考或模型受限状态。
- 当前实测 key 已生效，但完整 AI 仍受 provider 额度、限流和小模型结构化输出能力影响；下一轮应优先解决 Groq/OpenRouter 备用模型输出完整度或切换更强的免费/低成本模型。
- 公开环境当前所有可用 AI provider 都处于额度不足或限流状态；如果不补主模型额度或更换可用 fallback，即使结构修复代码存在，也无法生成完整 AI。

## 下一步
- 等 Render 完成新部署后，运行固定网址 live status 和 AI 分析接口复测。
- 如果完整 AI 仍失败，继续看每个 provider 的失败类型、冷却时间和输出校验原因。
- 下一轮优先：更换或新增一个当前可用、免费/低成本、支持稳定 JSON 输出的备用模型，或给主模型补额度；之后再复测完整 AI 是否能从 `real-provider` 输出。

## English Summary
- Completed the Render Environment Variables left-column names for the four real AI model keys: `FINANCE_AI_MODEL_API_KEY`, `FINANCE_AI_MODEL_FALLBACK_API_KEY`, `FINANCE_AI_MODEL_FALLBACK2_API_KEY`, and `FINANCE_AI_MODEL_FALLBACK3_API_KEY`; real key values remain only in Render Dashboard and are not stored in the repo, logs, or docs.
- Added non-secret `FINANCE_AI_STABLE_PUBLIC_URL=https://finance-ai-assistant-web.onrender.com` to `render.yaml`, so the hosted runtime knows the current fixed public test URL.
- `scripts/stable-access-gate.mjs` and `scripts/stable-hosting-preflight.mjs` now default to the fixed Render URL for stable validation instead of falling back to temporary `lhr.life` links.
- Updated `tests/stable-access-gate.test.mjs`: the fixed URL is considered configured, but stable external use still requires the 2-3 minute continuous health gate.
- Verification passed: stable access / stable hosting / Render env-template tests `8/8`, plus syntax checks for both stable-access scripts.
- Public runtime check returned `200` for `/health` and `/api/ai-services/provider-adapter`; all model keys are now visible to the backend as configured and relay-ready.
- MSFT analysis still returned `real-data-rule-reference`, not full AI: `gpt-5.5` had insufficient quota, Gemini/Qwen were rate-limited, and Groq Llama returned incomplete structured metrics after repair.
- Added a second-level real-model repair path: if normal metric repair is still incomplete, the backend now asks the model for a forced exact JSON shape with 6 factor rows and 3 scenarios. This still requires a real model response; it does not fabricate full AI locally.
- Pushed commit `916ae8d` to GitHub `main`. The fixed Render URL returned `200` for `/health`, `/api/ai-services/provider-adapter`, and MSFT analysis after the push.
- Public MSFT retest still returned `real-data-rule-reference`; the current blocker is now provider quota/rate-limit across all attempted models, including Groq Llama.

## AI 同 Key 备用模型轮换

## 今日完成
- 后端 AI provider adapter 新增 `*_ALT_IDS` 支持：同一个 fallback provider / API key 可以拆成多个模型依次尝试，不再一个 key 只绑死一个模型。
- `render.yaml` 为 Groq 备用 3 新增 `FINANCE_AI_MODEL_FALLBACK3_ALT_IDS=openai/gpt-oss-120b`。该模型 ID 来自 Groq 官方模型文档，不需要新增 key。
- `scripts/stable-hosting-preflight.mjs` 同步检查 Groq 备用模型轮换变量，避免 Render 蓝图漏配。
- 新增回归测试：主模型限流、Groq `llama-3.1-8b-instant` 限流后，会继续尝试同一个 Groq key 下的 `openai/gpt-oss-120b`，并在成功时显示完整 AI。

## 问题风险
- 如果 Groq 账户级额度整体耗尽，轮换到 `openai/gpt-oss-120b` 仍可能继续 429；但如果只是单模型或短窗口限流，这能提高完整 AI 命中率。
- Render 需要重新部署后才会读取 `FINANCE_AI_MODEL_FALLBACK3_ALT_IDS`。

## 下一步
- 推送到 GitHub，等待 Render 自动部署后复测 `/api/ai-services/provider-adapter` 是否显示 Groq 轮换模型。
- 继续复测 MSFT 分析是否能通过 `openai/gpt-oss-120b` 生成完整 AI。

## English Summary
- Added `*_ALT_IDS` support to the AI provider adapter, so one fallback provider/API key can rotate through multiple model IDs instead of being locked to a single model.
- Added `FINANCE_AI_MODEL_FALLBACK3_ALT_IDS=openai/gpt-oss-120b` to `render.yaml` for the Groq fallback. This model ID is from Groq's official model docs and does not require a new key.
- Stable-hosting preflight now checks this Groq alternate-model variable.
- Added regression coverage proving the relay tries `openai/gpt-oss-120b` after the primary and `llama-3.1-8b-instant` are rate-limited.
- Verification passed: AI provider relay / stable-hosting / Render env-template tests `13/13`, plus syntax checks.
- GitHub push `7a53596` triggered Render auto-deploy. Public provider status now lists `fallback3-alt1` / `openai/gpt-oss-120b` as configured and relay-ready.
- Public MSFT analysis now succeeds as full real AI: `serviceMode=real-provider`, `serviceModel=openai/gpt-oss-120b`, `providerRelay.used=openai/gpt-oss-120b`, `modelIssue=null`, 6 factor rows, and 3 scenario cases. The relay still records primary quota and earlier fallback limits, but the final alternate model passes validation and can show `完整 AI 分析已生成`.

## 固定 Render 3 分钟稳定门禁复核

## 今日完成
- 使用固定网址 `https://finance-ai-assistant-web.onrender.com` 运行 180 秒连续稳定门禁，检查首页、`/health`、`/api/health`、`/api/analysis?symbol=MSFT&riskProfile=balanced` 和 `/api/stocks/search?q=Microsoft`。
- 权威网络复测结果：`ok=true`、`smokeReady=true`、`externalUseReady=true`、`continuousHealthPassed=true`、`iterationCount=5`、`lastFailure=null`、`blockers=[]`。
- 门禁后再次复测 MSFT 分析接口：返回 `200`，仍为完整真实 AI，`serviceMode=real-provider`、`serviceModel=openai/gpt-oss-120b`、`modelIssue=null`、最终尝试 `完整 AI 分析已生成 / 校验通过`，并保留 6 个因子和 3 个情景。

## 问题风险
- 非提权沙盒网络曾出现 DNS 解析失败，但提权网络门禁和普通 curl 复测均证明固定 Render 网址可达；后续稳定性判断应以外部网络/Render 监控为准。
- 固定测试环境已通过当前 3 分钟门禁，但正式上线仍需要更长周期监控、告警和生产运维策略。

## 下一步
- 继续处理普通用户界面剩余项：新闻加载空状态、来源可信度说明、首页技术诊断收敛和数据更新时间展示。

## English Summary
- Ran a 180-second stable-access gate against `https://finance-ai-assistant-web.onrender.com`, covering homepage, `/health`, `/api/health`, MSFT analysis, and stock search.
- Authoritative network result: `ok=true`, `smokeReady=true`, `externalUseReady=true`, `continuousHealthPassed=true`, `iterationCount=5`, `lastFailure=null`, and no blockers.
- Post-gate MSFT analysis still returned full real AI: `serviceMode=real-provider`, `serviceModel=openai/gpt-oss-120b`, `modelIssue=null`, final attempt `完整 AI 分析已生成 / 校验通过`, with 6 factors and 3 scenarios.

## 新闻加载态与来源可信度 UX / News Loading and Source-Credibility UX

## 今日完成
- 前端版本升级到 `finance-ai-assistant-v115`，`index.html` 加载 `app.js?v=115`，项目进度递增到 `456 条自动化回归目标`。
- 新闻加载中状态改为明确显示：已请求几个来源、当前已返回几条、正在按公司名/ticker/产品词/公告/公开言论做相关性过滤。
- 新闻空结果改为完成态文案：`筛选完成：暂未发现高重要性新闻`，避免刚打开页面时把“加载中”误认为“没有新闻”。
- 低可信来源提示增加解释：低于 70 分阈值，仅作辅助参考，不直接推动结论，需要更多公告或权威来源确认。
- 补充前端回归测试 `connected backend news loading state explains requested sources and filtering progress`，并加强低可信来源断言。

## 问题风险
- 本轮主要解决普通用户可见的新闻状态说明；真实新闻 provider 的可用性、授权和更长周期监控仍按现有数据源路线继续处理。
- 本地静态预览没有接入后端 API，因此浏览器验证只确认前端 v115 和空结果/覆盖文案；真实线上数据仍需 Render 部署后复测。

## 下一步
- 推送后等待 Render 自动部署，再检查固定网址是否加载 `app.js?v=115`。
- 继续处理剩余普通用户体验项：更多数据更新时间可见性、首页/设置页诊断信息进一步收敛、规则参考与完整 AI 的视觉区分复核。

## English Summary
- Upgraded the frontend to `finance-ai-assistant-v115`; `index.html` now loads `app.js?v=115`, and project progress is `456 regression targets`.
- News loading now explicitly shows how many sources were requested, how many items have returned so far, and that relevance filtering is in progress.
- Empty news results now say `filtering completed: no high-importance news found`, preventing loading from being mistaken as no news.
- Low-credibility source warnings now explain the below-70 threshold, mark the item as auxiliary evidence, and say it should not directly drive conclusions without stronger confirmation.
- Verification passed: syntax checks for `app.js` and `service-worker.js`; frontend regression `178/178`; PWA assets `5/5`; Render live-status `3/3`; local browser loaded `app.js?v=115` and showed the completed empty-state wording.

## Render v115 线上状态一致性 / Render v115 Live-Status Consistency

## 今日完成
- 固定 Render 网址已确认加载 `app.js?v=115`。
- 修正后端 `/api/project/progress` 的完成项文案，从 `455 条自动化回归目标` 对齐到 `456 条自动化回归目标`。
- `scripts/render-live-status.mjs` 新增分析接口独立超时：普通健康接口仍默认 15 秒，`/api/analysis` 默认 30 秒，并支持 `--analysis-timeout-ms` / `FINANCE_AI_RENDER_LIVE_STATUS_ANALYSIS_TIMEOUT_MS`。
- 新增回归测试，证明即使普通接口超时窗口很短，分析 smoke 也可以使用更长窗口，避免真实 AI 偶发慢响应造成误报。
- 复测固定网址：首页、`/health`、`/api/health`、`/api/project/progress`、`/api/ai-services/provider-adapter` 和 MSFT 分析接口均返回 `200`；MSFT 分析约 2.36 秒。
- 复核“规则参考”和“完整 AI”视觉区分：聚焦前端测试通过，规则参考不会被标成完整 AI，完整 AI 成功态也不会显示规则参考标签。

## 问题风险
- 当前线上 MSFT 分析虽然返回 `200`，但处于 `real-data-rule-reference`：所有模型尝试均为 `REAL_AI_MODEL_PROVIDER_COOLDOWN_ACTIVE`，说明完整 AI 不是持续可用状态。
- 后端全量测试中提醒/通知/调度相关 5 个用例失败，本轮改动不涉及这些模块；已用聚焦项目进度测试确认本轮后端进度改动通过。

## 下一步
- 等 provider 冷却结束后，继续复测完整 AI 是否重新回到 `real-provider`。
- 继续处理剩余普通用户体验项：设置页内部诊断进一步收敛、线上完整 AI 冷却状态更清晰、正式上线级监控。

## English Summary
- Confirmed the fixed Render URL loads `app.js?v=115`.
- Aligned backend `/api/project/progress` from `455 regression targets` to `456 regression targets`.
- Added a dedicated analysis timeout to `scripts/render-live-status.mjs`: normal health endpoints remain at 15s, while `/api/analysis` defaults to 30s via `--analysis-timeout-ms` / `FINANCE_AI_RENDER_LIVE_STATUS_ANALYSIS_TIMEOUT_MS`.
- Added regression coverage proving the analysis smoke can use the longer timeout.
- Public retest returned `200` for homepage, `/health`, `/api/health`, `/api/project/progress`, `/api/ai-services/provider-adapter`, and MSFT analysis; MSFT analysis took about 2.36s.
- Current MSFT output is still `real-data-rule-reference` because all provider attempts are in `REAL_AI_MODEL_PROVIDER_COOLDOWN_ACTIVE`; full AI should not be described as continuously available during cooldown.

## AI 冷却状态摘要 / AI Cooldown Status Summary

## 今日完成
- `scripts/render-live-status.mjs` 的 `analysis` 段新增 `relayAttempts`，逐个展示模型角色、模型名、错误码、最终主因、调用状态、输出状态、校验状态、上次失败时间、建议重试时间和下一步动作。
- 新增 `analysis.cooldown` 摘要：是否处于冷却、冷却模型数量、最早重试时间、最长等待秒数、是否还有未冷却备用模型可立即尝试。
- `nextSteps` 现在会明确提示最早建议重试时间，以及当前是否还有未冷却备用模型。
- 线上固定网址复测：全部关键接口返回 `200`，`projectProgress` 已显示 `456 条自动化回归目标`；MSFT 分析返回规则参考，原因是 5 个模型尝试都处在额度/限流/HTTP 错误后的冷却记录中，最早重试时间为 `2026-06-14T16:42:18.037Z`，当前没有未冷却备用模型可立即继续检查。

## 问题风险
- 冷却摘要让失败原因更透明，但没有消除 provider 额度和速率限制本身；完整 AI 仍需等冷却结束、补额度或切换更稳定 provider 后复测。

## 下一步
- 冷却结束后再次运行固定网址 live-status，确认是否重新生成 `real-provider` 完整 AI。
- 若仍然降级规则参考，继续优化备用模型选择或模型请求格式。

## English Summary
- Added per-model `analysis.relayAttempts` to `scripts/render-live-status.mjs`, including role, model, code, final reason, call/output/validation status, last failure time, suggested retry time, and next action.
- Added `analysis.cooldown` with active status, cooldown attempt count, earliest retry time, longest wait, and whether any uncooldown fallback remains immediately available.
- `nextSteps` now includes the earliest retry time and fallback availability.
- Live fixed-URL retest returned `200` for all key endpoints; MSFT analysis is currently rule-reference because all five model attempts are cooling down after quota/rate-limit/HTTP-error results.
