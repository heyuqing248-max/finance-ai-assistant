# Dev Log 2026-06-06 / 开发日志 2026-06-06

## 今日完成 / Completed Today
- 修正项目进度快照日期：`GET /api/project/progress` 与网页设置页进度卡从 `2026-06-05` 更新为 `2026-06-06`。
- 更新真实进度：本地网页 Demo `88.7%`，面向社会正式上线 `40.7%`。
- 新增今日开发日志，保持每日开发记录连续。
- 同步更新后端、前端、本地 fallback、API 契约和测试期望。
- `GET /api/project/progress` 新增 `readiness` 分项准备度，覆盖真实数据源、真实 AI 分析、生产数据库、生产认证、合规发布和部署运维。
- 设置页项目进度卡新增分项进度条，用户可以直接看到正式上线主要卡点。
- `GET /api/project/progress` 从静态分项快照升级为 `mock-backend-computed-readiness`：分项百分比现在由后端子系统状态检查计算，并返回 `readinessEvidence`。
- 设置页分项进度条新增“计算依据”和来源接口说明，例如 `3/5` 项通过、来源 `/api/data-sources`。
- 新增 `GET /api/data-sources/vendor-readiness`，把真实数据源供应商筛选、授权、成本、缓存、来源署名和接入顺序做成可执行清单。
- 数据源 readiness 接入供应商清单后，真实数据源分项从 `24%` 提升到 `30%`，整体本地 Demo 和正式上线准备度各提升 `1.0%`。
- 新增 `GET /api/data-sources/market-data-vendor-checklist`，把行情 provider 的报价、历史走势、交易日历、延迟标签、展示授权、缓存权限、限流成本和审计字段做成验收清单。
- 设置页新增“行情验收清单”展示，包含验收范围、检查项、供应商待确认问题和下一步动作。
- 数据源 readiness 接入行情验收清单后，真实数据源分项从 `30%` 提升到 `36%`，整体本地 Demo 和正式上线准备度各再提升 `1.0%`，当前为 `89.7%` / `41.7%`。
- 新增 `GET /api/data-sources/news-filings-vendor-checklist`，把新闻/公告 provider 的标题摘要、短摘录、原文链接、保留天数、来源署名和付费墙边界做成验收清单。
- 设置页新增“新闻公告验收清单”展示，包含验收范围、检查项、供应商待确认问题和下一步动作。
- 数据源 readiness 接入新闻公告验收清单后，真实数据源分项从 `36%` 提升到 `42%`，整体本地 Demo 和正式上线准备度各再提升 `1.0%`，当前为 `90.7%` / `42.7%`。
- 新增 `GET /api/data-sources/macro-data-vendor-checklist`，把宏观 provider 的利率、汇率、通胀、政策事件、修订规则、asOf 标签、时区归一和审计字段做成验收清单。
- 设置页新增“宏观数据验收清单”展示，包含验收范围、检查项、供应商待确认问题和下一步动作。
- 数据源 readiness 接入宏观数据验收清单后，真实数据源分项从 `42%` 提升到 `48%`，整体本地 Demo 和正式上线准备度各再提升 `1.0%`，当前为 `91.7%` / `43.7%`。
- 新增 `GET /api/data-sources/public-statements-vendor-checklist`，把公开言论 provider 的身份验证、原始链接、发言人角色、平台条款、短摘录边界、人工复核队列和审计字段做成验收清单。
- 设置页新增“公开言论验收清单”展示，覆盖 CEO、公司账号、政府高层、监管账号等来源验证和人工复核问题。
- 数据源 readiness 接入公开言论验收清单后，真实数据源分项从 `48%` 提升到 `54%`，整体本地 Demo 和正式上线准备度各再提升 `1.0%`，当前为 `92.7%` / `44.7%`。
- 新闻公告适配器的公开言论身份验证策略和人工复核策略已纳入真实数据源 readiness 计算，新增 `publicStatementVerificationPolicy` 与 `publicStatementManualReviewPolicy` 两项门禁。
- 设置页 Provider 状态新增“公开言论身份验证策略”和“公开言论人工复核策略”展示，包含身份信号、阻断规则、复核触发条件和 `24h` SLA。
- 数据源 readiness 接入公开言论策略门禁后，真实数据源分项从 `54%` 提升到 `60%`，整体本地 Demo 和正式上线准备度各再提升 `1.0%`，当前为 `93.7%` / `45.7%`。
- AI provider adapter 的输出校验、引用证据和人工复核策略已纳入真实 AI 分析 readiness 计算，新增 `responseValidationPolicyDefined`、`citationEvidencePolicyDefined`、`humanReviewPolicyDefined` 三项进度门禁。
- 真实 AI 分析 readiness 从 `36%` 提升到 `45%`，整体本地 Demo 提升到 `94.7%`，正式上线准备度按后端计算公式提升到 `47.2%`。
- AI provider adapter 的审计、成本限流和密钥管理结构已纳入真实 AI 分析 readiness 计算，新增 `auditPolicyDefined`、`budgetPolicyDefined`、`secretManagementPolicyDefined` 三项进度门禁。
- 真实 AI 分析 readiness 从 `45%` 提升到 `54%`，整体本地 Demo 提升到 `95.7%`，正式上线准备度按后端计算公式提升到 `48.7%`。
- AI provider 注册表、模型 request envelope 和人工 dry-run 预检已纳入真实 AI 分析 readiness 计算，新增 `providerRegistryDefined`、`modelRequestEnvelopeDefined`、`manualPreflightApprovalDefined` 三项进度门禁。
- 真实 AI 分析 readiness 从 `54%` 提升到 `63%`，整体本地 Demo 提升到 `96.7%`，正式上线准备度按后端计算公式提升到 `50.2%`。
- 生产认证的凭证存储、会话安全和 CSRF 防护结构已纳入 auth-security readiness 计算，新增 `credentialStoragePolicyDefined`、`sessionSecurityPolicyDefined`、`csrfProtectionPolicyDefined` 三项进度门禁。
- 生产认证 readiness 从 `46%` 提升到 `61%`，整体本地 Demo 提升到 `97.7%`，正式上线准备度按后端计算公式提升到 `52.7%`。
- 生产认证的 MFA、邮箱验证和 OIDC/OAuth 回调结构已纳入 auth-security readiness 计算，新增 `mfaPolicyDefined`、`emailVerificationPolicyDefined`、`oidcCallbackPolicyDefined` 三项进度门禁。
- 生产认证 readiness 从 `61%` 提升到 `76%`，整体本地 Demo 提升到 `98.7%`，正式上线准备度按后端计算公式提升到 `55.2%`。
- 生产认证的角色授权、登录风控和账号恢复结构已纳入 auth-security readiness 计算，新增 `roleAuthorizationPolicyDefined`、`loginRiskPolicyDefined`、`accountRecoveryPolicyDefined` 三项进度门禁。
- 生产认证 readiness 从 `76%` 提升到 `91%`，整体本地 Demo 提升到 `99.7%`，正式上线准备度按后端计算公式提升到 `57.7%`。

## 验证 / Verification
- 语法检查通过：`backend/server.mjs`、`app.js`、`backend/tests/backend-api.test.mjs`、`tests/frontend-regression.test.mjs`。
- 后端回归：134/134 通过。
- 前端回归：137/137 通过。
- 全量自动化：277/277 通过。
- 本地服务 smoke 通过：`GET /api/project/progress` 返回 `updatedAt=2026-06-06`、`92.7`、`44.7` 和 `270 条自动化回归`；运行中的 `app.js` 包含 `2026-06-06`、`270 条自动化回归`、`apiProjectProgress` 和 `/api/project/progress`。
- 分项 readiness smoke 通过：`GET /api/project/progress` 返回 `data-sources`、`ai-analysis`、`production-database`、`auth-security`、`compliance-release`、`deployment-ops`；运行中的 `app.js` 和 `styles.css` 包含 `readiness-grid`、`readiness-item` 和 `compact-track`。
- computed readiness smoke 通过：`GET /api/project/progress` 返回 `source=mock-backend-computed-readiness`、`readinessEvidence.mode=computed-from-backend-status`、`readinessAverage=39.8`，前端渲染 `计算依据` 和 `来源`。
- 供应商清单 smoke 通过：`GET /api/data-sources/vendor-readiness` 返回 `real-data-vendor-readiness-checklist`、`preferredContactOrder`、`3/8` 项通过和“未签约/未接入真实 provider”免责声明。
- 行情验收清单 smoke 通过：`GET /api/data-sources/market-data-vendor-checklist` 返回 `market-data-vendor-acceptance-checklist`、`quoteContract`、`delayLabel`、`3/9` 项通过和“未接入真实行情 provider”免责声明。
- 新闻公告验收清单 smoke 通过：`GET /api/data-sources/news-filings-vendor-checklist` 返回 `news-filings-vendor-acceptance-checklist`、`headlineSummary`、`paywallBoundary`、`3/9` 项通过和“未接入真实新闻/公告 provider”免责声明。
- 宏观数据验收清单 smoke 通过：`GET /api/data-sources/macro-data-vendor-checklist` 返回 `macro-data-vendor-acceptance-checklist`、`rateIndicators`、`revisionPolicy`、`3/10` 项通过和“未接入真实宏观 provider”免责声明。
- 公开言论验收清单 smoke 通过：`GET /api/data-sources/public-statements-vendor-checklist` 返回 `public-statements-vendor-acceptance-checklist`、`verifiedIdentity`、`manualReviewQueue`、`3/10` 项通过和“未接入真实公开言论 provider”免责声明。
- 公开言论策略门禁 smoke 通过：`GET /api/data-sources/news-filings-adapter` 返回 `publicStatementVerificationPolicy`、`publicStatementManualReviewPolicy`、`highMarketImpact`、`reviewStatus`，且 provider preflight 仍保持 `dry-run-no-provider-fetch`。
- 右侧 in-app browser 渲染检查通过：设置页显示 `93.7%`、`45.7%`、`计算依据 15/17 项通过`、`公开言论身份`、`公开言论复核` 和 `271 条自动化回归`。
- AI readiness smoke 通过：`GET /api/project/progress` 返回 AI 分项 `45%`、`7/9` 项通过、`readinessAverage=42.3`；`GET /api/ai-services/provider-adapter` 返回 `responseValidationPolicy`、`citationEvidencePolicy`、`humanReviewPolicy`。
- 右侧 in-app browser 最新渲染检查通过：设置页显示 `94.7%`、`47.2%`、`计算依据 7/9 项通过`、`AI 引用证据`、`AI 人工复核` 和 `272 条自动化回归`。
- AI operational readiness smoke 通过：`GET /api/project/progress` 返回 AI 分项 `54%`、`10/12` 项通过、`readinessAverage=43.8`；`GET /api/ai-services/provider-adapter` 返回 `auditPolicy`、`budgetPolicy`、`secretManagementPolicy`。
- 右侧 in-app browser 最新渲染检查通过：设置页显示 `95.7%`、`48.7%`、`计算依据 10/12 项通过`、`审计策略`、`成本限流`、`密钥管理` 和 `273 条自动化回归`。
- AI provider preflight smoke 通过：`GET /api/project/progress` 返回 AI 分项 `63%`、`13/15` 项通过、`readinessAverage=45.3`；`GET /api/ai-services/provider-adapter` 返回 `supportedProviderIds`、`endpointContracts`、`modelCallPreflightPlan` 和 `dry-run-no-model-call`。
- 右侧 in-app browser 最新渲染检查通过：设置页显示 `96.7%`、`50.2%`、`计算依据 13/15 项通过`、`调用预检`、`dry-run-no-model-call` 和 `274 条自动化回归`。
- Auth safety readiness smoke 通过：`GET /api/project/progress` 返回生产认证分项 `61%`、`8/9` 项通过、`readinessAverage=47.8`；`GET /api/auth/provider-adapter` 返回 `credentialStoragePolicy`、`sessionSecurityPolicy` 和 `csrfProtectionPolicy`。
- 右侧 in-app browser 最新渲染检查通过：设置页显示 `97.7%`、`52.7%`、`计算依据 8/9 项通过`、`凭证存储`、`会话安全`、`CSRF` 和 `275 条自动化回归`。
- Auth MFA/email/OIDC readiness smoke 通过：`GET /api/project/progress` 返回生产认证分项 `76%`、`11/12` 项通过、`readinessAverage=50.3`；`GET /api/auth/provider-adapter` 返回 `mfaPolicy`、`emailVerificationPolicy` 和 `oidcCallbackPolicy`。
- 右侧 in-app browser 最新渲染检查通过：设置页显示 `98.7%`、`55.2%`、`计算依据 11/12 项通过`、`MFA`、`邮箱验证`、`OIDC` 和 `276 条自动化回归`。
- Auth role/risk/recovery readiness smoke 通过：`GET /api/project/progress` 返回生产认证分项 `91%`、`14/15` 项通过、`readinessAverage=52.8`；`GET /api/auth/provider-adapter` 返回 `roleAuthorizationPolicy`、`loginRiskPolicy` 和 `accountRecoveryPolicy`。
- 右侧 in-app browser 最新渲染检查通过：设置页显示 `99.7%`、`57.7%`、`计算依据 14/15 项通过`、`角色授权`、`登录风控`、`账号恢复` 和 `277 条自动化回归`。
- Auth audit/privacy readiness smoke 通过：`GET /api/project/progress` 返回生产认证分项 `100%`、`16/17` 项通过、`readinessAverage=54.3`；`GET /api/auth/provider-adapter` 返回 `auditLoggingPolicy` 和 `privacyConsentPolicy`。
- 右侧 in-app browser 最新渲染检查通过：设置页显示 `99.9%`、`59.2%`、`计算依据 16/17 项通过`、`认证审计`、`隐私同意` 和 `278 条自动化回归`。
- Compliance structure readiness smoke 通过：`GET /api/project/progress` 返回合规发布分项 `53%`、`7/9` 项通过、`readinessAverage=57.7`；`GET /api/compliance/status` 返回 `suitabilityPolicy`、`jurisdictionEnforcementPolicy`、`disclosureVersioningPolicy` 和 `licensedAdviserReviewPolicy`。
- 右侧 in-app browser 最新渲染检查通过：设置页显示 `99.9%`、`62.6%`、`计算依据 7/9 项通过` 和 `279 条自动化回归`。
- Deployment ops structure readiness smoke 通过：`GET /api/project/progress` 返回部署运维分项 `52%`、`12/14` 项通过、`readinessAverage=61.7`；`GET /api/notification-services` 和 `GET /api/scheduler/status` 返回通知回执/webhook/抑制/反弹处理，以及调度队列/运行安全/背压/worker 认证/runbook 结构。
- 右侧 in-app browser 最新渲染检查通过：设置页显示 `99.9%`、`66.6%`、`计算依据 12/14 项通过` 和 `280 条自动化回归`。
- Production database cutover structure readiness smoke 通过：`GET /api/project/progress` 返回生产数据库分项 `56%`、`21/24` 项通过、`readinessAverage=64.0`；生产仓储 adapter、只读冒烟、SQL 契约、执行/校验、双读/双写、影子写、监控、回滚、审计链和人工切换结构已纳入进度，备份恢复证据、真实 PostgreSQL 连接和运行时切换仍保持阻断。
- 右侧 in-app browser 最新渲染检查通过：设置页显示 `99.9%`、`68.9%`、生产数据库 `56%`、`计算依据 21/24 项通过` 和 `282 条自动化回归`。
- Local Demo completion gate 已按新优先级推进到 `100%`；正式上线进度继续独立显示，不把真实数据源、真实 AI、生产数据库、合规法务和部署监控伪装成完成。
- Compliance release structure readiness smoke 通过：`GET /api/project/progress` 返回合规发布分项 `69%`、`13/16` 项通过、`readinessAverage=66.7`；固定免责声明、风险确认策略、适当性执行、地区回退、披露变更控制和持牌复核升级结构已纳入进度，真实用户确认、法律复核和公开发布总门禁仍保持阻断。
- 本轮回归目标更新：backend `139/139`、frontend `139/139`、full automated regression `284/284`；设置页目标显示为本地 Demo `100%`、正式上线 `71.6%`。
- Production preflight evidence readiness 已推进：修正生产数据库备份恢复证据字段读取，`backupRestoreEvidencePlanDefined` 从错误阻断改为结构通过；新增外部通知投递 dry-run 预检和后台 worker dry-run 预检 readiness 证据。
- 正式上线准备度从 `71.6%` 提升到 `72.9%`；生产数据库分项从 `56%` 到 `58%`、部署运维从 `52%` 到 `58%`，但真实 PostgreSQL 连接、运行时切换、真实外部投递 provider 和真实后台 worker 仍保持阻断。
- 本轮回归目标更新：backend `140/140`、frontend `140/140`、full automated regression `286/286`；右侧设置页目标显示本地 Demo `100%`、正式上线 `72.9%`、生产数据库 `22/24`、部署运维 `14/16`。
- Data-source preflight evidence readiness 已推进：新增真实数据源接入 `dry-run-no-provider-fetch` 预检 envelope 和 Provider 注册表 `dry-run-no-provider-runtime` rollout 预检，不请求真实 provider、不保存 provider 原始数据、不启用 live runtime。
- 正式上线准备度从 `72.9%` 提升到 `73.9%`；真实数据源分项从 `60%` 提升到 `66%`，证据从 `15/17` 到 `17/19`。真实 integrationPlan 与 providerRegistry 仍保持 blocked，避免把预检结构误写成真实接入完成。
- 本轮回归结果更新：backend `141/141`、frontend `140/140`、full automated regression `287/287`；右侧设置页 DOM 可见验证显示本地 Demo `100%`、正式上线 `73.9%`、真实数据源 `66%`、`计算依据 17/19 项通过`、`接入预检 defined`、`注册表预检 defined` 和 `287 条自动化回归`。
- 右侧浏览器截图捕获两次超时；页面 DOM 可见验证和后端 API smoke 均通过。
- Compliance pre-release evidence readiness 已推进：新增 `legalReviewPreflightPlan` 和 `publicReleaseEvidencePackage`，以 `dry-run-no-legal-approval` 和 `dry-run-no-public-release` 方式准备法律复核证据包和公开发布证据包，不标记真实法律复核、不打开 public beta。
- 正式上线准备度从 `73.9%` 提升到 `75.2%`；合规发布分项从 `69%` 提升到 `77%`，证据从 `13/16` 到 `15/18`。真实 `riskAcknowledgement`、`legalReview`、`publicReleaseGate` 仍保持 blocked。
- 本轮回归结果更新：backend `142/142`、frontend `140/140`、full automated regression `288/288`；设置页目标显示本地 Demo `100%`、正式上线 `75.2%`、合规发布 `77%`、`计算依据 15/18 项通过`、`法律预检 defined`、`发布证据包 defined` 和 `288 条自动化回归`。
- Data-source contract/secret handoff readiness 已推进：新增 `vendorContractHandoffPackage` 与 `providerSecretQuotaRunbook`，并暴露 `GET /api/data-sources/vendor-contract-handoff`、`GET /api/data-sources/provider-secret-quota-runbook`；两者分别使用 `dry-run-no-contract-signing` 与 `dry-run-no-secret-use`，不签约、不读密钥、不请求 provider、不启用真实 runtime。
- 正式上线准备度从 `75.2%` 提升到 `76.2%`；真实数据源分项从 `66%` 提升到 `72%`，证据从 `17/19` 到 `19/21`。真实 `integrationPlan`、`providerRegistry`、真实供应商签约、密钥读取和 provider runtime 仍保持 blocked。
- 本轮回归目标更新：backend `144/144`、frontend `140/140`、full automated regression `290/290`；设置页目标显示本地 Demo `100%`、正式上线 `76.2%`、真实数据源 `72%`、`计算依据 19/21 项通过`、`合同交接包 defined`、`密钥额度手册 defined` 和 `290 条自动化回归`。
- AI model evidence readiness 已推进：新增 `modelEvaluationEvidencePackage` 与 `modelReleaseRollbackEvidencePackage`，分别使用 `dry-run-no-model-certification` 与 `dry-run-no-model-release`，不认证模型、不发布用户建议、不发布模型版本、不启用 live runtime。
- 正式上线准备度目标从 `76.2%` 提升到 `77.2%`；真实 AI 分析分项从 `63%` 提升到 `69%`，证据从 `13/15` 到 `15/17`。真实 `liveModelGate` 和 `providerRuntime` 仍保持 blocked。
- 本轮回归目标更新：backend `146/146`、frontend `140/140`、full automated regression `292/292`；设置页目标显示本地 Demo `100%`、正式上线 `77.2%`、真实 AI 分析 `69%`、`计算依据 15/17 项通过`、`评测证据包 defined`、`发布回滚证据包 defined` 和 `292 条自动化回归`。
- Deployment observability/incident readiness 已推进：新增通知 `observabilityEvidencePackage` 与调度 `incidentResponseDrillPackage`，分别使用 `dry-run-no-observability-cutover` 与 `dry-run-no-worker-incident-cutover`，不启用外部投递、不发送外部告警、不启动后台 worker、不执行生产事故演练。
- 正式上线准备度目标从 `77.2%` 提升到 `78.2%`；部署运维分项从 `58%` 提升到 `64%`，证据从 `14/16` 到 `16/18`。真实 `externalDelivery` 和 `backgroundWorkers` 仍保持 blocked。
- 本轮回归目标更新：backend `148/148`、frontend `140/140`、full automated regression `294/294`；设置页目标显示本地 Demo `100%`、正式上线 `78.2%`、部署运维 `64%`、`计算依据 16/18 项通过`、`通知可观测证据 defined`、`事故演练包 defined` 和 `294 条自动化回归`。
- 按用户最新要求优先连接真实数据源：新增 Alpha Vantage `GLOBAL_QUOTE` 行情连接器，支持 A/HK/US symbol 映射、显式网络开关、API key 脱敏、真实 quote 响应归一化和失败回退到本地 fixture。
- 新增 Alpha Vantage `NEWS_SENTIMENT` 新闻情绪连接器，支持美股 ticker 映射、显式网络开关、API key 脱敏、新闻标题/来源链接/发布时间/情绪分数归一化和失败回退到本地 fixture；A 股与港股新闻映射保持 planned/unverified。
- 正式上线准备度从 `78.2%` 提升到 `80.7%`；真实数据源分项从 `72%` 提升到 `87%`，证据从 `19/21` 到 `24/26`。默认仍不联网，只有配置 `FINANCE_AI_MARKET_DATA_PROVIDER=alpha-vantage`、`FINANCE_AI_MARKET_DATA_API_KEY`、`FINANCE_AI_MARKET_DATA_ALLOW_NETWORK=true` 或 `FINANCE_AI_NEWS_PROVIDER=alpha-vantage-news`、`FINANCE_AI_NEWS_API_KEY`、`FINANCE_AI_NEWS_ALLOW_NETWORK=true` 后才请求真实 provider。
- 本轮回归结果更新：backend `152/152`、frontend `140/140`、full automated regression `298/298`；本地 smoke 使用 Alpha Vantage 官方 demo quote endpoint 成功返回 IBM 报价字段，NEWS_SENTIMENT 请求已到达 provider 但 demo key 返回“请申请免费 API key”提示，系统正确回退到本地 fixture 并暴露 `providerError`，没有伪装成真实新闻。设置页目标显示本地 Demo `100%`、正式上线 `80.7%`、真实数据源 `87%`、`计算依据 24/26 项通过`、`Alpha Vantage configured`、`Alpha Vantage smoke defined`、`Alpha Vantage News configured`、`Alpha Vantage News smoke defined` 和 `298 条自动化回归`。
- 本轮继续优先真实数据源：新增 Alpha Vantage quote/news 凭证预检，分别暴露 `alphaVantageCredentialPreflight` 与 `alphaVantageNewsCredentialPreflight`，只显示 `missing`、`demo-key-limited` 或 `configured-redacted`，不输出、不保存、不审计真实 API key。
- 正式上线准备度从 `80.7%` 提升到 `81.7%`；真实数据源分项从 `87%` 提升到 `93%`，证据从 `24/26` 到 `26/28`。设置页新增 `Alpha Vantage 凭证预检` 和 `Alpha Vantage News 凭证预检`，帮助判断 demo key 限制、网络开关和真实 key 验证条件。
- 本轮继续按“优先连接真实数据源”推进：新增 `GET /api/data-sources/provider-setup-guide`，把行情、新闻情绪、宏观数据和公开言论 provider 的必填环境变量、smoke endpoint、smoke 顺序、人工动作、禁止审计字段和不读密钥/不写 env/不启用 live runtime 门禁做成后端合同，并在设置页显示 `配置向导`、`配置分组`、`Smoke 顺序` 和 `安全边界`。
- 正式上线准备度从 `81.7%` 提升到 `82.7%`；真实数据源分项从 `93%` 提升到 `99%`，证据从 `26/28` 到 `28/30`。本轮回归结果更新：backend `153/153`、frontend `140/140`、full automated regression `299/299`。
- 本轮继续按“优先连接真实数据源”推进：新增 SEC EDGAR `sec-company-submissions` 公司公告连接器，支持美股 ticker 到 CIK 映射、显式 `FINANCE_AI_FILINGS_ALLOW_NETWORK=true` 开关、`FINANCE_AI_SEC_USER_AGENT` 访问预检、SEC submissions 响应归一化、来源链接/公告类型/日期/ accession 保留，以及失败回退本地 fixture。
- 设置页新增 `SEC 公告连接器`、`SEC 公告 smoke` 和 `SEC 公告访问预检`；`GET /api/news/filings` 在未启用真实源时继续返回 fixture，启用后可请求 SEC 公告并在失败时返回 `providerError`，不输出原始 provider 响应或 User-Agent 细节。
- 真实计算进度更新：正式上线准备度从 `83.7%` 到 `83.9%`；真实数据源分项从 `99%` 到 `100%`，证据从 `28/30` 到 `30/32`，但整体仍保持 blocked，因为 A 股/港股公告、完整授权、宏观数据和生产运行时仍未完成。同步 AI 模型配置向导后真实 AI 分析为 `75%`、证据 `17/19`。
- 本轮回归结果更新：backend `156/156`、frontend `140/140`、full automated regression `302/302`。

## 问题与风险 / Issues and Risks
- 当前进度仍是项目管理参考，不代表真实生产可上线；真实数据源、真实 AI、生产数据库、合规法务审核和部署监控仍是主要阻断。
- 右侧预览若缓存旧脚本，需要手动 `Cmd + Shift + R` 强制刷新。

## 明日待办 / Next Todo
- 继续优先处理真实上线阻断项：生产数据库结构已纳入进度，下一步应推进备份恢复证据验证、真实 PostgreSQL 连接预检，或真实外部 provider 配置向导。
- 后续可继续推进数据库持久化、真实数据源供应商接入和真实 AI provider 安全配置向导。

## 决策记录 / Decisions
- 每天开始开发时优先检查 `PROJECT_MEMORY.md`、当日 dev log 是否存在，以及项目进度接口日期是否与当前日期一致。
- 保持两个进度口径：本地网页 Demo 完成度与面向社会正式上线准备度。
