# 2026-06-13 开发日志

## 今日完成
- 延续 v40 AI 主备模型接力，确认 OpenAI 主模型和 Gemini fallback 运行时文件均存在。
- 验证后端 `/api/ai-services/provider-adapter`：OpenAI 主模型 `gpt-5.5` 使用 `responses`，Gemini fallback `gemini-2.5-flash` 已 `configured=true` 且 `canCallLiveModel=true`。
- 谨慎重试一次 `/api/analysis?symbol=MSFT&riskProfile=balanced`，确认链路会尝试 `gpt-5.5 -> gemini-2.5-flash`。
- 新增 AI 模型 10 分钟本地冷却：遇到 quota/rate-limit/429 后，页面不会持续请求模型，会显示冷却剩余时间。
- AI 空状态新增错误码和主备模型接力路径，方便区分“未接入”和“provider 限流/额度”。
- PWA 缓存版本升级到 `finance-ai-assistant-v41`。
- 修复 4174 本地网页不会自动连接后端的问题：本地开发端口白名单从仅 `4173` 扩展到 `4173-4190`。
- 前端 AI 状态清洗保留 `fallbackModelProvider`，页面可正确显示 Gemini 备用模型 `configured=true`、`canCallLiveModel=true`、`可接力`。
- HTML 脚本引用升级为 `app.js?v=42`，配合 PWA 缓存版本 `finance-ai-assistant-v42`，避免外部浏览器继续加载旧脚本。
- 项目进度更新到 `359 条自动化回归目标`。
- 数据源设置面板移除“改用本机样例”文案，后端断开入口改为“断开后端并保持空白”。
- 后端连接失败和未连接状态统一为严格真实数据空白模式，避免用户误以为还能使用样例数据。
- PWA 缓存和 HTML 脚本版本升级到 `finance-ai-assistant-v43` / `app.js?v=43`。
- 项目进度更新到 `360 条自动化回归目标`。
- v45 已取消 AI 全局冷却阻断：quota/rate-limit/429 只显示提示型状态，`重新检测 AI` 会继续请求后端并检查已配置备用模型。
- 设置页新增 OpenRouter/Groq 免费备用模型入口，和 Gemini 一样写入本机 fallback 运行时；免费 provider 额度以各 provider 控制台为准，不使用 mock/sample 分析兜底。
- PWA 缓存和 HTML 脚本版本升级到 `finance-ai-assistant-v45` / `app.js?v=45`。
- 项目进度更新到 `362 条自动化回归目标`。
- v46 将 AI 接力扩展为主模型 + 三个备用槽位：Gemini 写入 `fallback`，OpenRouter 写入 `fallback2`，Groq 写入 `fallback3`，后端按顺序继续尝试。
- PWA 缓存和 HTML 脚本版本升级到 `finance-ai-assistant-v46` / `app.js?v=46`。
- 项目进度更新到 `363 条自动化回归目标`。
- v47 新增 HKEXnews 港股公告 provider：港股 `market=hk` 会走 HKEX 公开公告检索，按 `STOCK_CODE` 严格二次校验，只展示标题、发布时间和原始 PDF 链接，不抓取 PDF 正文。
- v47 增加 HKEX 股票 autocomplete 自动查询：先调用 `prefix.do` 获取内部 `stockId`，再调用 `titleSearchServlet.do` 拉公告；腾讯 `00700` 已验证 `stockId=7609`。
- 本地真实数据启动脚本的公告 provider 默认切换为 `multi-free-filings`：A 股走上交所，港股走 HKEXnews，美股走 SEC EDGAR。
- v48 处理分析指标空白显示：上涨参考概率、下跌风险概率、市场情绪、估值吸引力、技术面强弱在真实 AI 未生成时显示 `待模型`，请求中显示 `生成中`，不再显示裸 `--` 或初始样例概率。
- 上涨参考概率和下跌风险概率补充 `?` 解释按钮；保留市场情绪、估值吸引力、技术面强弱的解释按钮。
- PWA 缓存和 HTML 脚本版本升级到 `finance-ai-assistant-v48` / `app.js?v=48`。
- v49 修复操作参考残留样例文案：初始网页壳显示 `等待真实 AI 模型生成。`，后端真实 AI 加载中显示 `正在等待真实 AI 模型生成。`，真实 AI 失败/未配置显示 `暂无真实 AI 分析。`。
- PWA 缓存和 HTML 脚本版本升级到 `finance-ai-assistant-v49` / `app.js?v=49`。
- v50 新增前端 AI 分析等待上限：后端真实模型长时间未返回时，页面退出 `生成中`，显示 `真实 AI 请求超时` 和严格空白状态，不使用样例建议兜底。
- PWA 缓存和 HTML 脚本版本升级到 `finance-ai-assistant-v50` / `app.js?v=50`。
- v51 修复持仓摘要样例盈亏：严格真实数据模式下，没有真实当前价时不显示 `样例当前价` 或 `样例浮动收益率`；只有真实行情 quote 覆盖后才计算浮动收益率。
- PWA 缓存和 HTML 脚本版本升级到 `finance-ai-assistant-v51` / `app.js?v=51`。
- v52 修复自选股卡片样例概率：自选股列表不再显示本地样例 `上涨参考概率 xx%` 或 `偏利好/偏谨慎` 标签，改为 `上涨参考概率 待模型` 和 `AI 待真实模型`。
- PWA 缓存和 HTML 脚本版本升级到 `finance-ai-assistant-v52` / `app.js?v=52`。
- v53 修复正式上线进度过度乐观：只要仍有生产门禁 blocked 分项，`面向社会正式上线` 最高显示 `80%`，不再显示 `99.9%`。
- PWA 缓存和 HTML 脚本版本升级到 `finance-ai-assistant-v53` / `app.js?v=53`。
- 项目进度更新到 `379 条自动化回归目标`。
- v54 修复分析指标解释按钮误读：上涨参考概率、下跌风险概率、市场情绪、估值吸引力、技术面强弱旁的解释按钮从可见 `?` 改为信息按钮 `i`，并补充 `aria-label/title`，避免用户把按钮误认为数据未知。
- v54 收紧进度口径：blocked 分项准备度最高显示 `80%`，原始计算值保留在 `rawPercent` 证据里；当前正式上线进度精确为 `74.5%`。
- PWA 缓存和 HTML 脚本版本升级到 `finance-ai-assistant-v54` / `app.js?v=54`。
- 项目进度更新到 `381 条自动化回归目标`。
- v55 清理前端可见样例文案：市场切换提示从 `样例市场` 改为普通市场提示；新闻情报保存提示不再写 `后端样例仓储` 或 `本机样例仍可继续使用`；操作边界价格标签从 `样例现价` 改为 `当前参考价`。
- v55 严格真实 AI tradePlan 规则：后端真实 AI 响应没有返回 `tradePlan` 时，前端隐藏操作边界，不再用本地规则生成操作边界兜底。
- PWA 缓存和 HTML 脚本版本升级到 `finance-ai-assistant-v55` / `app.js?v=55`。
- 项目进度更新到 `383 条自动化回归目标`。
- v56 清理调度/队列设置面板的本机样例操作文案：worker 心跳和队列任务按钮改为“演练”语义，避免把本地手动检查误读为生产后台 worker。
- v57 处理 AI 分析五个核心指标：上涨参考概率、下跌风险概率、市场情绪、估值吸引力、技术面强弱统一显示 `待AI模型` / `生成中` / 真实模型值；不再显示裸 `--`，也不把解释按钮显示成 `?`。
- v57 新增真实返回字段覆盖规则：后端 AI 只返回部分指标时，缺失项保持 `待AI模型`，不会用本地默认分数或样例概率补齐。
- v57 初始网页壳把上涨概率圆环从 `--score: 64` 改为 `--score: 0`，徽标从 `偏利好` 改为 `等待AI`，避免真实 AI 未返回前暗示已有分析结论。
- PWA 缓存和 HTML 脚本版本升级到 `finance-ai-assistant-v57` / `app.js?v=57`。
- 项目进度更新到 `386 条自动化回归目标`。
- v58 清理宽前端/PWA 回归旧口径：把仍期待本机样例新闻、样例走势、样例 AI 恢复、旧默认贵州茅台路径和 fixture 行情的断言，改为严格真实数据空白、MSFT 默认真实数据路径和 real-provider 口径。
- v58 修复可见重试提示：自选股重试不再写“样例数据”，AI 重试不再写“重新生成 AI 分析样例”，均改为严格真实数据语义。
- v58 AI 设置页补回运行时路径提示：主模型 key 显示保存到 `/private/tmp/finance_ai_model_key`，备用 key 显示保存到 `/private/tmp/finance_ai_model_fallback*`，只显示路径不显示密钥。
- PWA 缓存和 HTML 脚本版本升级到 `finance-ai-assistant-v58` / `app.js?v=58`。
- 项目进度更新到 `387 条自动化回归目标`。
- v59 强化真实 AI 核心量化指标校验：真实模型必须返回上涨参考概率、下跌风险概率、市场情绪、估值吸引力、技术面强弱五个核心字段；缺失时后端返回 `REAL_AI_MODEL_MISSING_METRICS`，页面保持 `待AI模型`，不使用本地默认值补齐。
- v59 同步 Responses JSON schema 必填字段，避免模型接口层允许缺少情绪、估值或技术面分数的响应通过。
- PWA 缓存和 HTML 脚本版本升级到 `finance-ai-assistant-v59` / `app.js?v=59`。
- 项目进度更新到 `388 条自动化回归目标`。

## 测试记录
- 语法检查通过：`app.js`、`backend/services/ai-provider-adapter.mjs`。
- 前端/PWA 目标回归通过 `5/5`。
- 后端 AI 目标回归通过 `2/2`。
- v41 临时服务已启动：网页 `http://127.0.0.1:4174`，后端 `http://localhost:4181`。
- v41 后端状态验证：本地 Demo `100%`、公开上线 `99.9%`、AI readiness `99%`、真实数据 readiness `99%`。
- v42/v43 浏览器验证通过：`http://127.0.0.1:4174/?refresh=20260613-v43-cache-bust-ai#settings` 显示 `后端 API 已连接`、`真实 AI 模型已连接`、`备用模型 已配置 · gemini-2.5-flash · chat-completions · 可接力`，且页面未泄露 OpenAI/Gemini key。
- v43 严格空白数据源验证通过：`http://127.0.0.1:4174/?refresh=20260613-v43-strict-blank-source#settings` 显示 `后端 API 已连接`、`断开后端并保持空白`、`真实 AI 模型已连接`、`360 条自动化回归目标`，且不再出现 `改用本机样例` 或 `当前使用本机样例数据`。
- v45 目标回归通过：`app.js` / `backend/server.mjs` 语法检查通过，前端/PWA `AI quota advisory|connected AI settings|project progress|PWA` 通过 `6/6`，后端 `project progress endpoint` 通过 `1/1`。
- v46 新增回归覆盖：本机 AI 配置接口支持 `fallback2` 且不回显 key；AI adapter 可从 OpenAI 主模型继续接力到 Gemini，再到 OpenRouter。
- v47 新增回归覆盖：HKEX 公司公告 payload 可解析为真实公告；港股公告请求会先走 HKEXnews 且不会误打 SEC。
- v47 新增回归覆盖：HKEX 股票 autocomplete JSONP 可解析；未映射港股会先查 `prefix.do` 再查公告。
- 右侧浏览器验证通过：点击 `腾讯控股` 后显示 `公告 已连接`，新闻区出现 8 条 HKEXnews 公司公告，标题包括 `Next Day Disclosure Return`，授权标签为 `官方公告来源`。
- v48 目标回归通过 `8/8`：初始网页壳不硬编码样例概率；真实 AI 空状态指标显示 `待模型`；真实 AI 加载中指标显示 `生成中`；PWA/cache/progress 检查通过。
- v49 新增回归覆盖：初始网页壳、真实 AI 加载中、真实 AI 未配置状态均不显示任何操作建议样例文案。
- v49 目标回归通过 `14/14`：`app.js`、`backend/server.mjs` 语法检查通过；初始网页壳、真实 AI 加载中、真实 AI 未配置、风险偏好/持仓不伪造模型概率、PWA/cache/progress/service worker 检查通过。
- v49 右侧浏览器验证通过：`http://127.0.0.1:4174/?refresh=20260613-v49-action-placeholder#overview` 加载 `app.js?v=49`，五个分析指标显示 `待模型`，`操作参考` 显示 `暂无真实 AI 分析。`，页面未出现旧样例建议文案。
- v50 目标回归通过 `13/13`：`app.js`、`backend/server.mjs` 语法检查通过；真实 AI 前端超时会退出加载态并保持严格空白；AI 加载、AI 未配置、PWA/cache/progress/service worker 检查通过。
- v51 目标回归通过 `15/15`：`app.js` 语法检查通过；持仓摘要不再显示样例当前价/样例浮动收益率；只有真实行情 quote 覆盖后才计算浮动收益率；PWA/cache/progress 检查通过。
- v51 右侧浏览器验证通过：`http://127.0.0.1:4174/?refresh=20260613-v51-portfolio-real-quote#overview` 加载 `app.js?v=51`，进度显示 `375 条自动化回归目标`；填写买入价 `100`、数量 `5` 后，持仓摘要显示真实当前价与浮动收益率，未出现 `样例当前价` 或 `样例浮动收益率`。
- v52 目标回归通过 `19/19`：`app.js`、`backend/server.mjs` 语法检查通过；自选股新增、移除、选择、后端同步和保存状态均不显示本地样例概率/利好标签；PWA/cache/progress 检查通过。
- v52 右侧浏览器验证通过：`http://127.0.0.1:4174/?refresh=20260613-v52-watchlist-empty-probability#overview` 加载 `app.js?v=52`，进度显示 `377 条自动化回归目标`；加入 Microsoft 自选后卡片显示 `上涨参考概率 待模型`、`AI 待真实模型`，未出现样例百分比或 `偏利好/偏谨慎`。
- v53 目标回归通过 `27/27`：`app.js`、`backend/server.mjs` 语法检查通过；后端 `/api/project/progress` 与前端进度面板均显示正式上线 blocked 时封顶 `80%`；生产数据库、合规、部署运维证据检查通过；PWA/cache 检查通过。
- v53 右侧浏览器验证通过：`http://127.0.0.1:4174/?refresh=20260613-v53-honest-launch-progress#settings` 加载 `app.js?v=53`，进度显示 `379 条自动化回归目标`，`面向社会正式上线 80%`，未出现 `面向社会正式上线 99.9%`。
- v54 目标回归通过 `27/27`：`app.js`、`backend/server.mjs` 语法检查通过；术语解释按钮显示为 `i` 且有明确无障碍标签；初始网页壳不显示样例概率；后端 blocked readiness 分项封顶 `80%`；正式上线进度显示 `74.5%`；历史行情无真实 provider 时保持空白且不恢复 fixture 点位。
- v54 右侧浏览器验证通过：`http://127.0.0.1:4174/?refresh=20260613-v54-metric-info-cap#overview` 加载 `app.js?v=54`；上涨参考概率、下跌风险概率、市场情绪、估值吸引力、技术面强弱均显示 `待模型`；五个解释按钮均显示 `i` 并带 `aria-label/title`；未出现指标 `--` 或可见 `?`；项目进度显示 `381 条自动化回归目标`、`面向社会正式上线 74.5%`、数据源/AI/认证分项 `80%`。
- v55 目标回归通过 `27/27`：`app.js`、`backend/server.mjs` 语法检查通过；市场切换、tradePlan、项目进度、PWA、初始网页壳和术语按钮回归均通过。
- v55 右侧浏览器验证通过：`http://127.0.0.1:4174/?refresh=20260613-v55-no-sample-wording#overview` 加载 `app.js?v=55`；项目进度显示 `383 条自动化回归目标`、`面向社会正式上线 74.5%`；五个分析指标仍为 `待模型`；操作边界隐藏；页面没有 `样例现价`、`样例市场`、`后端样例仓储`、`本机样例仍可继续使用`。
- v56 目标回归通过 `18/18`：调度 worker/队列、项目进度、PWA、严格空白相关测试通过；另一个更宽的调度测试组合暴露 3 个既有 reminder job runner 断言失败，和本轮文案改动无关。
- v57 目标回归通过 `10/10`：`app.js`、`backend/server.mjs` 语法检查通过；初始网页壳、部分 AI 指标返回、AI 加载、AI 超时、风险偏好/持仓不伪造模型概率、项目进度和 PWA 检查通过。
- v57 右侧浏览器验证通过：`http://127.0.0.1:4174/?refresh=20260613-v57-ai-metric-placeholders#overview` 加载 `app.js?v=57`；AI quota/rate-limit 后五个指标均显示 `待AI模型`，圆环 `--score=0`，五个解释按钮均显示 `i`，未出现 `--` 或可见 `?`。
- v57 宽前端/PWA 测试运行 `164` 项，`151` 通过、`13` 失败；失败集中在旧断言仍期待本机样例新闻/走势、默认贵州茅台或旧空状态，和当前严格真实数据/刷新默认 MSFT 口径冲突，本轮目标指标回归均已通过。
- v58 宽前端/PWA 回归通过 `164/164`；`app.js`、`backend/server.mjs` 语法检查通过。
- v58 右侧浏览器验证通过：`http://127.0.0.1:4174/?refresh=20260613-v58-regression-green#settings` 加载 `app.js?v=58`，显示 `387 条自动化回归目标`、`面向社会正式上线 74.5%`、`/private/tmp/finance_ai_model_key` 和 `/private/tmp/finance_ai_model_fallback*`，指标没有 `--` 或可见 `?`。
- v59 语法检查通过：`app.js`、`backend/server.mjs`、`backend/services/ai-provider-adapter.mjs`。
- v59 前端/PWA 全量回归通过 `164/164`。
- v59 后端 AI 相关回归通过 `19/19`，包含缺失核心量化指标拒绝展示、AI 历史保存、持仓联动、模型 provider 接力等。
- v59 右侧浏览器验证通过：`http://127.0.0.1:4174/?refresh=20260613-v59-core-metrics#overview` 加载 `app.js?v=59`；五个指标均为 `待AI模型`，解释按钮均为 `i`，没有 `--` 或可见 `?`。
- 完整后端测试仍有 `21` 个既有失败，集中在旧测试期待 sample/fixture 数据、旧 reminder/notification 样例触发和旧 sample 口径；这些与当前严格真实数据空白策略冲突，不是 v59 核心指标改动引入。

## 问题风险
- 完整 AI 分析仍未达到 `hasAnalysis=true`；本轮真实重试返回 provider 429，说明 Gemini fallback 仍处于额度/速率限制或冷却状态。
- 当前不应继续密集请求模型，否则可能延长免费层限流窗口。
- 旧 `4173/4180` 服务仍可能显示旧页面；当前验证新版本使用 `4174/4181`。
- OpenAI 主模型仍可能因账户额度不足进入冷却；Gemini 备用模型已配置，但完整 AI 分析仍可能受 provider 429/速率限制影响。
- 仓储、任务、调度、通知等后端模块仍有样例/Mock 文案；本轮只完成数据源和 AI 入口的严格真实数据语义，后续继续逐块替换。
- 免费 AI provider 没有无限额度；v45 解决的是“不全局锁死”和“快速切换备用 provider”，不是绕过 provider 限额。
- fallback2/fallback3 需要用户分别提供 OpenRouter/Groq key；没有 key 时槽位会保持未配置，不会伪造模型输出。
- HKEXnews 搜索接口使用内部 `stockId`，不是股票代码；运行时必须按返回的 `STOCK_CODE` 二次校验，错配结果必须丢弃。常见港股内部编号映射仍需继续通过公开接口扩展验证。
- 指标旁的 `i` 是术语解释按钮，不是数据值；真实 AI 未成功返回前，指标值必须保持 `待模型` 或 `生成中`，不能显示 `--`、`?`、样例概率或规则估算概率。
- 仍需继续扫描其它设置/后台模块的样例/Mock 可见文案，尤其是审计、通知、任务、调度、认证管理等后端状态面板；本轮只处理了市场切换、新闻保存提示和操作边界。
- 从 v57 起，指标空状态文案统一为 `待AI模型`；旧测试或文档中若仍写 `待模型`，需要按新 UI 语义更新，避免外部浏览器误以为只是普通数据加载延迟。
- 宽前端/PWA 测试仍有旧样例口径断言，需要单独清理；不要为了让旧测试通过而恢复样例新闻、样例走势或默认样例股票。
- v58 已清理宽前端/PWA 旧样例口径断言；后续如果新增测试，应默认严格真实数据空白，不应再期待本机样例新闻/走势/AI 建议恢复。
- 后端完整测试仍保留若干旧 sample/fixture 断言；后续要逐步迁移到严格真实数据空白或真实 provider 口径，不能为了测试通过恢复样例数据。

## 明日待办
- 冷却后只重试一次完整 AI 分析，目标是 `hasAnalysis=true`，并确认网页显示模型参考概率、理由、风险和免责声明。
- 若仍被 429 阻断，优先补充其它免费 provider key 或切换 fallback provider；不再恢复全局冷却禁用态。
- 用真实 OpenRouter/Groq key 各做一次最小 smoke，确认多槽位接力在本机真实运行时可用。
- 后续把 v41 稳定版切回 `4173/4180`。
- 下一轮优先在冷却结束后验证一次完整 `hasAnalysis=true`，并将 AI 连接状态文案继续区分“模型已配置”和“本次分析已生成”。
- 继续验证阿里巴巴、美团、京东、汇丰等港股公告卡片；腾讯控股已经通过自动 stockId 查询和右侧浏览器验证。
- 下一轮优先修复宽测试中的旧断言，把它们改为严格真实数据空白模式、刷新默认 MSFT 和真实数据 provider 口径。
- 下一轮优先继续清理真实 UI 中剩余的 `mock`/`样例用户`/`本机样例` 后台状态文案，尤其是账户、权限、审计、通知和 provider 详情面板。
- 下一轮清理后端完整测试里的旧 sample/fixture 期望，让后端回归也和“无真实数据就空白”的产品决策一致。

## 决策记录
- 本轮不继续密集请求 Gemini；优先保护免费层额度和用户体验。
- 严格真实数据策略保持不变：模型失败时保持空白，不展示样例 AI 分析。
- v45 决策：AI 限流只做提示和来源记录，不再阻止用户手动重试或检查其它已配置免费备用模型。
- v46 决策：保留严格真实模型空白策略，但允许多个免费 provider key 同时存在并按顺序接力。
- v47 决策：港股公告优先用 HKEXnews 官方公开检索；若内部编号未映射或 HKEX 返回错配，不使用新闻或样例公告冒充官方公告。
- v54 决策：正式上线进度不再把“封顶 80%”当作固定显示值；先封顶 blocked 分项，再按平均值展示当前真实进度，因此当前值为 `74.5%`。
- v55 决策：真实 AI 输出缺失结构化 `tradePlan` 时宁可隐藏操作边界，也不使用本地规则补出看似专业的买卖边界。
- v57 决策：指标缺失时宁可显示 `待AI模型`，不从本地默认股票数据推导或补齐；真实模型未返回的指标不能被包装成专业概率。
- v58 决策：测试体系也必须服务严格真实数据目标；不能为了测试通过恢复样例数据，应该把旧测试迁移到真实空白、真实 provider 或明确演练语义。
- v59 决策：AI 核心量化指标不允许用默认值补齐；如果真实模型缺少上涨、下跌、情绪、估值或技术面任一字段，宁可返回缺失指标错误并保持 UI 空白。
- v60 决策：为满足“让概率可用”，允许在真实 AI 不可用或额度受限时返回 `真实数据规则参考` 概率，但只允许使用真实 provider 输入：真实行情/历史、真实新闻/公告/社交公开言论、真实宏观数据。没有真实输入仍保持空白；不得使用 mock/sample/fixture/catalog-only 数据伪造概率。

## v60 追加记录：概率可用

## 今日完成
- 后端 `/api/analysis` 新增 `real-data-rule-reference` 路径：真实 AI 不可用时，如果已有真实行情、历史走势、新闻/公告/公开言论或宏观输入，可以生成上涨参考概率、下跌风险概率、市场情绪、估值吸引力、技术面强弱和操作参考。
- 保留严格空白底线：没有真实输入时仍返回空白/错误，不使用样例、fixture、mock 或目录默认数据补概率。
- 前端/PWA 版本升级到 `finance-ai-assistant-v60`，`index.html` 加载 `app.js?v=60`，项目进度更新为 `389 条自动化回归目标`。
- 右侧浏览器验证通过：`http://127.0.0.1:4174/?refresh=20260613-v60-probability-usable#overview` 显示 `Microsoft · MSFT`，五项指标分别为 `48%`、`52%`、`50/100`、`56/100`、`29/100`，并显示 `真实数据规则参考：保持观察，等待更多真实数据确认方向。`
- 后端旧测试断言清理：不再期待空新闻也保存 1 条记录，不再要求样例新闻覆盖或样例宏观文案。

## 测试记录
- 语法检查通过：`backend/server.mjs`、`app.js`。
- 后端真实数据/新闻/分析目标回归通过 `17/17`。
- 前端/PWA 全量回归通过 `164/164`。
- AI 相关回归通过 `18/18`。

## 问题风险
- v60 概率是“真实数据规则参考”，不是完整 AI 深度分析，也不是收益保证；后续仍需要真实模型稳定可用后进行交叉校验。
- 免费 AI provider 仍可能限流；v60 解决的是限流时页面不再全空，但不能替代专业合规审查。
- 规则概率依赖当前能拿到的公开真实数据，新闻、公告或宏观源缺失时置信度会下降。

## 明日待办
- 在 UI 上继续强化 `真实数据规则参考` 标签和风险提示，避免用户误以为是确定性买卖信号。
- 等免费模型冷却后，再跑一次真实 AI 输出，与 v60 规则概率做对比校验。
- 继续补港股/A 股公告和新闻 provider 的稳定性，提升概率输入质量。

## v61 追加记录：规则参考标签修正

## 今日完成
- 修复 v60 可见误导点：后端返回 `real-data-rule-reference` 时，股票覆盖标签不再显示 `AI 已生成`，改为 `AI 规则参考`。
- 前端 `normalizeAnalysisPayload()` 保留 `analysisMode` 和 `analysisService`，让 UI 能区分完整真实 AI 模型输出与真实数据规则参考。
- 股票覆盖摘要在规则参考模式下显示 `当前概率为真实数据规则参考，未连接项保持空白。`
- PWA/cache/script 版本升级到 `finance-ai-assistant-v61`，`index.html` 加载 `app.js?v=61`。
- 项目进度更新到 `390 条自动化回归目标`。

## 测试记录
- 语法检查通过：`app.js`、`backend/server.mjs`。
- 前端/PWA 全量回归通过 `165/165`。
- 后端/API 重点回归通过 `16/16`。
- 新增回归 `real-data rule reference analysis is not labeled as full AI generation` 通过，确保规则参考不会被标成完整 AI 生成。
- 右侧浏览器验证通过：`http://127.0.0.1:4174/?refresh=20260613-v61-rule-reference-label#overview` 加载 `app.js?v=61`，最终显示 `AI 规则参考`，五项指标为 `48%`、`52%`、`50/100`、`56/100`、`29/100`，且未显示 `AI 已生成`。

## 问题风险
- 规则参考概率仍不是完整真实 AI 模型输出；必须继续保留免责声明和来源说明。
- 免费 AI 模型仍可能限流；规则参考只能作为本地 Demo 的可用性兜底，正式上线仍需稳定模型和合规审查。

## 明日待办
- 在分析结果区域增加更显眼的 `规则参考` 小标签或说明，进一步减少误解。
- 冷却后继续验证完整真实 AI 输出，并让完整 AI 输出与规则参考在 UI 上有清晰差异。

## v64 追加记录：公开网址预览

## 今日完成
- 新增 `scripts/public-preview-server.mjs`，让网页静态资源、`/health` 和 `/api/*` 从同一个本机预览端口提供，解决公网域名访问时继续请求 `localhost` 的问题。
- 前端公开域名自动连接逻辑已调整：非本地/非内网的公开 host 会使用同源 `/api/*`，不会把用户浏览器导向本机 `localhost:4180/4181`。
- 创建公开预览说明文件：`docs/technical/PUBLIC_PREVIEW.md`。
- 当前临时公网地址为 `https://de33a135f02a5b.lhr.life`，右侧浏览器已打开并验证。
- PWA/cache/script 版本升级到 `finance-ai-assistant-v64`，`index.html` 加载 `app.js?v=64`。
- 项目进度更新到 `393 条自动化回归目标`。

## 测试记录
- 语法检查通过：`app.js`、`backend/server.mjs`、`scripts/public-preview-server.mjs`。
- 前端/PWA 全量回归通过 `166/166`。
- 后端/API 重点回归通过 `16/16`。
- 公网 `/health` 返回 `200`。
- 公网 `/api/project/progress` 返回 `200`，并显示 `393 条自动化回归目标`。
- 右侧浏览器验证通过：`https://de33a135f02a5b.lhr.life/?refresh=public-v64-browser-final#overview` 加载 `app.js?v=64`，显示 `后端 API 已连接`、`AI 规则参考`、上涨参考概率 `48%`、下跌风险概率 `52%`。

## 问题风险
- 当前公网地址是临时隧道，不是正式部署；电脑睡眠、网络断开、本机预览服务停止或 SSH 隧道停止后都会失效。
- 该公开链接可能被任何拿到链接的人访问，不能用于正式用户、敏感数据或生产投资服务。
- 正式上线仍需要生产托管、正式域名、生产数据库、生产认证、密钥管理、监控告警、法律合规复核和数据授权确认。

## 明日待办
- 规划正式公网部署路线：生产托管、域名、HTTPS、后端进程托管、环境变量密钥管理、日志监控和回滚。
- 将临时预览与正式上线在 UI/文档中继续区分，避免把隧道链接误认为生产发布。
- 继续稳定真实数据源和真实 AI 模型输出，让公开预览不仅可访问，也能持续显示真实数据。

## v65 追加记录：公开预览文案清理

## 今日完成
- 公网预览继续使用临时地址 `https://de33a135f02a5b.lhr.life`，本机同源预览服务已重启并加载最新版本。
- PWA/cache/script 版本升级到 `finance-ai-assistant-v65`，`index.html` 加载 `app.js?v=65`。
- 项目进度更新到 `394 条自动化回归目标`。
- 严格真实数据模式下，数据源技术详情不再把内部 `fixture/mock/sample/开发样例` 文案直接展示给用户，统一转换成 `离线契约演练`、`本机演练` 等说明。
- 保持真实数据底线：这次只是可见文案清理，不恢复样例数据兜底；没有真实数据的模块仍然保持空白或显示真实 provider 错误。

## 测试记录
- 前端/PWA 全量回归通过 `166/166`。
- 响应式 CSS 回归通过 `2/2`。
- 公网 `/health` 返回 `200`。
- 右侧浏览器验证通过：`https://de33a135f02a5b.lhr.life/?refresh=public-v65-provider-copy#settings` 加载 `app.js?v=65`，显示 `后端 API 已连接`、`394 条自动化回归目标`，并显示 `离线契约演练可用，不作为真实数据兜底`。

## 问题风险
- 当前公网地址仍是临时隧道，不是正式部署；电脑睡眠、网络断开、本机服务或隧道停止都会导致链接失效。
- 公开预览虽然可访问，但不应承载正式用户、敏感数据或生产投资建议。
- 其它后台/设置模块仍可能存在需要继续清理的开发语义文案，后续应按模块逐步处理。

## 明日待办
- 继续把临时预览和正式部署路径拆清楚，规划正式域名、托管、数据库、密钥管理、监控告警和合规审核。
- 继续扫描设置页、账户、通知、调度、审计等模块中的旧开发文案。
- 在公开预览中继续验证真实数据源和规则参考概率的稳定性。

## 决策记录
- v65 决策：公开页面可给用户访问，但仍明确定位为临时预览；不得把临时隧道当作正式上线。
- v65 决策：用户界面不再暴露内部 `fixture/mock/sample` 词，但技术实现仍允许测试层使用演练契约验证；演练数据不得冒充真实数据。

## v66 追加记录：公网同源 API 地址文案修复

## 今日完成
- 修复公网公开预览设置页的可见文案问题：同源 API 模式下不再显示空的 `当前后端地址：。`。
- 新增统一的 API 连接显示函数；公网模式显示 `同源 /api`，本地开发模式继续显示具体后端地址。
- 检查中状态同步修复：公网模式显示 `正在连接 同源 /api`，不再出现 `正在连接 。`。
- PWA/cache/script 版本升级到 `finance-ai-assistant-v66`，`index.html` 加载 `app.js?v=66`。
- 项目进度更新到 `395 条自动化回归目标`。

## 测试记录
- 语法检查通过：`app.js`、`backend/server.mjs`、`service-worker.js`。
- 针对性回归通过 `6/6`，包含公网同源 API、进度面板和 service worker 版本。
- 前端/PWA 全量回归通过 `166/166`。
- 响应式 CSS 回归通过 `2/2`。
- 右侧浏览器验证通过：`https://de33a135f02a5b.lhr.life/?refresh=public-v66-same-origin-label#settings` 加载 `app.js?v=66`，显示 `后端 API 已连接`、`当前后端连接：同源 /api`、`395 条自动化回归目标`，未出现空地址文案。
- 临时隧道随后轮换：旧地址返回 `503`，当前可访问地址为 `https://d069977f6f5437.lhr.life`，`/health` 返回 `200`。
- 新域名右侧浏览器验证通过：`https://d069977f6f5437.lhr.life/?refresh=public-v66-current-domain#settings` 加载 `app.js?v=66`，显示 `后端 API 已连接`、`当前后端连接：同源 /api`、`395 条自动化回归目标`。

## 问题风险
- 当前公网链接仍是临时隧道，不是正式部署。
- 同源 API 文案修复只解决公网预览可见状态，不等同于完成生产托管、域名、数据库、认证或合规发布。

## 明日待办
- 继续扫描设置页、账户、通知、调度、审计等模块中的旧开发文案。
- 继续规划正式部署，把临时隧道迁移为生产托管和正式域名。

## 决策记录
- v66 决策：公网预览状态栏应描述用户真实访问方式 `同源 /api`，不暴露空地址或本机 localhost 细节。

## v67 追加记录：处理公开测试报告问题

## 今日完成
- 根据 `/Users/serena/Desktop/project/测试报告/d069977f6f5437-lhr-life-test-report.md` 修复 4 个问题。
- AI 429/额度受限状态新增更明确的接力详情：显示主备模型尝试路径、备用模型错误码和冷却提示。
- 顶部股票快捷按钮改为带股票代码的直选逻辑，避免在 Apple 页面状态下点击 `腾讯控股` 或 `贵州茅台` 后仍停留在 Apple。
- 项目进度面板收敛内部门禁信息：详细上线门禁进入 `查看上线诊断详情` 折叠区，直接展示的完成项限制为前 8 项。
- 移动端 CSS 增加视口宽度约束，覆盖页面壳、侧边栏、主内容、面板、市场按钮和 provider 标签，修复 390px 横向溢出。
- PWA/cache/script 版本升级到 `finance-ai-assistant-v67`，`index.html` 加载 `app.js?v=67`。
- 项目进度更新到 `399 条自动化回归目标`。
- 临时公网地址再次轮换，当前可访问地址为 `https://2ca08760dbbf39.lhr.life`。

## 测试记录
- 语法检查通过：`app.js`、`backend/server.mjs`、`service-worker.js`。
- 报告相关定向回归通过 `8/8`。
- 前端/PWA 全量回归通过 `167/167`。
- 响应式 CSS 回归通过 `3/3`。
- 新公网 `/health` 返回 `200`。
- 右侧浏览器验证通过：`https://2ca08760dbbf39.lhr.life/?refresh=public-v67-report-fixes#overview` 加载 `app.js?v=67`，显示 `399 条自动化回归目标`，项目诊断为折叠状态。
- 右侧浏览器点击验证通过：快捷按钮可切换到 `腾讯控股 · 0700` 和 `贵州茅台 · 600519`。
- 390px 视口验证通过：`clientWidth=390`、`scrollWidth=390`，未复现横向溢出。

## 问题风险
- 完整真实 AI 模型仍可能受免费 provider 或 OpenAI 项目额度限制；v67 改善的是尝试记录、备用错误和冷却提示的可见性，不等于突破 provider 限额。
- 当前公网地址仍是临时隧道，可能继续轮换或失效。

## 明日待办
- 继续观察完整真实 AI 模型是否能在额度恢复后输出结构化分析。
- 继续把诊断类内容迁移到更明确的设置/管理视图，保持首页更像用户产品而不是内部看板。

## 决策记录
- v67 决策：顶部固定快捷股票可以直接按本地代码目录切换股票；普通最近搜索 chip 仍走后端搜索，避免跳过真实目录查询。
- v67 决策：规则参考和真实 AI 仍保持视觉区分；AI 限流时只增加透明度，不伪造成完整 AI 输出。

## v68 追加记录：AI 接力可见、搜索切换复测、首页诊断收纳

## 今日完成
- 规则参考分析面板新增真实 AI 主备接力详情：显示主模型/备用模型尝试路径、备用错误码、失败原因和冷却提示。
- 后端 `/api/analysis` 在真实模型失败后改用规则参考时，会随响应返回脱敏后的 `modelIssue`，前端可解释为什么不是完整真实 AI 输出。
- 首页默认只展示行情、新闻、分析和风险提示；设置/进度/数据源/审计等诊断内容只在 `#settings` 路由显示。
- 新增后端股票搜索回归：从 stale Apple 状态搜索 `腾讯控股` 和 `贵州茅台` 必须稳定切到 `0700` 和 `600519`。
- PWA/cache/script 版本升级到 `finance-ai-assistant-v68`，`index.html` 加载 `app.js?v=68`。
- 项目进度更新到 `402 条自动化回归目标`。
- 临时公网地址再次轮换，当前可访问地址为 `https://f703e59436bb1e.lhr.life`。

## 测试记录
- 语法检查通过：`app.js`、`backend/server.mjs`、`service-worker.js`。
- 前端/PWA 全量回归通过 `169/169`。
- 响应式 CSS 回归通过 `3/3`。
- 精确后端 AI relay 回归通过 `3/3`；精确后端分析/搜索回归通过 `4/4`。
- 大范围后端回归目前仍有 5 个与本轮无关的提醒/通知调度失败，集中在 triggered count 为 0 的旧问题。
- 新公网 `/health` 返回 `200`。
- 右侧浏览器验证通过：`https://f703e59436bb1e.lhr.life/?refresh=public-v68-improvements#overview` 加载 `app.js?v=68`，显示 `402 条自动化回归目标`，首页 `#settings.hidden=true`。
- 右侧浏览器等待后端连接后验证：AI 状态显示 `gpt-5.5 + gemini-2.5-flash 备用`；规则参考面板显示 `gpt-5.5 -> gemini-2.5-flash`、`REAL_AI_MODEL_RATE_LIMIT_OR_QUOTA`、`429` 和冷却提示。
- 右侧浏览器搜索验证通过：搜索 `腾讯控股` 切换到 `腾讯控股 · 0700`，搜索 `贵州茅台` 切换到 `贵州茅台 · 600519`。
- 右侧浏览器设置页验证通过：`#settings` 路由显示设置/诊断并隐藏首页内容；回到 `#overview` 后设置区隐藏。

## 问题风险
- 完整真实 AI 深度分析仍受免费 provider 或 OpenAI 项目额度限制；v68 解决的是可见接力、失败原因和不锁死重试，不突破 provider 限额。
- 当前公网地址仍是临时隧道，可能继续轮换或失效。
- 大范围后端提醒/通知调度测试仍有旧失败，需要后续单独处理。

## 明日待办
- 继续处理提醒/通知调度 triggered count 相关后端失败。
- 继续评估免费备用模型额度与稳定性，必要时增加更多可配置 provider。

## 决策记录
- v68 决策：真实数据规则参考可以展示概率，但必须同时公开说明它不是完整真实 AI，并展示主备模型失败/接力状态。
- v68 决策：首页默认面向使用者，诊断/进度/门禁信息归入设置页，避免普通用户首屏被内部信息淹没。

## v69 追加记录：AI 备用槽位、搜索按钮区分、新闻空状态详情

## 今日完成
- AI 规则参考面板改为首行显示 `当前仅为规则分析`，明确不是完整真实 AI 深度分析。
- AI 接力详情新增主模型、第一备用、第二备用、第三备用槽位状态；第二备用 OpenRouter、第三备用 Groq 未配置 key 时会明确显示。
- AI 接力详情新增“剩余额度”行：明确 provider 未返回剩余额度，只能显示本机可见的 429/额度限制和冷却状态。
- 搜索建议按钮加入上下文：常用股票显示 `常用：股票名`，最近搜索显示 `最近：关键词`，并加唯一 aria-label，避免同名按钮造成测试和用户点击歧义。
- 新闻空状态新增来源诊断：已请求来源、返回条数、过滤掉条数、个股情报阈值和最后更新时间。
- 首页设置区隐藏状态增加 `aria-hidden`，CSS 增加 `[hidden] { display: none !important; }`，防止布局样式意外显示内部诊断。
- PWA/cache/script 版本升级到 `finance-ai-assistant-v69`，`index.html` 加载 `app.js?v=69`。
- 项目进度更新到 `406 条自动化回归目标`。
- 临时公网地址再次轮换，当前可访问地址为 `https://28b5bb85d09317.lhr.life`。

## 测试记录
- 语法检查通过：`app.js`、`backend/server.mjs`、`service-worker.js`。
- 本轮定向回归通过 `6/6`。
- 前端/PWA 全量回归通过 `170/170`。
- 响应式 CSS 回归通过 `4/4`。
- 新公网 `/health` 返回 `200`。
- 右侧浏览器验证通过：`https://28b5bb85d09317.lhr.life/?refresh=public-v69-improvements#overview` 加载 `app.js?v=69`，显示 `406 条自动化回归目标`，首页 `#settings.hidden=true` 且 `aria-hidden=true`。
- 右侧浏览器等待后端连接后验证：AI 面板显示 `当前仅为规则分析`、`gpt-5.5 -> gemini-2.5-flash`、第二备用 OpenRouter 未配置 key、第三备用 Groq 未配置 key、`Provider 未返回剩余额度`。
- 右侧浏览器搜索 `腾讯控股` 后验证：页面切换到 `腾讯控股 · 0700`，并同时显示可区分的 `常用：腾讯控股` 与 `最近：腾讯控股`。

## 问题风险
- 完整真实 AI 深度分析仍受当前 OpenAI/Gemini 额度限制；v69 继续提升透明度，不等于突破免费额度。
- OpenRouter/Groq 第二、第三备用槽位已在 UI 中显示，但仍需要用户注册并配置 key 才能真正接力。
- 腾讯控股当前能返回真实新闻和公告，因此浏览器实测未触发空新闻面板；空状态详情由自动化回归覆盖。

## 明日待办
- 引导用户注册 OpenRouter/Groq 或其它免费 provider key，配置第二/第三备用模型。

## v69 公开地址恢复记录

## 今日完成
- 用户反馈 `https://28b5bb85d09317.lhr.life` 打不开后，确认该临时隧道已返回 `503`。
- 当前 localhost.run 隧道自动轮换为 `https://c45546cd10371b.lhr.life`。
- 已用 `/health` 和首页请求确认新地址返回 `200`。
- 已把右侧浏览器切换到 `https://c45546cd10371b.lhr.life/?refresh=public-v69-recovered#overview`。

## 测试记录
- `https://c45546cd10371b.lhr.life/health` 返回 `200`。
- `https://c45546cd10371b.lhr.life/?refresh=public-v69-recovered#overview` 返回 `200`。
- 右侧浏览器验证页面标题为 `AI 财经情报助手`，并加载 `app.js?v=69`。

## 问题风险
- `localhost.run` 免费临时域名不是固定生产域名，可能再次自动轮换或失效。

## 待办事项
- 正式上线前需要使用稳定部署平台和固定域名，避免用户访问临时链接时遇到 `503`。

## v70 追加记录：OpenRouter / Groq 备用模型接入

## 今日完成
- 将 OpenRouter 配置为第二备用 AI 模型，密钥仅保存到 `/private/tmp/finance_ai_model_fallback2_key`，不写入项目文件。
- 将 Groq 配置为第三备用 AI 模型，密钥仅保存到 `/private/tmp/finance_ai_model_fallback3_key`，不写入项目文件。
- OpenRouter 第二备用模型 ID 设置为 `qwen/qwen3-next-80b-a3b-instruct:free`。
- Groq 第三备用模型 ID 设置为 `llama-3.1-8b-instant`。
- AI 服务摘要从只显示 `gpt-5.5 + Gemini` 改为动态显示所有已配置可接力模型。
- PWA/cache/script 版本升级到 `finance-ai-assistant-v70`，`index.html` 加载 `app.js?v=70`。
- 项目进度更新到 `407 条自动化回归目标`。
- 临时公网地址轮换为 `https://7a92f1e3f16542.lhr.life`。

## 测试记录
- OpenRouter models API 返回 `200`；OpenRouter chat smoke 对免费模型返回上游临时 `429`，属于免费模型 provider 限流，不是 key 无效。
- Groq models API 返回 `200`；Groq chat smoke 返回 `200`，最小响应为 `OK`。
- 新公网 `/health` 返回 `200`，首页返回 `200`。
- 右侧浏览器验证通过：`https://7a92f1e3f16542.lhr.life/?refresh=public-v70-ai-fallback-keys#settings` 加载 `app.js?v=70`，显示 `407 条自动化回归目标`。
- 右侧浏览器 AI 设置区显示：`gpt-5.5 + gemini-2.5-flash + qwen/qwen3-next-80b-a3b-instruct:free + llama-3.1-8b-instant 备用`。
- 分析接口验证：系统已尝试主模型、Gemini、OpenRouter、Groq 四段接力；最后因备用模型输出触发禁用收益承诺/强制买卖表达过滤，安全降级为规则参考。
- 语法检查通过：`app.js`、`backend/server.mjs`、`service-worker.js`。
- 定向回归通过 `4/4`。
- 前端/PWA 全量回归通过 `170/170`。
- 响应式 CSS 回归通过 `4/4`。

## 问题风险
- OpenRouter 免费模型当前受上游临时限流影响，可能需要稍后重试或绑定自有 provider key。
- Groq 已能真实生成，但部分输出可能被合规过滤拦截；后续需要优化提示词和结构化修复流程，减少可修复输出被直接降级。
- `localhost.run` 临时公网域名仍可能再次轮换。

## 明日待办
- 优化备用模型输出的合规修复流程：当模型输出可修正但触发禁用表达时，先尝试安全改写和 JSON 修复，再决定是否降级为规则参考。

## v71 追加记录：AI 失败用户语言与自选股卡片状态

## 今日完成
- AI 接力失败区改为用户语言：按模型显示“额度不足 / 备用模型限流 / 输出格式或请求失败 / 输出未通过安全校验”等类型。
- 每个模型失败行新增“是否可重试”和“下一步操作”，普通用户不用直接阅读技术错误码。
- 技术错误码保留在 `查看技术诊断详情` 折叠区，用于后续排查。
- 当页面已有接力记录但 AI 服务状态尚未加载时，标题从误导性的“真实模型待配置”改为“真实 AI 接力未完成”。
- 自选股卡片会同步当前股票分析状态：规则参考已生成时显示 `规则参考 N%` 和 `完整 AI 待模型`，不再统一显示 `待AI模型 / AI 待真实模型`。
- PWA/cache/script 版本升级到 `finance-ai-assistant-v71`，`index.html` 加载 `app.js?v=71`。
- 项目进度更新到 `408 条自动化回归目标`。
- 临时公网地址轮换为 `https://6dd798809f453c.lhr.life`。

## 测试记录
- 语法检查通过：`app.js`、`backend/server.mjs`、`service-worker.js`。
- 定向回归通过：自选股规则参考卡片、真实数据规则参考、AI 限流接力、PWA 缓存、项目进度。
- 前端/PWA 全量回归通过 `171/171`。
- 响应式 CSS 回归通过 `4/4`。
- 新公网 `/health` 返回 `200`，首页返回 `200`，HTML 加载 `app.js?v=71`。
- 右侧浏览器设置页验证：后端连接、`408 条自动化回归目标`、OpenAI + Gemini + OpenRouter + Groq 四段接力仍显示正常。

## 问题风险
- 完整真实 AI 输出仍未成功：主模型额度不足，Gemini/Qwen 限流，Llama 结果仍可能触发安全/格式校验。
- 设置页整体仍偏开发诊断视图；本轮只收敛 AI 错误展示，后续需要做普通/管理员视图分层。

## 明日待办
- 增加模型输出安全改写和 JSON 修复流程，减少可修复输出直接降级为规则参考。
- 继续把设置页内部门禁、审计、模型配置细节收纳到管理员诊断区。
- 继续处理提醒/通知调度 triggered count 相关后端失败。

## 决策记录
- v69 决策：不能显示虚假的“剩余额度百分比”；provider 未返回时必须写明“未返回剩余额度”。
- v69 决策：同名搜索入口必须在可见文字或可访问名称上区分来源。
