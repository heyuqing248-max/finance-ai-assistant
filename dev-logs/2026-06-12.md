# 2026-06-12 开发日志

## 今日完成
- 对比免费行情 API 方案后，确认 Twelve Data 官方 Basic 免费档适合作为 Alpha Vantage 的优先替代源：免费额度为 `8 API credits/minute`、`800/day`。
- 新增 `twelve-data` 行情 provider：支持美股 quote 映射、Twelve Data quote 响应归一化、来源署名、延迟标签、API key 脱敏和严格真实数据空白回退。
- 新增 `multi-free` 免费 API 接力模式：按 `Twelve Data -> Alpha Vantage` 顺序尝试真实行情；任一 provider 失败、限流或无数据时尝试下一家，全部失败才返回空白。
- 支持独立 provider key：`FINANCE_AI_TWELVE_DATA_API_KEY` 与 `FINANCE_AI_ALPHA_VANTAGE_API_KEY`，避免不同服务共用同一个 key。
- Provider 注册表新增 `twelve-data` 与 `multi-free` 候选项；活跃真实通道可显示 `twelve-data-quote`、`alpha-vantage-quote` 或 `free-api-relay`。
- 设置页新增 Twelve Data 连接器、smoke test 和凭证预检状态；PWA 缓存版本升级到 `finance-ai-assistant-v10`。
- 修复 `multi-free` 只有 Alpha key 时 Twelve Data 被误标为 `configured` 的状态问题；现在会明确显示 `missing-key` 并提示缺 `FINANCE_AI_TWELVE_DATA_API_KEY`。
- 用户完成 Twelve Data 注册后，已将 Twelve Data key 仅以本机临时运行时方式写入 `/private/tmp/finance_ai_twelve_data_key`，没有写入代码、日志或项目记忆。
- 已用 `FINANCE_AI_MARKET_DATA_PROVIDER=multi-free` 重启本地服务，Twelve Data 作为第一行情源，Alpha Vantage 作为 fallback。
- 修复前端真实行情识别：`real-provider-relay` 现在会被视为真实 provider 数据，不再在股票页显示成“后端报价样例”。
- 项目进度快照更新为 `323 条自动化回归目标`。
- 设置页新增“本地真实行情与生产门禁”摘要，明确区分本机真实 quote 通道可用和正式上线门禁仍 blocked。
- 修复右侧网页刷新参数仍显示旧后端状态的问题：URL 带 `refresh=` 时只清理后端状态缓存，不删除自选股、持仓、提醒等用户数据。
- 修复真实数据源状态误报：Provider 注册表和配置向导不再固定显示 `mock`，会按当前可用真实 provider 显示 `multi-free + alpha-vantage-news`。
- 修复 `multi-free` 的总计划识别：数据源总计划和配置向导现在会认可 `FINANCE_AI_TWELVE_DATA_API_KEY` / `FINANCE_AI_ALPHA_VANTAGE_API_KEY` 这类专用 key，不再误报通用 `FINANCE_AI_MARKET_DATA_API_KEY` 缺失。
- 修复总览页真实报价不可见的问题：AI 分析接口不可用时仍会单独加载真实 quote；严格真实数据模式下只显示真实报价和来源，不生成本地交易计划。
- PWA 缓存版本升级到 `finance-ai-assistant-v11`，避免右侧网页继续运行旧脚本。
- 修复真实行情接入后的顶部文案：当股票仍来自 metadata-only 目录、但行情 quote 已连接时，`#stockCoverageNote` 不再显示“当前股票仅由代码目录识别”，改为“当前股票数据覆盖如下，未连接项保持空白”。
- PWA 缓存版本升级到 `finance-ai-assistant-v12`，用于刷新本轮覆盖说明修复。
- 优化数据源状态可读性：`#dataSourceState` 现在默认显示关键摘要（运行时、必选 provider、行情/新闻/公告/宏观状态和下一步缺口），长技术诊断收进 `技术详情与上线门禁` 折叠区。
- PWA 缓存版本升级到 `finance-ai-assistant-v13`，用于刷新本轮数据源状态折叠体验。
- 修复股票覆盖 chip 残留误导：当真实行情或其它真实覆盖已连接时，`股票` chip 从 `代码目录识别` 改为 `股票已识别`；只有完全没有真实覆盖时才显示代码目录识别。
- 优化新闻区可读性：`#newsList` 增加新闻摘要，默认显示前 6 条重点新闻，其余新闻收进 `展开另外 N 条新闻` 折叠区，减少首页滚动噪音。
- PWA 缓存版本升级到 `finance-ai-assistant-v15`，用于刷新股票 chip 和新闻摘要折叠体验。
- 修复顶部覆盖条新闻状态不同步：当当前股票的后端真实新闻/个股情报已返回时，`#stockCoverageNote` 的 `新闻` chip 会显示 `已连接`，不再残留 `待真实数据`。
- 切换股票时会清理旧新闻覆盖状态，避免上一只股票的新闻状态误显示到新股票上；新闻请求返回后再按当前股票/市场刷新覆盖条。
- PWA 缓存版本升级到 `finance-ai-assistant-v16`，用于刷新新闻覆盖条状态同步。
- 启用 SEC EDGAR 美股公告真实通道：本地真实数据服务增加 `FINANCE_AI_FILINGS_PROVIDER=sec-company-submissions`、`FINANCE_AI_FILINGS_ALLOW_NETWORK=true` 和 SEC User-Agent；MSFT 公告接口返回 `mode=real-provider`、来源 `SEC EDGAR`、8 条 filing。
- 新增 `scripts/real-data-dev-server.mjs` 和 `npm run dev:real`，统一读取 `/private/tmp` 临时 key 并启动 Twelve Data 行情、Alpha Vantage 新闻、SEC EDGAR 公告和 World Bank 宏观数据，不打印或保存密钥。
- 接入 World Bank Open Data 作为第一版真实宏观 provider：支持 US/CN/HK 年度 GDP 增速、通胀和实际利率指标，保留来源、asOf、年度频率和滞后免责声明。
- 修复严格真实数据模式下 macro adapter 被提前短路的问题；配置真实宏观 provider 时允许通过 adapter 请求真实数据，否则继续保持空白。
- 前端分析加载失败时现在会单独请求 `/api/macro/context`，真实宏观返回后 `#stockCoverageNote` 的 `宏观` chip 显示 `已连接`。
- PWA 缓存版本升级到 `finance-ai-assistant-v17`，用于刷新宏观覆盖条状态。
- 新增 GDELT DOC API 公开新闻补充源和 `multi-free-news` 接力：新闻请求按 `Alpha Vantage -> GDELT` 尝试；Alpha 日额度失败时会继续尝试 GDELT，不再直接结束为空。
- 当所有真实新闻源仍失败时，前端不再笼统显示 `新闻 待真实数据`，而是显示具体状态如 `新闻 额度/超时`，新闻摘要区展示已尝试 provider 和失败原因。
- `scripts/real-data-dev-server.mjs` 的新闻通道改为 `multi-free-news`，并打开 `FINANCE_AI_GDELT_NEWS_ALLOW_NETWORK=true`。
- PWA 缓存版本升级到 `finance-ai-assistant-v18`，用于刷新新闻接力失败态显示。
- 新增 Yahoo Finance RSS 作为公开新闻接力中间层：新闻请求顺序调整为 `Alpha Vantage -> Yahoo Finance RSS -> GDELT`，当 Alpha 无内容时可继续读取公开 RSS 新闻，保留原文链接和来源署名。
- 修复新闻接力后网页状态：MSFT 新闻接口可返回 `mode=real-provider`、provider `yahoo-finance-rss`，覆盖条可显示真实新闻来源，不再把已返回的公开 RSS 新闻误判为样例数据。
- 完成真实 AI 模型适配器第一阶段：`backend/services/ai-provider-adapter.mjs` 支持 OpenAI-compatible chat completions、本机运行时开关、结构化 JSON 输出校验、禁止收益保证表达检查和失败空白策略。
- `scripts/real-data-dev-server.mjs` 支持从 `/private/tmp/finance_ai_model_key`、`/private/tmp/finance_ai_model_id`、可选 `/private/tmp/finance_ai_model_base_url` 读取真实模型配置；没有模型 key/id 时只报告缺口，不打印或保存密钥。
- 严格真实数据模式下，`GET /api/analysis` 在缺少真实模型时返回 `REAL_AI_MODEL_NOT_CONFIGURED`，网页显示 `暂无真实 AI 分析`，不再用样例规则输出冒充真实 AI。
- 顶部覆盖条 AI 状态从笼统 `待真实模型` 改为可执行状态：`待配置模型`、`待启用模型`、`已连接`、`已生成`。
- PWA 缓存版本升级到 `finance-ai-assistant-v20`，用于刷新真实 AI 模型状态显示。
- 优化总览页 AI 空状态：新增 `真实模型待配置 / 配置缺口 / 空白策略 / 重新检测 AI` 卡片，明确告诉用户当前没有真实模型，不展示样例建议。
- PWA 缓存版本升级到 `finance-ai-assistant-v21`，用于刷新本轮 AI 空状态说明和 service worker 缓存。
- 修复“重新检测 AI”后的错误提示：前端现在保留后端错误码，`REAL_AI_MODEL_NOT_CONFIGURED` 会显示为真实模型配置缺口，不再泛化为普通 API 不可用。
- 项目进度快照更新为 `324 条自动化回归目标`。
- PWA 缓存版本升级到 `finance-ai-assistant-v22`，用于刷新本轮错误码提示和进度显示。
- 修复外部浏览器与右侧浏览器显示不一致：`refresh=` 现在会清理旧的后端/原型状态缓存，真实数据刷新会默认进入已验证的 `Microsoft · MSFT` 美股真实数据通道，避免外部浏览器因旧 localStorage 默认 A 股样例而显示 `行情 待真实数据 / 公告 待真实数据`。
- 严格真实数据总览徽标修复：当行情、新闻、公告或宏观至少一个真实 provider 已返回时，总览徽标显示 `已有真实数据`；完全没有真实非 AI 数据时才显示 `等待真实数据`，不再笼统显示 `暂无真实数据`。
- PWA 缓存版本升级到 `finance-ai-assistant-v24`，用于刷新外部浏览器/Service Worker 缓存。
- 可靠 AI 模型默认路径已配置：OpenAI 官方 base URL 默认使用 Responses API + JSON Schema；`scripts/real-data-dev-server.mjs` 在发现模型 key 但未设置 model id 时默认使用 `gpt-5.5`，并保留 `FINANCE_AI_MODEL_API_STYLE=responses`。
- 项目进度快照更新为 `327 条自动化回归目标`。

## 测试记录
- 语法检查通过：`backend/providers/market-data-provider.mjs`、`backend/providers/mock-provider.mjs`、`backend/server.mjs`、`app.js`、`service-worker.js`、`backend/tests/backend-api.test.mjs`、`tests/frontend-regression.test.mjs`。
- 后端目标回归通过 `7/7`：Provider 注册表、provider-specific key、Alpha Vantage quote、Twelve Data 映射/解析、Twelve Data quote、Twelve Data 失败后 Alpha Vantage 接力成功。
- 前端目标回归通过 `2/2`：严格真实数据进度快照、PWA 缓存版本状态。
- 本轮合计目标回归通过 `9/9`。
- 追加状态修复回归通过：后端 provider relay 目标 `8/8`，新增覆盖 Twelve Data 缺 key 的 `missing-key` 状态。
- 真实 quote smoke 通过：`GET /api/market-data/quote?market=us&code=MSFT` 返回 `status=ok`、`mode=real-provider-relay`、`provider.id=twelve-data`、`source.label=Twelve Data Quote`，请求 URL 已脱敏。
- 追加前端真实接力回归通过 `2/2`：自动抓取真实 quote、免费 API 接力 quote 均会显示“最新真实报价”，不会出现“后端报价样例”。
- 追加 PWA/进度回归通过 `2/2`：严格真实数据进度快照、`finance-ai-assistant-v10` 离线缓存版本状态。
- 本地 API 验证通过：`/api/data-sources/market-data-adapter` 显示 Twelve Data key 为 `configured-redacted`；`/api/data-sources/auto-ingestion-run?market=us&symbol=MSFT` 返回 `partial-real-data`，真实数据分组 `2/5`，行情来自 Twelve Data，新闻来自 Alpha Vantage News。
- 追加网站可用性回归通过 `4/4`：刷新参数清理旧后端状态缓存、设置页真实行情/生产门禁拆分、PWA 缓存、进度快照。
- 右侧浏览器验证通过：`http://127.0.0.1:4173/?refresh=20260612-cache-live-gate-v10#settings` 显示 `2026-06-12`、`323 条自动化回归目标`，不再显示旧 `2026-06-10` 进度，并显示“本地真实行情：Twelve Data / Alpha Vantage 可请求 quote；生产门禁 blocked”。
- 追加真实数据源状态回归通过 `3/3`：默认无 key 仍显示 mock；`multi-free` 部分真实配置显示 `multi-free + alpha-vantage-news`；宏观未配置时仍保持生产 blocked。
- 追加前端真实报价回归通过 `3/3`：v11 缓存、免费 API 接力 quote、AI 失败时仍显示真实 quote。
- 后端 API 验证通过：`/api/data-sources` 显示 `partial-real-provider-connected`、真实数据源 `2/3`、注册表 `multi-free + alpha-vantage-news`、行情配置 `ready-for-smoke`；`/api/market-data/quote?market=us&code=MSFT` 返回 `real-provider-relay` 与 `Twelve Data Quote`。
- 右侧浏览器最终验证通过：`http://127.0.0.1:4173/?refresh=20260612-real-quote-main-card#overview` 显示 `Microsoft · MSFT`、`行情 已连接`、`最新真实报价 387.9`、来源 `Twelve Data Quote`，并且交易计划保持隐藏。
- 追加覆盖说明回归通过 `3/3`：PWA v12、免费 API 接力 quote、AI 失败但真实行情可用时的顶部覆盖文案。
- 右侧浏览器覆盖说明验证通过：`http://127.0.0.1:4173/?refresh=20260612-coverage-summary-v12#overview` 显示 `当前股票数据覆盖如下`、`行情 已连接`、`最新真实报价 387.7`、来源 `Twelve Data Quote`，旧文案 `当前股票仅由代码目录识别` 已消失，交易计划仍隐藏。
- 追加数据源状态可读性回归通过：后端连接水合测试确认 `provider-operational-summary` 存在、`技术详情与上线门禁` 存在且默认折叠；PWA v13、严格真实数据空状态和响应式 CSS 回归通过。
- 右侧浏览器数据源状态验证通过：`http://127.0.0.1:4173/?refresh=20260612-provider-details-v13#settings` 显示关键摘要 `运行时 multi-free + alpha-vantage-news · 必选 2/3`、`行情 已连接`、`新闻 可请求/空白回退`、`下一步缺口：宏观经济数据`，技术详情默认折叠但 DOM 中仍保留 Provider 注册表明细。
- 追加股票 chip / 新闻摘要回归通过 `3/3`：PWA v15、长新闻列表摘要折叠、AI 失败但真实行情可用时 `股票已识别`。
- 右侧浏览器验证通过：`http://127.0.0.1:4173/?refresh=20260612-stock-chip-news-v15#overview` 显示 `股票 已识别`、不再显示 `股票 代码目录识别`、`最新真实报价 388.2`；新闻区显示 `后端真实数据通道`、`优先显示 6 条重点内容`、`50 条`、`另有 44 条已折叠`，更多新闻默认折叠。
- 追加新闻覆盖条回归通过 `3/3`：PWA v16、长新闻列表加载后 `新闻 已连接`、AI 失败但真实行情可用时保持严格真实数据空状态。
- 语法检查通过：`app.js`、`tests/frontend-regression.test.mjs`。
- 右侧浏览器验证通过：`http://127.0.0.1:4173/?refresh=20260612-news-coverage-v16#overview` 的 Microsoft · MSFT 页面显示 `新闻 已连接`、`行情 已连接`、新闻摘要 `50 条 / 个股情报 50 / 另有 44 条已折叠`，不再显示 `新闻 待真实数据`。
- SEC 公告接口验证通过：`GET /api/news/filings?market=us&symbol=MSFT` 返回 `status=ok`、`mode=real-provider`、`source.label=SEC EDGAR`、8 条 filing。
- World Bank 宏观接口验证通过：`GET /api/macro/context?market=us` 返回 `mode=real-provider`、`source.label=World Bank Open Data`、GDP 增速 `2.79%`、通胀 `2.95%`、实际利率 `-1.09%`。
- 追加宏观目标回归通过 `4/4`：World Bank 无 key provider、严格无 provider 空白、PWA v17、AI 空状态下 `宏观 已连接`。
- 语法检查通过：`backend/providers/macro-data-provider.mjs`、`backend/providers/mock-provider.mjs`、`backend/server.mjs`、`app.js`、`scripts/real-data-dev-server.mjs`。
- 右侧浏览器验证通过：`http://127.0.0.1:4173/?refresh=20260612-worldbank-macro-v17#overview` 的 Microsoft · MSFT 页面显示 `公告 已连接`、`宏观 已连接`、页面包含 `World Bank Open Data`，不再显示 `宏观 待真实数据`。
- 追加新闻接力回归通过 `4/4`：GDELT 响应解析、Alpha 额度失败后 GDELT fallback、PWA v18、前端新闻接力失败态显示。
- 语法检查通过：`app.js`、`tests/frontend-regression.test.mjs`、`backend/providers/news-filings-provider.mjs`、`backend/tests/backend-api.test.mjs`、`scripts/real-data-dev-server.mjs`。
- 右侧浏览器验证通过：`http://127.0.0.1:4173/?refresh=20260612-news-relay-v18#overview` 的 Microsoft · MSFT 页面显示 `新闻 额度/超时`，不再显示 `新闻 待真实数据`；新闻摘要显示已尝试 `alpha-vantage-news -> gdelt-doc-news` 和失败原因 `ALPHA_VANTAGE_NEWS_EMPTY / GDELT_NEWS_TIMEOUT`，同时 `公告 已连接`、`宏观 已连接` 保持正常。
- Yahoo RSS 目标回归通过 `5/5`：公开 RSS 解析、Alpha 空结果后 Yahoo fallback、来源署名、真实 provider 模式和前端新闻展示状态。
- 真实 AI 目标语法检查通过：`backend/services/ai-provider-adapter.mjs`、`backend/services/mock-ai-service.mjs`、`backend/server.mjs`、`app.js`、`scripts/real-data-dev-server.mjs`、`backend/tests/backend-api.test.mjs`、`tests/frontend-regression.test.mjs`。
- 真实 AI 目标回归通过 `7/7`：AI 服务状态、provider adapter 安全门禁、模型设置指南、发布证据包、门禁 ready 状态、OpenAI-compatible 本机模型调用适配、PWA v20 / AI chip 状态。
- API 验证通过：`GET /api/ai-services/provider-adapter` 在未配置模型时返回 `status=blocked`、`configured=false`、`canCallLiveModel=false`、缺少 `FINANCE_AI_MODEL_PROVIDER / FINANCE_AI_MODEL_API_KEY / FINANCE_AI_MODEL_ID`；`GET /api/analysis?symbol=MSFT&riskProfile=balanced` 返回 `REAL_AI_MODEL_NOT_CONFIGURED`。
- 右侧浏览器验证通过：`http://127.0.0.1:4173/?refresh=20260612-ai-model-v20#overview` 显示 `AI 待配置模型`、`暂无真实 AI 分析`，同时新闻来自 Yahoo Finance RSS、公告来自 SEC EDGAR、宏观来自 World Bank Open Data。
- 追加 AI 空状态/PWA 目标检查通过：语法检查 `app.js`、`service-worker.js`；前端目标回归 `2/2`；PWA 资产回归 `4/4`。
- 本地 HTTP 验证通过：`http://127.0.0.1:4173/app.js` 和 `service-worker.js` 均已返回 `finance-ai-assistant-v21`。
- 追加错误码/进度目标检查通过：语法检查 `app.js`、`backend/server.mjs`；前端目标回归 `6/6`；后端进度目标回归 `1/1`；PWA 资产回归 `4/4`。
- 本地服务已重启到 v22；API 验证通过：`/api/project/progress` 返回 `324 条自动化回归目标`，`/api/analysis?symbol=MSFT&riskProfile=balanced` 返回 `REAL_AI_MODEL_NOT_CONFIGURED` 和“真实 AI 模型尚未配置”。
- 本地 HTTP 验证通过：`http://127.0.0.1:4173/app.js` 响应体首行为 `const PWA_CACHE_VERSION = "finance-ai-assistant-v22";`。
- v24 目标回归通过：语法检查 `app.js`、`service-worker.js`、`backend/server.mjs`、`backend/services/ai-provider-adapter.mjs`、`scripts/real-data-dev-server.mjs`、`backend/tests/backend-api.test.mjs`；前端目标回归 `7/7`；后端 AI/进度目标回归 `4/4`；PWA 资产回归 `4/4`。
- 本地服务已重启到 v24；HTTP 验证通过：`http://127.0.0.1:4173/app.js` 首行为 `const PWA_CACHE_VERSION = "finance-ai-assistant-v24";`，`/api/project/progress` 返回 `327 条自动化回归目标`。
- 真实 provider API 验证通过：MSFT 行情返回 `real-provider-relay / Twelve Data Quote / 390.34`；新闻返回 `real-provider / yahoo-finance-rss / Yahoo Finance RSS`；公告返回 `real-provider / SEC EDGAR`；宏观返回 `real-provider / World Bank Open Data / GDP 增速`。
- 右侧浏览器验证通过：`http://127.0.0.1:4173/?refresh=20260612-real-data-ai-v24#overview` 显示 `Microsoft · MSFT`、`行情 已连接`、新闻真实来源、`公告 已连接`、`宏观 已连接`、`AI 待配置模型`，且不再出现旧的 `暂无真实数据`。

## 问题风险
- Twelve Data key 只存在本机临时文件和当前运行时；服务重启或临时文件清理后需要重新注入。
- Twelve Data 第一阶段只开放美股 quote；A 股/港股还需要确认符号映射、市场授权和延迟标签。
- 新闻、公告、宏观和公开言论仍需独立真实 provider；不能用未经授权的 App 抓包、付费墙或登录绕过方式采集。
- `/api/market-data/quote` 已能做本地真实 provider smoke，但生产授权、缓存、限流、署名、交易所协议和延迟标签门禁仍显示 blocked，不能当作正式上线已完成。
- 总览页目前能展示真实 quote 与真实新闻列表，但 AI 分析、公告、宏观和公开言论仍会在缺少真实 provider 时保持空白。
- A 股/港股新闻仍没有稳定真实 provider；这些市场显示 `新闻 待真实数据` 是准确空白状态，不应伪装成已连接。
- World Bank Open Data 是年度宏观指标，不是实时政策日历；UI 和免责声明必须保留年度/asOf 和发布滞后说明。
- Alpha Vantage 新闻今日可能受免费额度限制；额度耗尽时 `新闻 待真实数据` 是严格空白状态，不能用样例新闻顶替。
- GDELT 在当前本地网络中可能超时；它是公开新闻补充源，不是持牌财经新闻源。超时时 UI 必须显示具体原因，不能伪装成已连接。
- 当前本机没有真实 AI 模型 key/model id，所以 AI 分析仍保持空白；这不是 bug，是严格真实数据策略。
- 当前本机没有 `/private/tmp/finance_ai_model_key` 和 `/private/tmp/finance_ai_model_id`，所以 AI 模型只完成可靠默认路径配置，还不能真实调用；需要用户提供模型 key 后再做 live smoke。
- 即使真实模型通道已完成，也不能把模型输出写成保证收益、稳赚、无风险、必须买入或必须卖出；只能使用“模型参考概率”“分析参考”“不构成投资建议”等表达。
- 大范围旧回归套件仍有部分历史样例/fixture 期望没有清理；本轮只声明真实 AI 和 PWA 相关目标回归通过。

## 明日待办
- 优先让网站可用：继续修复刷新、搜索、真实数据空状态、错误提示、页面可见状态这些直接影响使用的问题。
- 继续接入宏观、公告、新闻或基本面 provider，减少只有行情 quote 的数据孤岛。
- 后续可继续加入 FMP、SEC、FRED 等 provider，形成行情、基本面、公告和宏观的多源真实数据层。
- 下一轮优先继续改善总览页：让新闻列表和 AI 空状态更像面向普通用户的可读摘要，而不是调试输出。
- 如果用户提供真实模型 key，下一轮写入 `/private/tmp/finance_ai_model_key` 后重启本机服务；未提供 model id 时默认使用 `gpt-5.5`，并验证 `AI 已连接 / 已生成`、Responses API JSON Schema、引用来源、合规措辞和失败空白策略。
- 继续把“空白但可理解”的状态补齐到公开言论、基本面、估值和技术面等后续模块，减少普通用户看到调试式空白。

## 决策记录
- 免费行情源优先级采用 `Twelve Data -> Alpha Vantage`。
- 免费新闻源优先级采用 `Alpha Vantage -> Yahoo Finance RSS -> GDELT`。
- 严格真实数据模式继续保留：没有真实 provider 数据时保持空白，不回退样例数据。
- 真实 AI 模型采用 OpenAI-compatible 适配器优先；未配置模型 key/id 前，AI 模块显示 `待配置模型` 并保持空白。
- OpenAI-compatible 官方 base URL 优先使用 Responses API；非官方兼容服务可通过 `FINANCE_AI_MODEL_API_STYLE=chat-completions` 保持旧接口兼容。

## 追加记录：v25 AI 配置显示与浏览器一致性

## 今日完成
- 已把 PWA 缓存版本升级到 `finance-ai-assistant-v25`，用于解决外部浏览器和 Codex 右侧浏览器显示不一致的问题。
- 已修复前端 AI provider 状态清洗逻辑：`baseUrlStatus`、`apiStyle`、`recommendedModelId` 不再被丢弃。
- 设置页现在会明确显示：`推荐模型 gpt-5.5`、`接口 responses`、`base URL` 状态，以及运行时 key 路径 `/private/tmp/finance_ai_model_key`。
- 项目进度快照更新为 `328 条自动化回归目标`。
- 本地服务已重启到 v25，并验证 `http://127.0.0.1:4173/app.js` 首行为 `const PWA_CACHE_VERSION = "finance-ai-assistant-v25";`。
- API 验证通过：`/api/project/progress` 返回 `328 条自动化回归目标`；`/api/ai-services/provider-adapter` 返回 `apiStyle=responses`、`recommendedModelId=gpt-5.5`、`status=ready-for-user-configuration`。
- 右侧浏览器验证通过：`http://127.0.0.1:4173/?refresh=20260612-ai-setup-v25#settings` 显示 `328 条自动化回归目标`、`推荐模型 gpt-5.5`、`接口 responses`、`/private/tmp/finance_ai_model_key`、`待模型 key`。

## 测试记录
- 语法检查通过：`app.js`、`service-worker.js`。
- 前端目标回归通过 `5/5`：本地开发后端刷新缓存服务状态、service worker ready 状态、生产数据库进度、部署进度、合规进度。
- PWA 资产回归通过 `4/4`。

## 问题风险
- 当前本机仍没有 `/private/tmp/finance_ai_model_key`，因此 AI 模块显示 `待模型 key` 是正确状态，不是页面错误。
- `gpt-5.5` 已作为推荐默认模型写入配置路径，但在没有真实 key 前不能做 live smoke，也不能显示“AI 已连接”。
- 外部浏览器如果仍显示旧页面，需要强制刷新或用带 `refresh=20260612-ai-setup-v25` 的 URL 触发旧缓存清理。

## 明日待办
- 用户提供真实模型 key 后，写入 `/private/tmp/finance_ai_model_key`，重启本地服务并验证 Responses API 的真实结构化输出。
- 继续补齐 A 股/港股真实新闻和公告 provider，减少只在美股 MSFT 上验证通过的范围限制。
- 继续完善总览页空状态，让“待真实数据 / 待模型 key”对普通用户更直观。

## 决策记录
- 可靠 AI 模型默认路径定为 OpenAI 官方 Responses API + JSON Schema。
- 未提供 model id 时，开发服务默认使用 `gpt-5.5`。
- 严格真实数据策略继续保留：没有真实数据或真实模型时保持空白，不用 mock 数据顶替。

## 追加记录：v26 刷新选股修复与本机 AI Key 配置入口

## 今日完成
- 已把 PWA 缓存版本升级到 `finance-ai-assistant-v26`。
- 已修复用户反馈的“现在还是这样”：任何带 `refresh=` 的页面启动都会清理旧选股状态，并切到已验证真实数据路径 `Microsoft · MSFT`，避免外部浏览器继续停在 `贵州茅台 · 600519` 导致行情/公告待真实数据。
- 设置页新增可见入口 `本机 AI 模型配置`，包含模型 API key、模型 ID、Base URL 和 `保存到本机运行时` 按钮。
- 后端新增 `POST /api/ai-services/local-model-config`：仅允许 localhost，本机写入 `/private/tmp` 运行时密钥文件，刷新当前进程 AI adapter，返回时只显示脱敏状态，不回显 API key。
- 项目进度更新为 `329 条自动化回归目标`；真实 AI 分析分项从 `93%` 提升到 `94%`。
- 本地服务已重启到 v26；右侧浏览器验证 `http://127.0.0.1:4173/?refresh=20260612-v26-fix-2#settings` 当前选中 `Microsoft · MSFT`，并显示 `本机 AI 模型配置`、`保存到本机运行时`、`gpt-5.5`、`接口 responses`、`329 条自动化回归目标`。

## 测试记录
- 语法检查通过：`backend/server.mjs`、`backend/services/mock-ai-service.mjs`、`app.js`、`service-worker.js`、`tests/frontend-regression.test.mjs`、`backend/tests/backend-api.test.mjs`。
- 后端目标回归通过 `2/2`：AI provider 安全门禁、本机 AI 模型配置接口 localhost-only 且不回显 key。
- 前端目标回归通过 `6/6`：刷新旧 A 股选中项后切 MSFT、AI 设置页本机 key 表单、service worker ready、生产数据库/部署/合规进度。
- PWA 资产回归通过 `4/4`。

## 问题风险
- 当前仍没有真实模型 key，因此 `AI 待配置模型` 仍是正确状态。
- 新配置入口只解决“普通用户如何把 key 提交给本机后端”的问题；真实 AI 输出还需要 key、live smoke、引用证据、成本限流和合规输出校验。
- A 股/港股仍缺完整真实行情/新闻/公告 provider；refresh 默认展示 MSFT 是为了让本地 demo 先进入已验证真实数据路径。

## 明日待办
- 用户在设置页提交真实模型 key 后，验证 `/api/analysis?symbol=MSFT&riskProfile=balanced` 的真实 Responses API 结构化输出。
- 继续补齐 A 股/港股真实数据 provider，不再依赖默认切到 MSFT 来展示真实数据。

## 决策记录
- `refresh=` 现在统一作为“清旧缓存并进入已验证真实数据路径”的用户操作。
- 模型 key 只允许本机后端接收；不得写入项目文件、浏览器 localStorage、日志或回显到页面。

## 追加记录：v27 Yahoo Chart 行情 fallback

## 今日完成
- 已把 PWA 缓存版本升级到 `finance-ai-assistant-v27`。
- 行情免费接力从 `Twelve Data -> Alpha Vantage` 扩展为 `Twelve Data -> Alpha Vantage -> Yahoo Chart`。
- 新增 Yahoo Finance Chart symbol 映射：美股 `MSFT`、港股 `0700.HK`、沪市 A 股 `600519.SS`、深市 A 股 `000001.SZ`。
- 新增 Yahoo Finance Chart quote 解析器，能读取价格、前收盘、涨跌幅、成交量、币种、时间戳和来源。
- 项目进度更新为 `330 条自动化回归目标`。
- 本地 API 验证通过：`/api/market-data/quote?market=a&code=600519` 返回 `real-provider-relay`，provider 为 `yahoo-chart`，来源 `Yahoo Finance Chart`，最新真实报价约 `1281.55`。
- 右侧浏览器验证通过：点击页面里的 `贵州茅台` 后，当前股票显示 `贵州茅台 · 600519`，覆盖标签显示 `行情 已连接`，走势区显示 `最新真实报价 1282` 和 `Yahoo Finance Chart`，不再显示 `行情 待真实数据`。

## 测试记录
- 语法检查通过：`backend/providers/market-data-provider.mjs`、`backend/server.mjs`、`app.js`、`service-worker.js`、`backend/tests/backend-api.test.mjs`、`tests/frontend-regression.test.mjs`。
- 后端目标回归通过 `7/7`：Alpha Vantage quote、Twelve Data 映射/quote、Twelve Data -> Alpha Vantage 接力、Yahoo Finance symbol/quote、A 股 Yahoo fallback、Twelve Data missing-key。
- 前端目标回归通过 `4/4`：生产数据库进度、部署进度、合规进度、service worker ready。
- PWA 资产回归通过 `4/4`。

## 问题风险
- Yahoo Chart 是公开端点 fallback，用于本地 Demo 扩展覆盖，不代表交易所授权实时行情或正式生产许可。
- A 股目前只解决 quote 覆盖；新闻、公告和更完整历史走势图仍可能保持空白或接力失败。
- 生产上线前仍需要正式行情授权、缓存/限流策略、延迟标签和来源条款复核。

## 明日待办
- 继续把 Yahoo fallback 扩展到港股常用股票 UI 验证，例如 `0700.HK`。
- 后续再评估是否为 Yahoo Chart 增加历史走势图 provider，但必须保持来源、延迟和许可说明。

## 决策记录
- 严格真实数据模式继续保留：Yahoo fallback 成功才显示真实 quote；失败仍保持空白，不使用样例行情兜底。
- Yahoo Chart 在 UI 中必须明确显示 `Yahoo Finance Chart` 和非投资建议说明。

## 追加记录：v28 Yahoo Chart 真实走势图 fallback

## 今日完成
- 已把 PWA 缓存版本升级到 `finance-ai-assistant-v28`。
- 新增 Yahoo Finance Chart history 解析器，能读取日期、收盘价、币种、来源、时间戳。
- `getPriceHistory` 现在可在真实 market-data provider 启用时优先返回 Yahoo Chart 真实走势。
- 当 `interval=1mo` 月线返回点数不足时，系统会自动重试 `interval=1d`、`range=6mo`，避免页面只有真实报价但没有真实走势图。
- 项目进度更新为 `332 条自动化回归目标`。
- 本地 API 验证通过：`/api/market-data/history?market=a&code=600519&range=6m&interval=1mo` 返回 `status=ok`、`mode=real-provider`、`source.label=Yahoo Finance Chart`、8 个真实日线点。
- 右侧浏览器验证通过：点击 `贵州茅台` 后显示 `贵州茅台 · 600519`，走势区显示 `6/3 至 6/12 下跌 -0.03%`，来源 `Yahoo Finance Chart · 1d · 6mo`，SVG 图包含 polyline 和 8 个点，不再显示 `暂无真实走势`。

## 测试记录
- 语法检查通过：`backend/providers/market-data-provider.mjs`、`backend/providers/mock-provider.mjs`、`backend/server.mjs`、`app.js`、`backend/tests/backend-api.test.mjs`、`tests/frontend-regression.test.mjs`。
- 后端目标回归通过 `5/5`：Yahoo symbol/quote、Yahoo history 解析、A 股 Yahoo quote fallback、Yahoo history provider、月线不足自动日线重试。
- 前端目标回归通过 `4/4`：生产数据库进度、部署进度、合规进度、service worker ready。
- PWA 资产回归通过 `4/4`。

## 问题风险
- Yahoo Chart history 是公开端点 fallback，不代表正式授权行情历史数据库。
- 当前 history fallback 会在点数不足时退到日线，UI 会显示 `1d · 6mo`，必须保留来源和延迟说明。
- A 股新闻、公告仍可能保持空白或接力失败；本轮只解决真实报价和真实走势图。

## 明日待办
- 继续验证港股 `腾讯控股 · 0700` 的 quote/history 是否能通过 Yahoo Chart fallback 显示。
- 后续评估是否把真实走势图数据纳入 AI 因子输入，但必须先保留来源、延迟和合规边界。

## 决策记录
- 真实 history 成功才展示走势图；失败仍为空白，不回退本地样例走势。
- 月线不足自动日线重试属于真实 provider fallback，不属于样例兜底。

## 追加记录：v29 A 股新闻与上交所公告真实源

## 今日完成
- 已把 PWA 缓存版本升级到 `finance-ai-assistant-v29`。
- 新闻免费接力扩展为 A/HK/US symbol 映射：Alpha Vantage 不支持 A 股时，继续尝试 Yahoo Finance RSS 和 GDELT DOC 公开新闻源。
- 新增上交所公开公告源 `sse-company-bulletins`，用于沪市 A 股公告；`600519` 可返回上海证券交易所公告标题、发布时间和 PDF 原文链接。
- `scripts/real-data-dev-server.mjs` 启动提示已更新：公告源为 A 股上交所公告 + 美股 SEC EDGAR。
- 项目进度更新为 `335 条自动化回归目标`。
- 本地 API 验证通过：`/api/news/intelligence?market=a&symbol=600519` 返回 `mode=real-provider`、来源 `Yahoo Finance RSS`、5 条真实新闻标题。
- 本地 API 验证通过：`/api/news/filings?market=a&symbol=600519` 返回 `mode=real-provider`、来源 `上海证券交易所公告`、8 条真实公告。
- 右侧浏览器验证通过：`贵州茅台 · 600519` 显示 `行情 已连接`、`新闻 已连接`、`公告 已连接`、`宏观 已连接`、`AI 待配置模型`，并显示真实新闻、真实公告和 Yahoo Chart 真实走势图。

## 测试记录
- 语法检查通过：`backend/providers/news-filings-provider.mjs`、`backend/tests/backend-api.test.mjs`、`app.js`、`service-worker.js`、`backend/server.mjs`、`scripts/real-data-dev-server.mjs`。
- 后端目标回归通过 `6/6`：Yahoo RSS fallback、GDELT fallback、A 股 GDELT fallback、SSE 公告解析、SEC filings、A 股 SSE filings。
- 追加 provider 回归通过 `4/4`：Yahoo RSS 解析、A 股新闻 fallback、SSE 公告解析、SSE 公告请求。
- 前端目标回归通过 `3/3`：进度面板相关目标。
- PWA 资产回归通过 `4/4`。

## 问题风险
- Yahoo Finance RSS 和 GDELT 是公开标题/索引补充源，不是付费财经新闻全文授权；页面必须保留来源、链接和非投资建议说明。
- 上交所公告当前接入沪市 A 股；深市 A 股和港股公告仍需继续接入对应官方来源。
- AI 仍正确保持 `待配置模型`，因为本机没有真实模型 key；不能用样例 AI 替代。

## 明日待办
- 继续补深交所公告、港交所公告和港股新闻覆盖。
- 配置真实 AI 模型 key 后进行 live smoke，并验证引用证据、成本限流和失败空白策略。

## 决策记录
- A 股公告优先用上交所公开公告接口；只展示标题、发布时间和原文链接，不抓取 PDF 正文。
- 新闻和公告成功才点亮 `已连接`；provider 失败时仍保持空白或显示失败原因，不使用 mock/sample 兜底。

## 追加记录：v31 新闻相关性过滤与 Google News RSS fallback

## 今日完成
- 已把 PWA 缓存版本升级到 `finance-ai-assistant-v31`。
- 个股新闻增加公司别名相关性过滤：`600519` 只保留匹配 `Moutai / Kweichow Moutai / 贵州茅台` 的标题，避免真实 RSS 混入明显无关新闻。
- 新闻接力新增错误识别：Yahoo 临时错误页、GDELT 限频文本会转成明确错误码，不再泛化成普通失败。
- 新增 Google News RSS 本地 Demo fallback，接力顺序为 `Alpha Vantage -> Yahoo Finance RSS -> Google News RSS -> GDELT DOC`。
- 项目进度更新为 `342 条自动化回归目标`。
- 本地 API 验证通过：`/api/news/intelligence?market=a&symbol=600519` 返回 `mode=real-provider`、来源 `google-news-rss`、5 条 Moutai/Kweichow Moutai 相关新闻。
- 右侧浏览器验证通过：`贵州茅台 · 600519` 显示 `新闻 已连接`，新闻列表含 AASTOCKS、富途牛牛、36Kr、Investing.com、Bloomberg 的 Moutai 相关新闻；不再显示无关 `China AI Supplier` 标题。

## 测试记录
- 语法检查通过：`backend/providers/news-filings-provider.mjs`、`backend/tests/backend-api.test.mjs`、`app.js`。
- 目标回归通过 `13/13`：Yahoo 相关性过滤、Yahoo 临时错误页、Google News RSS 解析与 fallback、GDELT 限频、A 股新闻 fallback、进度面板和 PWA。

## 问题风险
- Google News RSS 条款限制个人/非商业使用；当前只作为本地 Demo 公开标题 fallback，不能当作正式上线授权新闻源。
- Google News RSS 链接是 Google News 跳转链接，不是原站全文；页面只能展示标题、来源、发布时间和链接。
- AI 仍为 `待配置模型`，需要真实模型 key 后才能继续 live smoke。

## 明日待办
- 继续补深交所、港交所公告源，并为港股新闻增加相关性过滤。
- 为新闻源增加“来源授权等级”显示，区分正式授权、公开 RSS、官方公告、临时 fallback。

## 决策记录
- 真实新闻宁可为空或显示限频，也不能混入明显不相关标题冒充个股新闻。
- Google News RSS 只作为本地 Demo fallback；正式上线前必须换成授权新闻源或确认可用条款。

## 追加记录：v32 真实数据显示一致性与 Tencent Quote fallback

## 今日完成
- 已把 PWA 缓存版本升级到 `finance-ai-assistant-v32`，减少外部浏览器继续显示旧页面包的概率。
- 新增 Tencent Quote 公开行情兜底，`multi-free` 行情接力顺序更新为 `Twelve Data -> Alpha Vantage -> Yahoo Chart -> Tencent Quote`。
- 设置页本地真实行情状态已识别 `Tencent Quote 公开兜底`，避免后台可用但 UI 仍显示未就绪。
- 新闻卡片新增来源授权标签：Google News RSS 显示 `公开 RSS · 本地 Demo · 非商业限制`，上交所公告显示 `官方公告 · 公开披露`。
- `scripts/real-data-dev-server.mjs` 启动日志已同步显示 Tencent Quote fallback。
- 项目进度更新为 `344 条自动化回归目标`。
- 本地 API 验证通过：`/api/market-data/quote?market=a&code=600519` 返回 `status=ok`、`mode=real-provider-relay`、真实报价。
- 本地 API 验证通过：`/api/news/intelligence?market=a&symbol=600519&minImportance=70` 返回 Google News RSS 的 Moutai/Kweichow Moutai 相关新闻。
- 本地 API 验证通过：`/api/news/filings?market=a&symbol=600519` 返回上交所公开公告。
- 右侧浏览器验证通过：`贵州茅台 · 600519` 显示 `行情 已连接`、`新闻 已连接`、`公告 已连接`、`宏观 已连接`、`AI 待配置模型`，不再显示截图中的 `行情 待真实数据 / 新闻 接力失败 / 公告 待真实数据`。

## 测试记录
- 语法检查通过：`app.js`、`backend/providers/market-data-provider.mjs`、`backend/server.mjs`、`scripts/real-data-dev-server.mjs`、`backend/tests/backend-api.test.mjs`、`tests/frontend-regression.test.mjs`。
- 后端目标回归通过 `3/3`：Tencent symbol/quote 解析、Yahoo A 股 fallback、Yahoo 429 后 Tencent fallback。
- 前端/PWA 目标回归通过 `4/4`：进度面板和 PWA 版本。
- 合并目标回归通过 `7/7`。

## 问题风险
- Tencent Quote 和 Google News RSS 都是本地 Demo 公开端点 fallback，不代表正式授权、可商用或可公开上线的数据源。
- Yahoo Chart 当前又恢复可用，但若再次 429，Tencent fallback 会接力；两者都必须保留来源和延迟/公开端点说明。
- AI 仍缺少真实模型 API key；项目默认模型为 `gpt-5.5`，但没有 `/private/tmp/finance_ai_model_key` 时必须保持空白，不能显示样例建议。

## 明日待办
- 用户提供模型 API key 后，通过设置页或 `/private/tmp/finance_ai_model_key` 配置真实 AI，并跑 live smoke。
- 继续补深交所、港交所公告和港股个股新闻相关性过滤。
- 正式上线前替换或补充持牌行情/新闻源，并完成缓存、限流、授权、合规复核。

## 决策记录
- 截图问题的真实原因是后端行情/新闻/公告状态和外部浏览器缓存/旧选股状态叠加；本轮用 v32 缓存、真实接力和 UI 标签一起修复。
- 没有模型 key 时，不允许把 AI 状态标记为已连接；只能提示 `待配置模型`。

## 追加记录：v33 AI 配置直达入口

## 今日完成
- 已把 PWA 缓存版本升级到 `finance-ai-assistant-v33`。
- 首页 AI 空状态新增 `配置 AI 模型` 按钮，避免用户停在 `AI 待配置模型` 但找不到配置入口。
- 新增设置卡片锚点 `#ai-model-settings`，点击按钮后进入 `#settings` 并定位到 `本机 AI 模型配置` 表单。
- 项目进度更新为 `346 条自动化回归目标`。
- 右侧浏览器验证通过：`http://127.0.0.1:4173/?refresh=20260612-v33-ai-config-jump#overview` 显示 `配置 AI 模型`；点击后 URL 变为 `#settings`，设置区显示 `本机 AI 模型配置`、`key 不回显`、默认模型 `gpt-5.5`。

## 测试记录
- 语法检查通过：`app.js`、`tests/frontend-regression.test.mjs`、`backend/server.mjs`。
- 前端/PWA 目标回归通过 `6/6`：AI 空状态、AI 设置入口、进度面板、PWA 版本。
- 后端/前端/PWA 合并目标回归通过 `23/23`：后端项目进度、AI readiness 证据数、AI 空状态跳转、设置入口、PWA 版本。
- 本地后端进度接口验证通过：`/api/project/progress` 返回 `346 条自动化回归目标`。

## 问题风险
- AI 仍缺真实模型 API key；按钮只解决配置路径，不会伪造真实模型已连接。
- 用户填写模型 key 后仍需要重启或重新检测，并验证模型 structured output、引用证据、成本限流和安全审计。

## 明日待办
- 用户提供模型 key 后运行真实 AI smoke，并确认 `AI 已连接/AI 已生成` 状态切换。
- 继续完善港股公告和深交所公告源。

## 决策记录
- 在没有模型 key 前，首页应保留空白策略，但必须给出清晰的下一步入口。

## 追加记录：v37 免费 Gemini 真实模型预设

## 今日完成
- 已把 PWA 缓存版本升级到 `finance-ai-assistant-v37`。
- 设置页新增免费真实模型优先预设：`Google Gemini Flash 免费层`。
- 默认模型配置改为 provider `openai-compatible`、model `gemini-3.5-flash`、API style `chat-completions`、Base URL `https://generativelanguage.googleapis.com/v1beta/openai`。
- 后端 `ai-provider-adapter` 暴露 `freeModelProviderPreset`，项目进度真实计入免费 Gemini 预设证据。
- `scripts/real-data-dev-server.mjs` 在存在模型 key 但未指定模型时默认使用 Gemini 免费层路线。
- 新增 `docs/technical/AI_MODEL_STRATEGY.md`，记录免费模型优先、OpenAI/GPT 备选、微调门槛和独立训练可行性。
- 设置页 AI 状态不再通过后端健康检查带回 `当前 AI 服务为 Mock AI 分析服务` 旧文案。
- 项目进度更新为 `354 条自动化回归目标`；AI readiness 更新为 `97%`，证据 `25/27`。
- 右侧浏览器验证通过：`http://127.0.0.1:4173/?refresh=20260612-v37-gemini-free#settings` 显示 `Google Gemini Flash 免费层`、`gemini-3.5-flash`、`chat-completions`、AI `97%`，并确认旧 Mock AI 服务文案已消失。

## 测试记录
- 语法检查通过：`app.js`、`tests/frontend-regression.test.mjs`、`backend/server.mjs`、`backend/services/ai-provider-adapter.mjs`、`service-worker.js`。
- 后端/前端/PWA 目标回归通过 `26/26`：项目进度、AI provider、Gemini 本地配置、OpenAI Responses 备选、自动连接、设置页和 PWA 版本。

## 问题风险
- 目前仍没有用户提供的 Gemini API key，所以 AI 分析保持空白；这是严格真实数据策略，不是故障。
- Gemini 免费层需要用户在 Google AI Studio 自行申请 key，并受 Google 当前地区、额度和条款限制。
- 免费模型只是本地 Demo 优先路线；正式上线前仍需引用证据、成本限流、输出校验、安全审计、人工复核和合规复核。

## 明日待办
- 用户提供 Gemini API key 后，通过设置页保存到本机运行时并运行 `/api/analysis` live smoke。
- 验证真实模型返回结构化 JSON、来源引用、概率表达、免责声明和失败空白策略。

## 决策记录
- 当前优先路线改为免费 Gemini OpenAI-compatible 真实模型；`gpt-5.5` 保留为高推理/付费备选。
- 独立训练基础模型不作为 MVP 路线；只有在评测数据证明微调有效后，才考虑微调。

## 追加记录：v38 OpenAI 主模型配置与额度诊断

## 今日完成
- 已把 AI 路线调整为 `OpenAI API 主模型 + Gemini 2.5 Flash 免费备用 + 严格真实数据源 + 不用样例分析兜底`。
- 本机运行时确认 OpenAI 主模型配置：`gpt-5.5`、`responses`、base URL 已脱敏配置、key 只在 `/private/tmp` 运行时读取。
- `scripts/real-data-dev-server.mjs` 现在默认 OpenAI base URL + Responses API；如果配置文件存在，会读取 `/private/tmp/finance_ai_model_api_style`，避免重启后回退到旧接口风格。
- `POST /api/ai-services/local-model-config` 现在会持久化 API style 到 `/private/tmp/finance_ai_model_api_style`。
- 后端 AI provider 错误处理新增 `REAL_AI_MODEL_INSUFFICIENT_QUOTA`，能把 OpenAI `insufficient_quota` 明确显示为额度/账单问题，而不是泛泛的 429。
- 项目进度修正：仍有 blocked checks 的 readiness 项最高显示 `99%`，避免出现“100% 但仍未完成”的误导。
- 项目进度更新为 `357 条自动化回归目标`；后端验证显示本地 Demo `100%`、公开上线 `99.9%`、AI `99%`、数据源 `99%`、认证 `99%`。

## 测试记录
- 语法检查通过：`app.js`、`backend/server.mjs`、`backend/services/ai-provider-adapter.mjs`、`scripts/real-data-dev-server.mjs`、`backend/tests/backend-api.test.mjs`。
- 后端目标回归通过 `13/13`：项目进度、AI provider、AI real-data 证据包、认证/合规/上线前门禁和本机模型配置。
- 前端/PWA 目标回归通过 `6/6`，追加 AI 设置/PWA 目标回归通过 `5/5`。
- OpenAI key 状态验证：`/v1/models` 返回 HTTP 200，并确认当前 key 可见 `gpt-5.5`。
- OpenAI 真实生成 smoke：`gpt-5.5` Responses API 返回 429；极小 `gpt-4o-mini` Responses API 请求也返回 `insufficient_quota`。

## 问题风险
- OpenAI key 已经有效，但当前 OpenAI 项目额度或账单不可用，所以真实 AI 分析仍必须保持空白。
- Gemini 备用路线需要单独可用的 Gemini key；不要用 OpenAI key 尝试调用 Gemini endpoint。
- 本地 Demo 能显示真实行情、新闻、公告、宏观和模型配置状态，但正式上线仍需要授权数据源、生产认证、审计、限流、法律合规和部署监控。

## 明日待办
- 用户处理 OpenAI billing/quota 后，重新运行 `/api/analysis?symbol=MSFT&riskProfile=balanced` smoke。
- 或配置独立 Gemini fallback key，再实现/验证 OpenAI 429 后自动接力 Gemini 的真实模型 fallback。
- 继续把 AI 输出校验、引用证据、成本限流和脱敏审计从结构定义推进到可执行门禁。

## 决策记录
- 当前问题不是“没有接入 OpenAI”，而是 OpenAI 项目 `insufficient_quota`；因此不能显示 AI 分析，也不能用样例兜底。
- blocked readiness 项不再允许显示 100%，即使通过项很多，也最多显示 99%。

## 追加记录：v40 AI 主备接力与临时端口验证

## 今日完成
- 已把 PWA 缓存升级到 `finance-ai-assistant-v40`。
- 前端本地开发 API 端口支持自动推断：`4173 -> 4180`，`4174 -> 4181`，方便不停止旧服务时验证新版本。
- 后端 AI provider 已支持 OpenAI 主模型失败后接力 Gemini fallback；fallback 使用独立运行时文件，不覆盖 OpenAI 主 key。
- 本机配置接口 `POST /api/ai-services/local-model-config` 支持 `slot=primary/fallback`，Gemini 预设会保存到 fallback 槽位。
- 设置页现在始终显示模型配置表单，并显示 `AI 备用模型状态`。
- `/api/analysis` 错误响应会返回 `providerRelay`，前端/诊断可看到尝试过的模型、是否使用 fallback、fallback 是否 ready。
- 临时 v40 服务已启动：网页 `http://127.0.0.1:4174`，后端 `http://localhost:4181`。

## 测试记录
- 语法检查通过：`app.js`、`backend/server.mjs`、`backend/services/ai-provider-adapter.mjs`、`backend/services/mock-ai-service.mjs`、`scripts/real-data-dev-server.mjs`。
- 后端目标回归通过 `3/3`：AI provider、项目进度、本机模型配置。
- 前端/PWA 目标回归通过 `5/5`：项目进度、设置页 AI 配置、PWA manifest/cache。
- v40 API smoke：`/api/ai-services/provider-adapter` 返回 OpenAI 主模型 `gpt-5.5` + `responses`，Gemini fallback 槽位存在但 `configured=false`。
- v40 分析 smoke：`/api/analysis?symbol=MSFT&riskProfile=balanced` 返回 `REAL_AI_MODEL_INSUFFICIENT_QUOTA`，`providerRelay.attempted=["gpt-5.5"]`，`fallbackReady=false`。

## 问题风险
- 用户之前提供过 Gemini key，但本机没有 `/private/tmp/finance_ai_model_fallback_key`，说明当时没有持久化为 fallback key。
- 为避免在日志/回复里暴露历史密钥，不能从聊天记录中复述或写入 key；需要用户在 v40 设置页粘贴一次 Gemini key 到 `Google Gemini 2.5 Flash 免费备用`。
- 旧 4173 服务仍运行 v37/v38 页面；当前可验证的新页面是 4174 临时端口。

## 明日待办
- 用户在 `http://127.0.0.1:4174/?refresh=20260612-v40-ai-fallback#settings` 选择 `Google Gemini 2.5 Flash 免费备用` 并粘贴 Gemini key 后，重新跑 `/api/analysis`，目标是 `hasAnalysis=true`。
- Gemini fallback 验证成功后，再把新服务切回 4173/4180。

## 决策记录
- 当前已经完成代码级主备接力；真正 AI 可用还取决于可用 fallback key 或 OpenAI billing/quota。
- 不再把“已给过但未持久化”的密钥写入文档或回复，避免泄露。

## 追加记录：Gemini fallback key 持久化与限流诊断

## 今日完成
- 已把用户之前提供的 Gemini key 保存到 fallback 运行时槽位，不覆盖 OpenAI 主 key。
- fallback 运行时文件已生成：`/private/tmp/finance_ai_model_fallback_key`、`/private/tmp/finance_ai_model_fallback_id`、`/private/tmp/finance_ai_model_fallback_base_url`、`/private/tmp/finance_ai_model_fallback_api_style`。
- 后端状态验证：`fallbackModelProvider.configured=true`、`canCallLiveModel=true`、model `gemini-2.5-flash`。
- Gemini 最小真实请求验证通过：HTTP 200，返回 `OK`。
- 已发现 Gemini OpenAI-compatible endpoint 对 `response_format: json_object` 不稳定，本轮已改成 Gemini 不发送 `response_format`，并新增 compact / ultra-compact 真实 prompt 重试。
- 已新增 `REAL_AI_MODEL_RATE_LIMIT_OR_QUOTA`，用于区分非 OpenAI provider 的 429 限流/额度问题。

## 测试记录
- 语法检查通过：`backend/services/ai-provider-adapter.mjs`。
- 后端目标回归通过 `2/2`：AI provider 安全门禁、本机模型配置。
- 真实链路 smoke：OpenAI 主模型返回 `REAL_AI_MODEL_INSUFFICIENT_QUOTA`；Gemini fallback key 最小请求返回 200。
- 完整 `/api/analysis` 尚未达到 `hasAnalysis=true`；密集 smoke 后 Gemini 返回 429/请求失败，判断为 provider 免费层限流或额度冷却。

## 问题风险
- 当前不能继续密集请求 Gemini，否则可能延长限流窗口。
- 不能把最小请求成功误写成“完整 AI 分析已生成”；完整页面 AI 分析仍需冷却后重试。

## 明日待办
- 等 Gemini 限流冷却后，重新运行一次 `/api/analysis?symbol=MSFT&riskProfile=balanced`，目标为 `hasAnalysis=true`。
- 如果仍被 429 阻断，增加 UI 冷却倒计时和自动延迟重试按钮，避免用户反复手动触发。

## 决策记录
- fallback key 已经持久化；现在的问题从“没有 key”推进为“provider 限流/额度冷却”。
- 严格真实数据策略继续保持：失败时空白，不使用样例 AI 分析。
